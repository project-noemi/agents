# Pending Clarifications

This file now tracks only active, unresolved questions that still require product-owner input or an external artifact.

## Current Status

- There are no open product clarifications blocking the repository at this time.
- [2026-04-03] Resolved ROI Google Sheets Template URL (URL confirmed in `tools/roi/README.md`) and `logging-mcp` configuration scope (remains reference-only, not added to `mcp.config.json`).
- Durable answers from the March-April 2026 clarification backlog were normalized into [DECISION_LOG.md](DECISION_LOG.md), especially the entries dated `2026-04-02`.
- Questions that were superseded by implemented repo changes were closed as overtaken by events and removed from the active backlog.
- [2026-04-04] Resolved Node.js Resilience Helper scope (mandate satisfied by reference pattern; core scripts do not need retry for local filesystem ops) and Legacy Example Labeling (bulk update completed — LEGACY/ILLUSTRATIVE headers added to all Python and Bash examples).
- [2026-05-28] Bulk closure: a large block of late-cycle clarifications was either resolved by Decision entries dated 2026-05-28-0001 through 2026-05-28-0006 (logging-mcp payload alignment, onboarding directory bootstrap, SecretOps reference standardization, case-insensitive heading audits, Value Lens/Operating Profile injection, and the bulk closure of clarifications overtaken by prior decisions), or recognized as restatements of already-resolved questions. See `DECISION_LOG.md` for the durable record.
- [2026-07-05] Backlog reconciliation: fifty questions that had already been decided by the `[2026-06-19-0001]` through `[2026-06-19-0016]` decision series (shared `audit_logger.js`, sync-script parameterization, `NOEMI_DOCKER_SMOKE_*` inventory gate, Red Team vector serialization, audit-coverage scope, verify-env docker hard-fail policy, Operating Profile baseline, and the approved audit-script enhancement set) — or that duplicated still-open questions — were removed from this backlog. See Decision [2026-07-05-0001] for the full removal-to-decision mapping. Verified during removal: `scripts/audit_logger.js`, env-driven `scripts/sync-upstream.sh`, the `NOEMI_DOCKER_SMOKE_*` inventory test, and `examples/red-team-gauntlet/test-vectors.yaml` all exist in the codebase.
- [2026-07-07] Four answerable clarifications resolved via Decisions `[2026-07-07-0001]` through `[2026-07-07-0004]`: Resilience Helper Persona Integration (incremental opt-in reference pattern), Automation Branch Flow (Requirement §7 codifies the develop-only invariant, both automation prompts updated), Red Team Vector Format (shipped YAML wins; `[2026-06-19-0008]` annotated as superseded), and ROI Auditor Baseline Access (committed JSON snapshot wins; `[2026-06-12-0001]` annotated as superseded). Backlog shrinks from 19 to 15 open questions; the remaining backlog is dominated by the PO-deferred substantive-remediation and tier-template families ([2026-06-19-0009], [2026-06-19-0010], [2026-06-19-0013]) plus the four genuine Executive Assistant / Sovereign JSON / Sovereign Model governance questions.
- [2026-07-10] Overtaken-by-events closure: Questions [2026-06-10] (Empty Tier Templates Implementation Gap) and [2026-06-15] (Service Tier Template Specifications) were closed because commit `9c19548` (salvaged from stale PR #254) shipped `templates/tiers/{basic,standard,premium}.md` and `operating-profiles/standard-operating-profile.md`. Verified on `develop`: all three tier templates and the standard operating profile exist, and the profile injects into generated context. See Decision [2026-07-10-0001]. Backlog shrinks from 15 to 13, then grows to 15 with two newly verified drifts (merge-gate wildcard re-accretion on `main`; ungoverned `compassion-lens.json`).
- [2026-07-10] Two same-day drift resolutions via Decisions `[2026-07-10-0002]` and `[2026-07-10-0003]`: (a) the merge-gate wildcard re-accretion on `main` is answered — the two `doc-*` wildcards are removed and the reconciling `develop → main` release PR is assigned to a scheduled weekly routine, with the initial reconciliation authored by a human this cycle to unblock the cadence; (b) the ungoverned `compassion-lens.json` is promoted to a canonical Markdown lens (`compassion-lens.md`) following the `american-dream` pattern (`.md` + `.json` companion), and the Project NoéMI Anti-Replacement rule is defined in `value-lenses/README.md` as the governing value-lens framework concept. Backlog shrinks from 15 to 13.
- [2026-07-11] Duplicate consolidation: the substantive-remediation family ([2026-04-26], [2026-05-10], [2026-06-17] for personas; [2026-05-28], [2026-05-29] for skills) restated one PO decision five times — Decision [2026-06-19-0010] defers exactly this authorization — and is consolidated into the single canonical question [2026-04-26] below, widened to cover both personas and skills. The Casdoor pair ([2026-05-02], [2026-05-22]) asked the same integration question twice and is consolidated into [2026-05-02], keeping the more specific action prompt from [2026-05-22]. No question was answered or closed — the consolidated entries still await product-owner input. See Decision [2026-07-11-0001] for the mapping. Backlog goes from 13 to 9, then to 11 with two newly verified drifts (licensing posture not codified in requirements; sync-upstream test harness outside the validation gate).
- [2026-07-14] Scheduled Doc run: no human answers found in the backlog this cycle. Whole-codebase reality check verified: 44/44 tests and the audit gate pass, context generation is deterministic, all `**Skill:**` references resolve, all Phase 0 kit files exist, and every Python example carries the LEGACY/ILLUSTRATIVE label. Implemented Decision [2026-07-10-0002](a) on `develop`: the two re-accreted wildcards were stripped from `.github/workflows/require-develop-source.yml` (they had been carried onto `develop` by the 2026-07-13 reconciliation merge). Three new questions added below, headlined by an escalation: open PR #291 (direct-to-main Jules automation) adds a **third** wildcard to the merge gate to unblock itself. Note: open PR #290 (doc/clarifications-and-implementations-2026-07-13 → develop) would resolve five of the questions listed here ([2026-07-11] ×2, [2026-07-05] ×2, [2026-07-13] Red Team JSON) once reviewed and merged; this run deliberately did not duplicate that in-flight work. See Decision [2026-07-14-0001].
- [2026-07-13] Main/develop reconciliation on the doc-automation branch: `main`'s 2026-07-12 uplift (PR #286 and predecessors) re-raised seven questions that prior decisions had already answered or that duplicated still-open entries; six were dropped during the merge — the two "TBD"/placeholder-rejection re-raises and the Audit Log schema-validation re-raise (decided by the [2026-06-19-0005] audit-enhancement approval and the [2026-07-03] mandate, per the [2026-07-05-0001] mapping), the Agent Index regex re-raise (the `AGENTS.md` full-first-paragraph mandate already decides it; it remains tracked as a Known Limitation), the Executive Assistant Admin API re-raise (duplicate of open Q [2026-06-22]), and the SecretOps docker fatal-error re-raise (decided by [2026-06-19-0007]; tracked as a Known Limitation). Only the genuinely new Guardian Layer runtime question ([2026-07-12]) was carried over. Two newly verified drifts were added: the diverged `test-vectors.json` companion with its dangling `$schema` reference, and the missing tagged-release anchor for the FSL conversion clock. See Decision [2026-07-13-0001]. Backlog stands at 13 open questions.
- [2026-07-13] Five clarifications answered and closed via Decisions `[2026-07-13-0002]` through `[2026-07-13-0006]`: Licensing Posture ([2026-07-11]) codified as Requirement §11 with an audit consistency check; Sync-Upstream Test Harness ([2026-07-11]) wired into `validate:full` and CI (`sync-upstream-regression` job); AI Model Baseline formally reframed as dual-track ([2026-07-05] Sovereign Model Pins); Sovereign JSON Asset Layer ([2026-07-05]) decided in favor of Markdown promotion + JSON companion (matching the `compassion-lens` precedent), with substantive authoring tracked as a Known Limitation; Red Team Vector JSON Companion Divergence ([2026-07-13]) resolved — JSON regenerated as lockstep companion of the canonical YAML, `test-vectors.schema.json` shipped. Backlog shrinks from 13 to 8 open questions; the remaining eight are the six PO-deferred substantive/scope questions (Fleet Dashboard multi-tenancy, Gatekeeper mutating actions, substantive persona/skill remediation, Casdoor integration, Executive Assistant Learning Agent and Admin API), the Guardian Layer runtime choice ([2026-07-12], requires domain review), and the FSL tagged-release anchor ([2026-07-13], requires a human release cut).

- [2026-07-24] Scheduled Doc run: no human answers found in the backlog this cycle (all 11 `Answer:` fields blank). Whole-codebase reality check against `develop` confirmed the 2026-07-13 remediations (merge-gate stripped, JSON vectors in lockstep, licensing audit, sync harness in CI) and surfaced five newly verified drifts: the stale §9 test-coverage enumeration and the overstated referential-integrity limitation were corrected directly in `REQUIREMENTS.md`; the OpenAI `gpt-4o-mini` pins in the two LEGACY secret-ops examples, the guardian layer's undocumented data contract, and the ROI subsystem's README-only governance were recorded as Known Limitations and raised as the three [2026-07-24] questions below. See Decision [2026-07-24-0001]. Backlog grows from 11 to 14 open questions.

## Template for New Questions

Add new questions below this line using the required format.

```md
### ❓ Question [YYYY-MM-DD] - Short Title
**Context:** Why this question exists and what file, workflow, or contract it relates to.
**Ambiguity / Drift:** What is unclear, contradictory, or externally blocked.
**Question for Product Owner:** The specific decision that still needs to be made.
**Answer:** [LEAVE BLANK FOR HUMAN TO FILL]
**🤖 Jules Action Prompt:** *Optional implementation prompt once the answer is known.*
```

### ❓ Question [2026-04-05] - Fleet Dashboard Multi-tenancy Implementation Gap
**Context:** The `Fleet Dashboard` persona (`agents/operations/fleet-dashboard.md`) specifies a multi-tenant registry system with per-agent HMAC secrets and asynchronous verification of mutating claims (merges, closes).
**Ambiguity / Drift:** The current reference implementation in `examples/gatekeeper-deployment/dashboard-ingest.js` is a single-agent sink with hardcoded validation logic and no registry or verification workflow.
**Question for Product Owner:** Should the `Fleet Dashboard` reference implementation be expanded to include the registry and asynchronous verification logic, or should the persona be simplified to reflect the current single-tenant implementation?
**Answer:** [LEAVE BLANK FOR HUMAN TO FILL]
**🤖 Jules Action Prompt:** *Implement the multi-tenant agent registry and asynchronous GitHub verification worker in the Fleet Dashboard reference stack.*

### ❓ Question [2026-04-23] - Gatekeeper Reference Implementation Mutating Actions
**Context:** The `Gatekeeper` agent persona describes mutating actions (merging PRs, closing issues), but the reference implementation in `examples/gatekeeper-deployment/` is currently limited to signed reporting.
**Ambiguity / Drift:** The implementation truth drifts from the persona specification, leaving the "Accelerator" role partially unimplemented in the reference stack.
**Question for Product Owner:** Should we extend the Gatekeeper reference implementation to include "dry-run" or optional mutating actions to better reflect the persona's mission?
**Answer:** [LEAVE BLANK FOR HUMAN TO FILL]
**🤖 Jules Action Prompt:** *Extend the Gatekeeper deployment example and `entrypoint.sh` to include a placeholder or dry-run mode for the mutating actions defined in the Gatekeeper persona.*

### ❓ Question [2026-04-26] - Substantive Content Remediation for Personas and Skills (Consolidated)
**Context:** Whole-codebase audits confirm that 100% of reusable skills and most agent personas use identical "TBD"/boilerplate text for the mandatory `Data Inventory` and `Refusal Criteria` sections. Decision [2026-06-19-0010] deferred the bulk-remediation authorization to the Product Owner and domain experts, and the question has since been restated four times without new information. This consolidated entry replaces [2026-05-10], [2026-06-17] (personas) and [2026-05-28], [2026-05-29] (skills) — see Decision [2026-07-11-0001].
**Ambiguity / Drift:** While structurally compliant (the headings exist), the fleet is in substantive drift from the 4D Description (D2) and Refusal Principle mandates. Agents and skills lack role-specific data definitions and safety-gating logic, creating a false sense of production readiness.
**Question for Product Owner:** Do you authorize a fleet-wide substantive remediation (personas and skills) using technically grounded defaults derived from each spec's documented Role/Procedure, or should remediation remain incremental, applied only when domain work touches a given spec?
**Answer:** [LEAVE BLANK FOR HUMAN TO FILL]
**🤖 Jules Action Prompt:** *Perform a fleet-wide substantive remediation of `agents/` and `skills/`, replacing "TBD"/boilerplate `Data Inventory` and `Refusal Criteria` sections (and template-only Audit Log JSON in skills) with role-specific, technically accurate content derived from each spec's documented Role and Procedure.*

### ❓ Question [2026-05-02] - Casdoor Identity Integration Gap (Consolidated)
**Context:** `DECISION_LOG.md` and `REQUIREMENTS.md` identify Casdoor as the reference identity layer for multi-tenant fleet deployments, and `examples/fleet-deployment/docker-compose.yml` includes a Casdoor service. Decision [2026-06-19-0013] deferred the integration decision to the Product Owner. This consolidated entry replaces the duplicate [2026-05-22] — see Decision [2026-07-11-0001].
**Ambiguity / Drift:** No code in the repository (scripts, middleware, or agent personas) actually performs Casdoor token validation or user-context extraction, so the identity-layer requirement is not yet "truthful" as a reference implementation.
**Question for Product Owner:** Should Jules implement a reference `casdoor-auth` skill and/or JWT validation middleware for the reference services to make the identity layer demonstrable, or should the Casdoor references remain compose-level only?
**Answer:** [LEAVE BLANK FOR HUMAN TO FILL]
**🤖 Jules Action Prompt:** *Create a `skills/security/casdoor-validate.md` specification and implement basic JWT validation middleware in `examples/gatekeeper-deployment/dashboard-ingest.js`.*

### ❓ Question [2026-06-20] - Executive Assistant "Learning Agent" Resolution Path
**Context:** The `Executive Assistant` reference implementation in `tools/executive-assistant/server.js` includes an `/api/resolution` endpoint that claims to be processed by a "Learning Agent" and updates internal execution logs with "Historical messages passed".
**Ambiguity / Drift:** This "Learning Agent" and the human-in-the-loop resolution pattern are not defined in `REQUIREMENTS.md` or any agent persona. This suggests a feature drift where the implementation introduces a feedback loop that isn't governed by the requirements.
**Question for Product Owner:** Is the "Learning Agent" resolution path a core architectural requirement for NoéMI reference services, and if so, should it be formalized in `REQUIREMENTS.md` and supported by a dedicated agent persona?
**Answer:** [LEAVE BLANK FOR HUMAN TO FILL]
**🤖 Jules Action Prompt:** *Document the "Learning Agent" resolution pattern in `REQUIREMENTS.md` and create a dedicated persona specification in `agents/operations/learning-agent.md` that governs how agents should learn from human resolutions.*

### ❓ Question [2026-06-22] - Executive Assistant Admin API Formalization
**Context:** The requirements state that agents and reference services must adhere to strict observability and security standards, but the codebase in `tools/executive-assistant/server.js` implements several undocumented admin endpoints (`/api/queue`, `/api/stats`, `/api/logs`, `/api/rules`).
**Ambiguity / Drift:** These endpoints provide direct access to internal agent state and configuration without any documented authentication or authorization requirement in `REQUIREMENTS.md`. This creates a "shadow" management surface that isn't governed by the project's security baseline.
**Question for Product Owner:** Should these admin endpoints be formalized in the requirements as a standard "Agent Control Plane" and secured via the reference identity layer (Casdoor), or should they be removed from the public reference implementation?
**Answer:** [LEAVE BLANK FOR HUMAN TO FILL]
**🤖 Jules Action Prompt:** *Formalize the Agent Control Plane API requirements in `REQUIREMENTS.md` and implement JWT-based authorization for all `/api/` endpoints in the `executive-assistant` tool.*

### ❓ Question [2026-07-12] - Guardian Layer Implementation Path (Node.js vs. Python)
**Context:** The `guardian-layer/` implements security evaluation logic in Python (`guardian_evaluator.py`), but Requirement §8 states that Node.js is the canonical implementation path and Python is for historical/illustrative purposes.
**Ambiguity / Drift:** Implementing core security/guardian logic in Python creates a maintenance split and deviates from the Node.js baseline for modern reference services.
**Question for Product Owner:** Should the `guardian-layer` be re-implemented in Node.js to align with the canonical architecture, or is Python the preferred runtime for LLM evaluation logic in this repository?
**Answer:** [LEAVE BLANK FOR HUMAN TO FILL]
**🤖 Jules Action Prompt:** *Re-implement the `guardian-layer/guardian_evaluator.py` logic in Node.js, ensuring it integrates with the shared `audit_logger.js` and follows the ESM baseline.*

### ❓ Question [2026-07-13] - FSL Conversion Clock Has No Tagged Release Anchor
**Context:** Decision [2026-07-05-0004] (Relicense to FSL-1.1-Apache-2.0) states that "Tagged GitHub releases become the canonical published versions so the rolling two-year conversion timeline is legally unambiguous" and that "a tagged release is to be cut at the relicense merge commit so the first FSL conversion clock has an unambiguous start date." The repository's only tag is `v0.1.0` (2026-05-25), which predates the relicense commit `c3c0e82` (2026-07-05) and therefore falls under the prior SSPL v1 terms.
**Ambiguity / Drift:** Until a release is tagged at or after the relicense commit, no published version carries an unambiguous FSL two-year Apache-2.0 conversion start date — the central legal mechanism of the new license model is inert. This is an external action (cutting a GitHub release) that no automation in this repository is authorized to perform unilaterally.
**Question for Product Owner:** Should a tagged release (e.g., `v0.2.0`) be cut at the relicense merge commit on `main` as Decision [2026-07-05-0004] prescribes, and do you want to cut it yourself or delegate the tagging to an authorized automation run?
**Answer:** [LEAVE BLANK FOR HUMAN TO FILL]
**🤖 Jules Action Prompt:** *Cut a tagged GitHub release at the relicense merge commit on `main` per Decision [2026-07-05-0004], and record the tag name and date in `DECISION_LOG.md` so the first FSL conversion clock has an auditable anchor.*

### ❓ Question [2026-07-14] - Direct-to-Main Automation Now Modifies the Merge Gate to Unblock Itself
**Context:** Requirement §7 codifies the develop-only merge invariant, and Decision [2026-07-10-0002] sub-task (ii) already calls for retargeting the automation that opens direct-to-main PRs. Since then the pattern has escalated: PR #286 (2026-07-12, branch `doc-requirements-uplift-2026-07-12-*`) merged straight into `main` through a wildcard, and PR #291 (2026-07-14, branch `doc-requirements-drift-uplift-20260713-*`, a Jules task run as user WSwarm) targets `main` and its "resolve CI failure" commit **adds a third wildcard** (`^doc-requirements-drift-uplift-`) to `.github/workflows/require-develop-source.yml` so its own PR can pass the gate.
**Ambiguity / Drift:** The governance control is being edited by the very automation it is meant to constrain — each occurrence re-diverges `main` from `develop` and invalidates the §7 invariant. PR #291's content is also based on stale `main` context and conflicts with `develop`'s newer documentation state. No repository control (CODEOWNERS, branch protection rule, audit check) currently prevents automation from modifying the gate file.
**Question for Product Owner:** Should PR #291 be closed unmerged, and can you reconfigure or disable the external Jules doc-uplift task (it runs outside this repository's prompts, so no in-repo edit can retarget it) so future runs base off and target `develop`? Should modifications to `.github/workflows/require-develop-source.yml` additionally require human review via CODEOWNERS?
**Answer:** [LEAVE BLANK FOR HUMAN TO FILL]
**🤖 Jules Action Prompt:** *Add a `.github/CODEOWNERS` entry requiring human review for `.github/workflows/require-develop-source.yml`, and document the automation-must-not-edit-the-gate rule in `AGENTS.md` and `CONTRIBUTING.md`.*

### ❓ Question [2026-07-14] - Merge-Gate Invariant Audit Guard
**Context:** The requirements state (§7) that any hotfix exception to the develop-only gate must be "a single explicit entry... never a broad wildcard," but wildcards have now been re-added to `.github/workflows/require-develop-source.yml` three separate times by automation (2026-07-07, 2026-07-09, and the pending PR #291 on 2026-07-14). `scripts/audit-repo.js` audits persona/skill contracts and generator invariants but performs no check on the gate file itself.
**Ambiguity / Drift:** A drift pattern that has recurred three times in one week has no automated detection — each recurrence was found manually by a scheduled doc run. Because `npm run audit` executes in CI on every push and PR, an audit check would catch wildcard re-accretion at the moment a PR introduces it.
**Question for Product Owner:** Should `scripts/audit-repo.js` gain a merge-gate invariant check that fails when `require-develop-source.yml` contains any regex/wildcard branch exception (i.e., anything beyond the literal `develop` comparison and explicitly documented single-branch exceptions)?
**Answer:** [LEAVE BLANK FOR HUMAN TO FILL]
**🤖 Jules Action Prompt:** *Extend `scripts/audit-repo.js` with a check that parses `.github/workflows/require-develop-source.yml` and fails the audit if the source-branch condition contains any `=~` wildcard pattern, keeping the gate aligned with Requirement §7.*

### ❓ Question [2026-07-14] - package.json Version Metadata Never Tracks Tagged Releases
**Context:** Decision [2026-07-05-0004] makes tagged GitHub releases the canonical published versions for the FSL conversion clock, and the repository already carries tag `v0.1.0`. However, `package.json` declares `"version": "0.0.0"`, and nothing in the release flow or `scripts/audit-repo.js` keeps the npm manifest version aligned with the release tags.
**Ambiguity / Drift:** Consumers and forks inspecting the manifest see a placeholder version that matches no published release, and once the FSL clock starts ticking per version, ambiguous version metadata weakens the "legally unambiguous" versioning the license model depends on.
**Question for Product Owner:** Should `package.json` `version` be bumped as part of each `develop → main` release PR to match the release tag (with an audit or CI check for tag/manifest agreement), or is the manifest version intentionally frozen at `0.0.0` because the package is private and never published?
**Answer:** [LEAVE BLANK FOR HUMAN TO FILL]
**🤖 Jules Action Prompt:** *Update the release routine documentation to include bumping `package.json` `version` to the release tag on each `develop → main` release PR, and add a consistency note (or audit check) that the manifest version matches the latest tag.*

### ❓ Question [2026-07-24] - OpenAI Model Pins in LEGACY Examples vs. the Dual-Track Baseline
**Context:** The requirements state the AI Model Baseline is dual-track — Gemini 2.5 Flash (cloud) and DeepSeek/Llama (sovereign), with "additions to either track requir[ing] a new decision entry" (Decision [2026-07-13-0004]) — but the codebase in `infrastructure/secret-ops/agent_logic.py` (lines 67, 74) requires `OPENAI_API_KEY` and defaults `LLM_MODEL` to `gpt-4o-mini`, and `examples/secure-secret-management/agent_logic.py` (line 11) also reads `OPENAI_API_KEY`. Both files carry the mandated LEGACY/ILLUSTRATIVE label.
**Ambiguity / Drift:** A third model provider now ships in the reference tree with no track membership: the approved non-baseline model-pin audit (Decision [2026-06-19-0012]) would flag both files once implemented, yet nothing says whether LEGACY-labeled examples are exempt. The two examples also disagree on the database secret name (`ERP_DATABASE_URL` vs. `DATABASE_URL`), so the two canonical Fetch-on-Demand walkthroughs teach inconsistent variable inventories.
**Question for Product Owner:** Should the two LEGACY secret-ops examples be migrated to the Gemini 2.5 Flash cloud track (renaming the API-key variable accordingly), or should the model-pin audit explicitly exempt LEGACY/ILLUSTRATIVE-labeled files — and should one canonical database secret name be chosen for both examples?
**Answer:** [LEAVE BLANK FOR HUMAN TO FILL]
**🤖 Jules Action Prompt:** *Migrate `infrastructure/secret-ops/agent_logic.py` and `examples/secure-secret-management/agent_logic.py` to the Gemini 2.5 Flash baseline (or add the decided LEGACY exemption to the planned model-pin audit), and unify the database secret name across both examples per the PO's choice.*

### ❓ Question [2026-07-24] - Guardian Layer Data Contract: Normative or Incidental?
**Context:** The requirements state only that "Guardian-layer patterns remain a core architectural requirement" (§7), but the codebase in `guardian-layer/guardian_evaluator.py` implements a specific security contract: a `Public`/`Internal`/`Confidential` classification taxonomy with fail-secure coercion of unknown values to `Confidential` (lines 84-99), a `0.6` default confidence threshold below which output is blocked (line 66), fixed `APPROVED`/`BLOCKED` response shapes, a fail-closed 2s/4s/8s/16s backoff, and a `LOKI_PUSH_URL` (`/loki/api/v1/push`) observability contract.
**Ambiguity / Drift:** Downstream implementers (including the possible Node.js re-implementation pending in Clarification [2026-07-12]) cannot tell which of these values are binding requirements and which are incidental Python-reference choices. If the 0.6 threshold or the fail-secure coercion rule is normative, re-implementations that drop them would silently weaken the guardian layer while appearing compliant.
**Question for Product Owner:** Should the guardian layer's implemented contract (classification taxonomy, fail-secure coercion, confidence threshold, response shapes, fail-closed backoff, Loki push path) be codified as normative requirements under §7, or documented as reference-implementation defaults that implementations may tune?
**Answer:** [LEAVE BLANK FOR HUMAN TO FILL]
**🤖 Jules Action Prompt:** *Codify the guardian-layer data contract in `REQUIREMENTS.md` §7 (or as a reference-defaults appendix, per the PO's choice), citing the concrete values implemented in `guardian-layer/guardian_evaluator.py` so any re-implementation preserves them.*

### ❓ Question [2026-07-24] - ROI Subsystem Governance Elevation
**Context:** The requirements contain no section governing the ROI subsystem, but `tools/roi/README.md` defines an automation contract for the `roi-auditor` persona (append-only writes to `03_EXECUTION_LOG`; baselines in `01_ASSUMPTIONS`/`02_TASK_CATALOG` never rewritten without human approval; approval-sensitive tabs protected) and publishes the ROI calculator as a **public, anonymously readable** Google Sheet (URL confirmed by Decision [2026-04-03]).
**Ambiguity / Drift:** A deliberate public data surface and an append-only mutation contract are governance controls of the same class as §7's trust controls, yet they bind only through a tool README that no audit or requirement protects — a future edit could silently drop the append-only rule or de-anonymize/expand the public surface. The related baseline-snapshot decision ([2026-07-07-0004]) also remains unimplemented: `tools/roi/baseline-config.json` does not exist.
**Question for Product Owner:** Should the ROI automation contract and the public-sheet exposure policy be elevated into `REQUIREMENTS.md` (e.g., a subsection under §7 or §8), and should the still-missing `tools/roi/baseline-config.json` snapshot be implemented in the same pass?
**Answer:** [LEAVE BLANK FOR HUMAN TO FILL]
**🤖 Jules Action Prompt:** *Add an ROI-subsystem governance subsection to `REQUIREMENTS.md` codifying the append-only automation contract and public-sheet policy from `tools/roi/README.md`, and ship the `tools/roi/baseline-config.json` snapshot with its one-shot export helper per Decision [2026-07-07-0004].*
