# Pending Clarifications

This file now tracks only active, unresolved questions that still require product-owner input or an external artifact.

## Current Status

- There are no open product clarifications blocking the repository at this time.
- Durable answers from the March-April 2026 clarification backlog were normalized into [DECISION_LOG.md](DECISION_LOG.md), especially the entries dated `2026-04-02`.
- Questions that were superseded by implemented repo changes were closed as overtaken by events and removed from the active backlog.
- [2026-04-03] Resolved ROI Google Sheets Template URL (URL confirmed in `tools/roi/README.md`) and `logging-mcp` configuration scope (remains reference-only, not added to `mcp.config.json`).
- [2026-04-04] Resolved Node.js Resilience Helper scope (mandate satisfied by reference pattern; core scripts do not need retry for local filesystem ops) and Legacy Example Labeling (bulk update completed — LEGACY/ILLUSTRATIVE headers added to all Python and Bash examples).
- [2026-05-28] Bulk closure: a large block of late-cycle clarifications was either resolved by Decision entries dated 2026-05-28-0001 through 2026-05-28-0006 (logging-mcp payload alignment, onboarding directory bootstrap, SecretOps reference standardization, case-insensitive heading audits, Value Lens/Operating Profile injection, and the bulk closure of clarifications overtaken by prior decisions), or recognized as restatements of already-resolved questions. See `DECISION_LOG.md` for the durable record.
- [2026-07-05] Backlog reconciliation: fifty questions that had already been decided by the `[2026-06-19-0001]` through `[2026-06-19-0016]` decision series (shared `audit_logger.js`, sync-script parameterization, `NOEMI_DOCKER_SMOKE_*` inventory gate, Red Team vector serialization, audit-coverage scope, verify-env docker hard-fail policy, Operating Profile baseline, and the approved audit-script enhancement set) — or that duplicated still-open questions — were removed from this backlog. See Decision [2026-07-05-0001] for the full removal-to-decision mapping. Verified during removal: `scripts/audit_logger.js`, env-driven `scripts/sync-upstream.sh`, the `NOEMI_DOCKER_SMOKE_*` inventory test, and `examples/red-team-gauntlet/test-vectors.yaml` all exist in the codebase.
- [2026-07-07] Four answerable clarifications resolved via Decisions `[2026-07-07-0001]` through `[2026-07-07-0004]`: Resilience Helper Persona Integration (incremental opt-in reference pattern), Automation Branch Flow (Requirement §7 codifies the develop-only invariant, both automation prompts updated), Red Team Vector Format (shipped YAML wins; `[2026-06-19-0008]` annotated as superseded), and ROI Auditor Baseline Access (committed JSON snapshot wins; `[2026-06-12-0001]` annotated as superseded). Backlog shrinks from 19 to 15 open questions; the remaining backlog is dominated by the PO-deferred substantive-remediation and tier-template families ([2026-06-19-0009], [2026-06-19-0010], [2026-06-19-0013]) plus the four genuine Executive Assistant / Sovereign JSON / Sovereign Model governance questions.
- [2026-07-10] Overtaken-by-events closure: Questions [2026-06-10] (Empty Tier Templates Implementation Gap) and [2026-06-15] (Service Tier Template Specifications) were closed because commit `9c19548` (salvaged from stale PR #254) shipped `templates/tiers/{basic,standard,premium}.md` and `operating-profiles/standard-operating-profile.md`. Verified on `develop`: all three tier templates and the standard operating profile exist, and the profile injects into generated context. See Decision [2026-07-10-0001]. Backlog shrinks from 15 to 13, then grows to 15 with two newly verified drifts (merge-gate wildcard re-accretion on `main`; ungoverned `compassion-lens.json`).
- [2026-07-10] Two same-day drift resolutions via Decisions `[2026-07-10-0002]` and `[2026-07-10-0003]`: (a) the merge-gate wildcard re-accretion on `main` is answered — the two `doc-*` wildcards are removed and the reconciling `develop → main` release PR is assigned to a scheduled weekly routine, with the initial reconciliation authored by a human this cycle to unblock the cadence; (b) the ungoverned `compassion-lens.json` is promoted to a canonical Markdown lens (`compassion-lens.md`) following the `american-dream` pattern (`.md` + `.json` companion), and the Project NoéMI Anti-Replacement rule is defined in `value-lenses/README.md` as the governing value-lens framework concept. Backlog shrinks from 15 to 13.
- [2026-07-11] Duplicate consolidation: the substantive-remediation family ([2026-04-26], [2026-05-10], [2026-06-17] for personas; [2026-05-28], [2026-05-29] for skills) restated one PO decision five times — Decision [2026-06-19-0010] defers exactly this authorization — and is consolidated into the single canonical question [2026-04-26] below, widened to cover both personas and skills. The Casdoor pair ([2026-05-02], [2026-05-22]) asked the same integration question twice and is consolidated into [2026-05-02], keeping the more specific action prompt from [2026-05-22]. No question was answered or closed — the consolidated entries still await product-owner input. See Decision [2026-07-11-0001] for the mapping. Backlog goes from 13 to 9, then to 11 with two newly verified drifts (licensing posture not codified in requirements; sync-upstream test harness outside the validation gate).
- [2026-07-13] Main/develop reconciliation on the doc-automation branch: `main`'s 2026-07-12 uplift (PR #286 and predecessors) re-raised seven questions that prior decisions had already answered or that duplicated still-open entries; six were dropped during the merge — the two "TBD"/placeholder-rejection re-raises and the Audit Log schema-validation re-raise (decided by the [2026-06-19-0005] audit-enhancement approval and the [2026-07-03] mandate, per the [2026-07-05-0001] mapping), the Agent Index regex re-raise (the `AGENTS.md` full-first-paragraph mandate already decides it; it remains tracked as a Known Limitation), the Executive Assistant Admin API re-raise (duplicate of open Q [2026-06-22]), and the SecretOps docker fatal-error re-raise (decided by [2026-06-19-0007]; tracked as a Known Limitation). Only the genuinely new Guardian Layer runtime question ([2026-07-12]) was carried over. Two newly verified drifts were added: the diverged `test-vectors.json` companion with its dangling `$schema` reference, and the missing tagged-release anchor for the FSL conversion clock. See Decision [2026-07-13-0001]. Backlog stands at 13 open questions.
- [2026-07-13] Five clarifications answered and closed via Decisions `[2026-07-13-0002]` through `[2026-07-13-0006]`: Licensing Posture ([2026-07-11]) codified as Requirement §11 with an audit consistency check; Sync-Upstream Test Harness ([2026-07-11]) wired into `validate:full` and CI (`sync-upstream-regression` job); AI Model Baseline formally reframed as dual-track ([2026-07-05] Sovereign Model Pins); Sovereign JSON Asset Layer ([2026-07-05]) decided in favor of Markdown promotion + JSON companion (matching the `compassion-lens` precedent), with substantive authoring tracked as a Known Limitation; Red Team Vector JSON Companion Divergence ([2026-07-13]) resolved — JSON regenerated as lockstep companion of the canonical YAML, `test-vectors.schema.json` shipped. Backlog shrinks from 13 to 8 open questions; the remaining eight are the six PO-deferred substantive/scope questions (Fleet Dashboard multi-tenancy, Gatekeeper mutating actions, substantive persona/skill remediation, Casdoor integration, Executive Assistant Learning Agent and Admin API), the Guardian Layer runtime choice ([2026-07-12], requires domain review), and the FSL tagged-release anchor ([2026-07-13], requires a human release cut).
- [2026-07-14] Scheduled Doc run: no human answers found in the backlog this cycle. Whole-codebase reality check verified: 44/44 tests and the audit gate pass, context generation is deterministic, all `**Skill:**` references resolve, all Phase 0 kit files exist, and every Python example carries the LEGACY/ILLUSTRATIVE label. Implemented Decision [2026-07-10-0002](a) on `develop`: the two re-accreted wildcards were stripped from `.github/workflows/require-develop-source.yml` (they had been carried onto `develop` by the 2026-07-13 reconciliation merge). Three new questions added below, headlined by an escalation: open PR #291 (direct-to-main Jules automation) adds a **third** wildcard to the merge gate to unblock itself. Note: open PR #290 (doc/clarifications-and-implementations-2026-07-13 → develop) would resolve five of the questions listed here ([2026-07-11] ×2, [2026-07-05] ×2, [2026-07-13] Red Team JSON) once reviewed and merged; this run deliberately did not duplicate that in-flight work. See Decision [2026-07-14-0001].
- [2026-07-16] Scheduled Doc run: no human answers found in the backlog this cycle (all `Answer:` fields blank). Instead of re-raising settled questions, this run implemented four already-decided items: the `FORCE_DOCKER_SMOKE` mandatory mode plus CI wiring (Decision [2026-07-05-0006]), the Phase 0 kit audit check (Decision [2026-07-05-0011]), Audit Log five-key JSON schema validation (Requirement §3 / Decision [2026-05-20]), and the `tools/roi/baseline-config.json` snapshot with ROI Auditor persona alignment (Decision [2026-07-07-0004]). All were verified (audit passes on the healthy tree, negative tests trip each new check, 44/44 tests pass). Reality-check escalation note: the external Jules doc-uplift task produced its **fourth consecutive direct-to-main PR** (#295, opened 2026-07-16) after #291 and #294 were closed unmerged by hand — the [2026-07-14] gate-governance question remains the highest-leverage open item. Two new questions added below dated [2026-07-16] (Executive Assistant broken container build; committed coverage artifacts); neither duplicates the pending backlog or the in-flight PR #293 (which owns the compassion-lens registries and the `infrastructure/secret-ops` / `n8n-templates` directory questions). See Decision [2026-07-16-0001].
- [2026-07-17] Scheduled Doc run: no human answers found in the backlog this cycle (every `Answer:` field blank). Reality check on `develop` verified: audit gate and 44/44 tests pass, generators are deterministic (no diff on regeneration), all `**Skill:**` references resolve, and the Known Limitations spot-checks hold (`FORCE_DOCKER_SMOKE`, `.mjs` shims, and `tools/roi/baseline-config.json` still pending as documented; `checkLicensingPosture` and the `sync-upstream-regression` CI job real as documented). **Escalation evidence updated with diff verification:** all four nightly direct-to-main Jules PRs (#291, #294, #295, #297) carry the identical gate-tampering commit — see the amended [2026-07-14] question below and the updated §7 Known Limitation. This run deliberately did not duplicate in-flight draft PRs #293 (compassion-lens registries, directory-integration questions) and #296 (four decision-backed implementations, executive-assistant build drift); one new question was added on the doc-PR review cadence those drafts expose. Backlog goes from 11 to 12. See Decision [2026-07-17-0001].
- [2026-07-19] Scheduled Doc run: no human answers found in the backlog this cycle. Working base was fast-forwarded to `develop` per the Requirement §7 invariant (a `main`-based run would have re-raised questions the 2026-07-13 decision series already closed). Whole-codebase reality check verified: `npm run validate` and 44/44 tests pass, context regeneration is deterministic. One drift fixed directly (stale `Repository Layout` in `templates/context/CLAUDE.template.md` — nine missing top-level directories, no PO decision required); two newly verified drifts recorded as Known Limitations and raised as the two questions dated [2026-07-19] below: the ungoverned `n8n-templates/layer-b-labs/` Layer B asset (added 2026-06-21, never entered the governance surface) and the orphaned `infrastructure/secret-ops/` Phase 0 boilerplate with its off-track `gpt-4o-mini` pin. Backlog grows from 11 to 13 open questions. See Decision [2026-07-19-0001].
- [2026-07-21] Scheduled Doc run: no human answers found in the backlog this cycle (all 11 `Answer:` fields blank), so no requirements rewrites or archive moves from the feedback loop. Whole-codebase reality check on `develop`: `npm run validate` passes (repository audit including `checkLicensingPosture` + 44/44 tests), `test:sync` is wired into `package.json`/`validate:full` as Requirement §9 states, the develop copy of the merge gate remains stripped to the bare develop-only check, and the Phase 0 kit directory holds 9 files (8 mandated + `README.md`). Two escalation facts recorded (diff-verified via the GitHub API): the external Jules task fired nightly through 2026-07-20 — five direct-to-main PRs then open (#295, #297, #299, #301, #304), the newest two adding a blanket `^jules-` wildcard to the gate — and the governed doc-automation pipeline itself is stalled (six unreviewed draft doc PRs #293–#303; `develop` frozen since 2026-07-15; first weekly `develop → main` release PR still unauthored). Two new questions added below; drifts already recorded in the six in-flight draft PRs were deliberately not re-raised. See Decision [2026-07-21-0001].
- [2026-07-22] Scheduled Doc run: Phase 1 again a no-op (all `Answer:` fields blank). The `develop` tree is unchanged since 2026-07-15, so the codebase reality check found no new tree-side drift; the materially new facts are GitHub-side — an **eighth** consecutive direct-to-main Jules PR (#306, 2026-07-21, diff-verified to add both `^doc-requirements-drift-uplift-` and blanket `^jules-` wildcards to the gate) and the review stall deepening to seven unreviewed draft doc PRs (#293–#305). This run's PR **subsumes** the unmerged 2026-07-21 run (PR #305): it carries #305's escalation-record update, stall record, and two [2026-07-21] questions forward with evidence updated through #306, so reviewers can merge one PR and close #305. No new questions raised — every drift found is already pending here or recorded in an in-flight draft PR. See Decision [2026-07-22-0001].
- [2026-07-23] Scheduled Doc run: Phase 1 again a no-op (all `Answer:` fields blank — verified on both `main`'s and `develop`'s copies of this backlog). The `develop` tree remains frozen at `23bc278`; `npm run validate` passes on it (repository audit including `checkLicensingPosture` + 44/44 tests), the tag inventory is still only the pre-relicense `v0.1.0`, and `package.json` remains at `0.0.0` — so all tree-side Known Limitations were re-verified, not re-discovered. Materially new facts are GitHub-side: Jules direct-to-main runs **nine** (#308, 2026-07-22, diff-verified to edit the gate with `^doc-requirements-drift-uplift-` + blanket `^jules-` wildcards) and **ten** (#309, 2026-07-23, docs-only — the first run without a gate edit, but still targeting `main` and re-raising already-consolidated questions), bringing the open direct-to-main set to eight PRs; the review stall deepened to eight unreviewed draft doc PRs (#293–#307). This run's PR **subsumes** the unmerged 2026-07-22 run (PR #307, which itself subsumed #305): reviewers can merge this one PR and close #305/#307 with no information loss. Drift candidates found this cycle (`n8n-templates/layer-b-labs/` and `infrastructure/secret-ops/` unreferenced; FSL tag anchor; `package.json` version) were all confirmed already tracked — in draft PR #302's record and in the standing [2026-07-13]/[2026-07-14] questions — so no new questions were raised. See Decision [2026-07-23-0001].
- [2026-07-24] Scheduled Doc run: no human answers found in the backlog this cycle (all 11 `Answer:` fields blank). Whole-codebase reality check against `develop` confirmed the 2026-07-13 remediations (merge-gate stripped, JSON vectors in lockstep, licensing audit, sync harness in CI) and surfaced five newly verified drifts: the stale §9 test-coverage enumeration and the overstated referential-integrity limitation were corrected directly in `REQUIREMENTS.md`; the OpenAI `gpt-4o-mini` pins in the two LEGACY secret-ops examples, the guardian layer's undocumented data contract, and the ROI subsystem's README-only governance were recorded as Known Limitations and raised as the three [2026-07-24] questions below. See Decision [2026-07-24-0001]. Backlog grows from 11 to 14 open questions.
- [2026-07-25] Scheduled Doc run: no human answers found in the backlog this cycle (all 11 `Answer:` fields blank). Reality check on `develop` (`23bc278`): `npm run audit` and the 44/44 test suite pass, `node scripts/generate_all.js` is deterministic (zero diff), model pins in `examples/workflows/` and `n8n-templates/` are canonical (`models/gemini-2.5-flash`), and `examples/rfp-split/` naming is verified remediated. Gate-tampering follow-up: PR #291 was closed unmerged on 2026-07-14 (first half of [2026-07-10-0002] sub-task (ii)), but the external automation was not retargeted — nine direct-to-main PRs are open (#295–#311), and the newest (#311, 2026-07-24) escalates the pattern by adding both a `^doc-requirements-drift-uplift-` wildcard **and a blanket `^jules-` wildcard** to the merge gate, while unilaterally rewriting the Requirement §1 Phase 0 kit inventory that an unmerged sibling run ([2026-07-21] question in PR #310) asks the PO to decide. Two new questions added below (documentation-mirror contract drift; ungoverned `examples/gmu-validation/`), chosen specifically to not duplicate the findings already recorded in the ten unmerged sibling doc PRs (#293–#312). See Decision [2026-07-25-0001].
- [2026-07-26] Scheduled Doc run: no human answers found in the backlog this cycle (all `Answer:` fields blank on `develop` and in every unmerged doc-run branch), so Phase 1 was a no-op. Instead of opening a twelfth parallel record, this run **consolidated the eleven unreviewed doc-run PRs (#293–#314) into a single omnibus branch**: all nine non-subsumed branches were merged with newest-timeline-wins conflict resolution, duplicate findings were deduplicated (the [2026-07-15] directories question folded into the more specific [2026-07-19] pair; the [2026-07-16] coverage question folded into [2026-07-20]; the [2026-07-17] cadence question folded into [2026-07-21]; the moot [2026-07-15] wildcard-strip-ownership question dropped — Decision [2026-07-14-0001] already implemented the strip), and the merged tree was re-validated (`npm run audit` + full test suite + regeneration determinism). New GitHub-side evidence recorded: direct-to-main PR #313 (2026-07-25) is diff-verified to add both the `^doc-requirements-drift-uplift-` and blanket `^jules-` gate wildcards, bringing the open direct-to-main set to ten. See Decision [2026-07-26-0001].
- [2026-07-28] Scheduled Doc run: Phase 1 again a no-op (all 23 `Answer:` fields blank on `develop` and on the omnibus branch). Tree-side reality check on the omnibus tree: `npm run audit` passes, 44/44 tests pass, and `node scripts/generate_all.js` is deterministic (zero diff) — no new tree-side drift. Materially new facts are GitHub-side: three more nightly direct-to-main Jules PRs — #316 (2026-07-26), #317 (2026-07-27), #318 (2026-07-28) — bring the open direct-to-main set to **thirteen**, and all three are diff-verified to edit the merge gate with the `^doc-requirements-drift-uplift-` and blanket `^jules-` wildcards. PR #317 marks a new escalation shape: it is **gate-edit-only** — its entire diff is the one-line wildcard insertion into `.github/workflows/require-develop-source.yml`, with no documentation payload at all. The doc payloads of #316/#318 re-raise three already-settled questions and were not salvaged (vitest testing-framework standardization → executive-assistant build/coverage territory already tracked by [2026-07-20] and Decision [2026-07-17-0001]; Phase 0 "exactly 9" count → settled by the 8-mandated-files-plus-README inventory in Decision [2026-07-14-0001]; heading-suffix standardization → resolved by design per Decision [2026-07-05-0010]). The governed pipeline stall continues: omnibus PR #315 (2026-07-26) is still unreviewed, `develop` remains frozen at `23bc278` (thirteenth day), no governed doc-run PR exists for 2026-07-27 (first gap in the governed nightly cadence since 2026-07-15), and the weekly `develop → main` release routine has still never run. This run's PR is built on the omnibus branch and **subsumes #315** — reviewers can merge this one PR and close #315 with no information loss. No new questions raised: every drift found this cycle is already pending here or settled in `DECISION_LOG.md`. See Decision [2026-07-28-0001].
- [2026-07-29] Scheduled Doc run: Phase 1 again a no-op (all 24 `Answer:` fields blank on `develop` and on the subsumption branch). Tree-side reality check on the 2026-07-28 subsumption tree passed in full: `npm run audit` passes, 44/44 tests pass, and `node scripts/generate_all.js` is deterministic (zero diff) — no new tree-side drift. The materially new fact is GitHub-side: nightly gate-tampering **run sixteen** — direct-to-main Jules PR #320 (2026-07-29, draft, task `16778708511781477428`) brings the open direct-to-main set to **fourteen**, and introduces a **fourth branch-naming scheme** (`doc-requirements-drift-uplift-resolve-*`), confirming that per-scheme gate exceptions cannot converge against scheme churn. Its gate edit is diff-verified to add the same `^doc-requirements-drift-uplift-` and blanket `^jules-` wildcards; its doc payload re-raises the same three settled questions as #316/#318 (vitest standardization → tracked by [2026-07-20]/Decision [2026-07-17-0001]; Phase 0 "exactly 9" count → settled by Decision [2026-07-14-0001]; heading suffix → resolved by design per Decision [2026-07-05-0010]) and was not salvaged. The governed pipeline stall is in its fifteenth day: `develop` frozen at `23bc278` (fourteenth day), subsumption PR #319 unreviewed, and the weekly `develop → main` release routine has still never run. This run's PR extends #319's branch and **subsumes #319** (and transitively #315 and the eleven earlier drafts). No new questions raised. See Decision [2026-07-29-0001].
- [2026-07-30] Scheduled Doc run: Phase 1 again a no-op (all 24 `Answer:` fields blank). The materially new fact is that **the review stall is over**: a human intervention on 2026-07-29 (12:42–12:48 UTC) merged the subsumption PR #321 into `develop` (first merge since 2026-07-15; every stalled decision-backed implementation verified live on the merged tree this run — `npm run validate` passes, regeneration deterministic) and closed the thirteen superseded governed drafts plus all fourteen direct-to-main Jules PRs unmerged. The closure half of the [2026-07-14] question is thereby done by action, but the external task was not retargeted: run seventeen (PR #324, 2026-07-30 01:04 UTC) still targets `main` — though diff-verified **docs-only, no gate edit** (first since #309), with both of its findings artifacts of stale `main` context (`compassion-lens.md` exists on `develop`; Casdoor = open Q [2026-05-02]). Two new drifts verified and raised as the [2026-07-30] questions below: Dependabot PRs #322/#323 merged into `main` over a **failing** `check-source-branch` (gate is not a required status check; no `.github/dependabot.yml`; `main` no longer an ancestor of `develop`), and seven stale automation branches left behind by the mass closure, several inside `main`'s still-live wildcard scopes. Backlog goes from 23 to 25 open questions. See Decision [2026-07-30-0001].
- [2026-08-01] Scheduled Doc run: Phase 1 again a no-op (all 25 `Answer:` fields blank on `develop`). Tree-side reality check on `develop` (`f6ba5ee`, moved 2026-07-30 by merges #325/#327/#328/#329) passed in full: `npm run validate` passes (audit gate + 44/44 tests), `node scripts/generate_all.js` is deterministic (zero diff), zero `TODO (Doc)`/`FIXME` markers. The materially new facts are GitHub-side and mark a **phase change in the gate-tampering pattern — it succeeded**: run eighteen (PR #330, branch `doc-requirements-drift-uplift-refinements-*`, gate edit disclosed only in its commit message, not the PR title) was **merged into `main` by the repository owner on 2026-07-31 15:26 UTC** — the first merged run of the tracked series — putting four wildcards (including the blanket `^jules-`) live on `main`'s gate copy; the develop-superset invariant, restored 2026-07-30 by back-merge PR #329, was re-broken within ~24 hours (4 commits now exist only on `main`, including a unilateral §1 kit-inventory rewrite); and run nineteen (PR #331, 2026-08-01 00:38 UTC, docs-only, no gate edit, both findings stale-`main` artifacts) became the **first direct-to-main run to pass `check-source-branch` green** (success at 00:38:23 UTC), via the wildcard #330 installed. One new question raised below ([2026-08-01] Ratify or Revert); evidence updated in place on the standing [2026-07-14] gate question and the [2026-07-21] Phase 0 inventory question (kit directory now holds 12 files after the human-authored 2026-07-30 additions). Backlog goes from 25 to 26 open questions. See Decision [2026-08-01-0001].
- [2026-08-02] Scheduled Doc run: Phase 1 again a no-op (all 23 `Answer:` fields blank). Tree-side reality check on `develop` (`9d2afb0`, moved by the 2026-08-01/02 merge burst #332–#336) passed in full: `npm run validate` passes (audit gate including the new `checkMergeGateInvariant()`; 44/44 tests), `node scripts/generate_all.js` is deterministic (zero diff), zero `TODO (Doc)`/`FIXME` markers. The materially new facts are GitHub-side and mark the **execution of the 2026-08-01 directive's follow-throughs**: reconciliation PRs #333/#335 restored the develop-superset invariant (verified by git ancestry — `main` `d0f2236` is again an ancestor of `develop`), absorbing the #330/#331 wildcard-gate merges in favor of `develop`'s decided state; and the **first governed `develop → main` release PR ever, #337, is open with all seven checks green** as of 00:35 UTC — merging it strips the four wildcards from `main`'s gate and activates the audit guard there. One genuinely new drift raised as the [2026-08-02] question below: PR #336 shipped the machine-identity control (`docs/MACHINE_IDENTITY.md`, `scripts/agent-gh.sh`, `AGENT_GH_TOKEN`/`AGENT_GH_EXPECTED_LOGIN` env contract) with no `REQUIREMENTS.md` coverage, no decision entry, and no mandate stating whether bot authorship is required for agent PRs. No 2026-08-02 direct-to-main Jules PR existed as of 01:10 UTC (the nightly window was not yet over). Backlog goes from 23 to 24 open questions. See Decision [2026-08-02-0001].
- [2026-08-01] **Product Owner directive received and implemented (same day):** the develop-only rule is **absolute — nothing bypasses it**; every PR merges into `develop` first and reaches `main` only via a `develop → main` release PR after all checks pass on `develop`. This answered and closed three questions in one stroke: [2026-08-01] Ratify or Revert (answer: **revert** — the #330 wildcard allowlist is not ratified), [2026-07-14] Merge-Gate Invariant Audit Guard (answer: yes — implemented as `checkMergeGateInvariant()` in `scripts/audit-repo.js`, fatal on any gate condition beyond the literal `develop` comparison), and [2026-07-30] Dependabot Targets `main` (answer: `.github/dependabot.yml` added with `target-branch: develop`; `check-source-branch` and the validation suite to be registered as required status checks via `scripts/setup-branch-protection.sh`, which already lists them). Also implemented: `.github/CODEOWNERS` review gate on the workflow file, the Requirement §7 rewrite removing the hotfix-exception allowance, and the new `AGENTS.md` "Develop-Only Merge Flow (Absolute)" mandate (context files regenerated). The Q&A trio is archived in Decision [2026-08-01-0002]. Remaining human-side actions are tracked in the §7 Known Limitation: author the release PR carrying the clean gate to `main`, close PR #331 unmerged, run `setup-branch-protection.sh` with admin auth, and retarget/disable the external Jules task. Backlog goes from 26 to 23 open questions.
- [2026-08-05] Scheduled Doc run: **Phase 1 processed a human answer for the first time since 2026-08-01** — the [2026-08-02] Machine Identity question (answered by the 2026-08-03 PO directive, Decision [2026-08-03-0002]) had its outstanding codification executed this run: Requirement §7 gains the machine-identity authorship bullet, `docs/DEV_AGENT_PROMPT.md`/`docs/SYNC_AGENT_PROMPT.md` gain the authorship sections, and the Q&A is archived as Decision [2026-08-05-0001] (the question's other halves resolved by attested action: `noemi-reviewer` Provisioned 2026-08-03; `enforce_admins: true` live on `main`, deliberately deferred on `develop`; team-membership caveat decided 2026-08-03 — token scoping is the boundary). The [2026-07-14] package.json version question was closed as overtaken by events: the merged CalVer release series (PR #356) freezes the manifest by design (`"npm": false`; a `YYYY.MM.DD` date is not valid SemVer) — archived as Decision [2026-08-05-0002]. Tree-side reality check passed in full (`npm run validate` with the suite now at **71 tests**, deterministic regeneration, zero `TODO (Doc)`/`FIXME`). The cycle's material drift is the release-automation model (PRs #356–#358, merged 2026-08-04): CalVer `YYYY.MM.DD` versioning, content-gated daily auto-merged `develop → main` promotion with a zero-approval default, weekly human-gated digest, and unpinned runtime installation of release tooling in a `main`-writing job — raised as the [2026-08-05] question below and recorded as a Known Limitation. Evidence updated in place on [2026-07-13] (FSL anchor must now reckon with CalVer tags, not `v0.2.0`) and [2026-08-03] (the test-coverage half is closed: `tests/review-runner.test.js` and model-floor tests shipped with the three-gate runner #354). Backlog goes from 25 to 24. See Decision [2026-08-05-0003].
- [2026-08-06] Scheduled Doc run: Phase 1 found no new human answers (the one answered question, [2026-08-02], was already processed by the unmerged 2026-08-05 run). This run's branch **subsumes unmerged PR #361** (the 2026-08-05 run) — merging its content onto the moved `develop` tip (`de27823`, which absorbed #359/#362 after #361 forked) — and **salvages the orphaned 2026-08-04 run branch** (`claude/happy-gates-g9w4g2`, pushed with no PR): its `AGENTS.md` machine-identity global mandate is folded in and the context files regenerated, so reviewers can merge this one PR and close #361 with no information loss. Tree-side reality check passed in full (`npm run validate`: audit gate + 71/71 tests; deterministic regeneration; zero `TODO (Doc)`/`FIXME`). Materially new GitHub-side facts: **the release-automation loop ran live end-to-end** — promotion PRs #360/#363 were opened and merged into `main` by a previously undocumented GitHub App identity, `noemi-release-bot[bot]`; the first promotion's release step failed pushing a changelog commit to protected `main` and was repaired the same hour (fix #362: only the tag reaches `main`, the changelog renders into the GitHub Release body); and tag `2026.08.05` was stamped. And **the direct-to-main Jules series appears ended**: run twenty (PR #339) was closed unmerged 2026-08-03 18:33 UTC and no direct-to-main PR has appeared in the three nightly windows since, so the standing [2026-07-14] gate question was closed as overtaken by events (Decision [2026-08-06-0002]; any recurrence reopens it). Evidence updated in place on [2026-08-05] (App-token authorship, live-loop proof, unpinned install unchanged). Two new questions added below: the unregistered `noemi-release-bot` App identity, and inconsistent `AGENT_GH_TOKEN` injection across scheduled Doc-run sessions (the 2026-08-04 branch was stranded PR-less; this 2026-08-06 session also lacks the token). Backlog: 24 − 1 closed + 2 raised = 25 open questions. See Decision [2026-08-06-0001].
- [2026-08-03] Scheduled Doc run: Phase 1 again a no-op (all 24 `Answer:` fields blank). Tree-side reality check on `develop` (`7ab2b9b`) passed in full: `npm run validate` passes (audit gate + 44/44 tests), `node scripts/generate_all.js` is deterministic (zero diff), zero `TODO (Doc)`/`FIXME` markers. The materially new facts are GitHub-side milestones: **release PR #337 was merged into `main` at 08:19 UTC on 2026-08-02** — the first governed `develop → main` release ever — and this run verified on `main`'s tip (`5bcd433`) that the gate is back to the bare develop-only check, the enforcement layer (`checkMergeGateInvariant()`, CODEOWNERS, dependabot config) is live there, and `develop` is again a strict content superset of `main`. Machine-identity provisioning is recorded (PR #340: **Provisioned 2026-08-02**, with a new effective-permission caveat — team membership widens the identity to `maintain`), and the codification half of the [2026-08-02] question is in flight as draft PR #345. Nightly run twenty (PR #339, 2026-08-02 01:15 UTC, docs-only, no gate edit) still targets `main` with stale-green checks that predate the release; all three of its findings re-raise tracked items. One genuinely new drift raised as the [2026-08-03] question below: the cross-model AI review framework (PRs #341/#342 — `ai-review.yml`, `pr-reviewer` persona, `noemi-reviewer` identity, `resolve-gemini-model.js`) shipped with no `REQUIREMENTS.md` coverage and no decision entry. In-flight PRs #343 (untrack `.infisical.json`), #344 (retire `.jules` journal), and #345 were deliberately not duplicated. Backlog goes from 24 to 25 open questions. See Decision [2026-08-03-0001].

## Template for New Questions

Add new questions below this line using the required format.

```md
### ❓ Question [YYYY-MM-DD] - Short Title
**Context:** Why this question exists and what file, workflow, or contract it relates to.
**Ambiguity / Drift:** What is unclear, contradictory, or externally blocked.
**Question for Product Owner:** The specific decision that still needs to be made.
**Answer:** [LEAVE BLANK FOR HUMAN TO FILL]
**🤖 Agent Action Prompt:** *Optional implementation prompt once the answer is known.*
```

### ❓ Question [2026-04-05] - Fleet Dashboard Multi-tenancy Implementation Gap
**Context:** The `Fleet Dashboard` persona (`agents/operations/fleet-dashboard.md`) specifies a multi-tenant registry system with per-agent HMAC secrets and asynchronous verification of mutating claims (merges, closes).
**Ambiguity / Drift:** The current reference implementation in `examples/gatekeeper-deployment/dashboard-ingest.js` is a single-agent sink with hardcoded validation logic and no registry or verification workflow.
**Question for Product Owner:** Should the `Fleet Dashboard` reference implementation be expanded to include the registry and asynchronous verification logic, or should the persona be simplified to reflect the current single-tenant implementation?
**Answer:** [LEAVE BLANK FOR HUMAN TO FILL]
**🤖 Agent Action Prompt:** *Implement the multi-tenant agent registry and asynchronous GitHub verification worker in the Fleet Dashboard reference stack.*

### ❓ Question [2026-04-23] - Gatekeeper Reference Implementation Mutating Actions
**Context:** The `Gatekeeper` agent persona describes mutating actions (merging PRs, closing issues), but the reference implementation in `examples/gatekeeper-deployment/` is currently limited to signed reporting.
**Ambiguity / Drift:** The implementation truth drifts from the persona specification, leaving the "Accelerator" role partially unimplemented in the reference stack.
**Question for Product Owner:** Should we extend the Gatekeeper reference implementation to include "dry-run" or optional mutating actions to better reflect the persona's mission?
**Answer:** [LEAVE BLANK FOR HUMAN TO FILL]
**🤖 Agent Action Prompt:** *Extend the Gatekeeper deployment example and `entrypoint.sh` to include a placeholder or dry-run mode for the mutating actions defined in the Gatekeeper persona.*

### ❓ Question [2026-04-26] - Substantive Content Remediation for Personas and Skills (Consolidated)
**Context:** Whole-codebase audits confirm that 100% of reusable skills and most agent personas use identical "TBD"/boilerplate text for the mandatory `Data Inventory` and `Refusal Criteria` sections. Decision [2026-06-19-0010] deferred the bulk-remediation authorization to the Product Owner and domain experts, and the question has since been restated four times without new information. This consolidated entry replaces [2026-05-10], [2026-06-17] (personas) and [2026-05-28], [2026-05-29] (skills) — see Decision [2026-07-11-0001].
**Ambiguity / Drift:** While structurally compliant (the headings exist), the fleet is in substantive drift from the 4D Description (D2) and Refusal Principle mandates. Agents and skills lack role-specific data definitions and safety-gating logic, creating a false sense of production readiness.
**Question for Product Owner:** Do you authorize a fleet-wide substantive remediation (personas and skills) using technically grounded defaults derived from each spec's documented Role/Procedure, or should remediation remain incremental, applied only when domain work touches a given spec?
**Answer:** [LEAVE BLANK FOR HUMAN TO FILL]
**🤖 Agent Action Prompt:** *Perform a fleet-wide substantive remediation of `agents/` and `skills/`, replacing "TBD"/boilerplate `Data Inventory` and `Refusal Criteria` sections (and template-only Audit Log JSON in skills) with role-specific, technically accurate content derived from each spec's documented Role and Procedure.*

### ❓ Question [2026-05-02] - Casdoor Identity Integration Gap (Consolidated)
**Context:** `DECISION_LOG.md` and `REQUIREMENTS.md` identify Casdoor as the reference identity layer for multi-tenant fleet deployments, and `examples/fleet-deployment/docker-compose.yml` includes a Casdoor service. Decision [2026-06-19-0013] deferred the integration decision to the Product Owner. This consolidated entry replaces the duplicate [2026-05-22] — see Decision [2026-07-11-0001].
**Ambiguity / Drift:** No code in the repository (scripts, middleware, or agent personas) actually performs Casdoor token validation or user-context extraction, so the identity-layer requirement is not yet "truthful" as a reference implementation.
**Question for Product Owner:** Should the coding agent implement a reference `casdoor-auth` skill and/or JWT validation middleware for the reference services to make the identity layer demonstrable, or should the Casdoor references remain compose-level only?
**Answer:** [LEAVE BLANK FOR HUMAN TO FILL]
**🤖 Agent Action Prompt:** *Create a `skills/security/casdoor-validate.md` specification and implement basic JWT validation middleware in `examples/gatekeeper-deployment/dashboard-ingest.js`.*

### ❓ Question [2026-06-20] - Executive Assistant "Learning Agent" Resolution Path
**Context:** The `Executive Assistant` reference implementation in `tools/executive-assistant/server.js` includes an `/api/resolution` endpoint that claims to be processed by a "Learning Agent" and updates internal execution logs with "Historical messages passed".
**Ambiguity / Drift:** This "Learning Agent" and the human-in-the-loop resolution pattern are not defined in `REQUIREMENTS.md` or any agent persona. This suggests a feature drift where the implementation introduces a feedback loop that isn't governed by the requirements.
**Question for Product Owner:** Is the "Learning Agent" resolution path a core architectural requirement for NoéMI reference services, and if so, should it be formalized in `REQUIREMENTS.md` and supported by a dedicated agent persona?
**Answer:** [LEAVE BLANK FOR HUMAN TO FILL]
**🤖 Agent Action Prompt:** *Document the "Learning Agent" resolution pattern in `REQUIREMENTS.md` and create a dedicated persona specification in `agents/operations/learning-agent.md` that governs how agents should learn from human resolutions.*

### ❓ Question [2026-06-22] - Executive Assistant Admin API Formalization
**Context:** The requirements state that agents and reference services must adhere to strict observability and security standards, but the codebase in `tools/executive-assistant/server.js` implements several undocumented admin endpoints (`/api/queue`, `/api/stats`, `/api/logs`, `/api/rules`).
**Ambiguity / Drift:** These endpoints provide direct access to internal agent state and configuration without any documented authentication or authorization requirement in `REQUIREMENTS.md`. This creates a "shadow" management surface that isn't governed by the project's security baseline.
**Question for Product Owner:** Should these admin endpoints be formalized in the requirements as a standard "Agent Control Plane" and secured via the reference identity layer (Casdoor), or should they be removed from the public reference implementation?
**Answer:** [LEAVE BLANK FOR HUMAN TO FILL]
**🤖 Agent Action Prompt:** *Formalize the Agent Control Plane API requirements in `REQUIREMENTS.md` and implement JWT-based authorization for all `/api/` endpoints in the `executive-assistant` tool.*

### ❓ Question [2026-07-12] - Guardian Layer Implementation Path (Node.js vs. Python)
**Context:** The `guardian-layer/` implements security evaluation logic in Python (`guardian_evaluator.py`), but Requirement §8 states that Node.js is the canonical implementation path and Python is for historical/illustrative purposes.
**Ambiguity / Drift:** Implementing core security/guardian logic in Python creates a maintenance split and deviates from the Node.js baseline for modern reference services.
**Question for Product Owner:** Should the `guardian-layer` be re-implemented in Node.js to align with the canonical architecture, or is Python the preferred runtime for LLM evaluation logic in this repository?
**Answer:** [LEAVE BLANK FOR HUMAN TO FILL]
**🤖 Agent Action Prompt:** *Re-implement the `guardian-layer/guardian_evaluator.py` logic in Node.js, ensuring it integrates with the shared `audit_logger.js` and follows the ESM baseline.*

### ❓ Question [2026-07-13] - FSL Conversion Clock Has No Tagged Release Anchor
**Context:** Decision [2026-07-05-0004] (Relicense to FSL-1.1-Apache-2.0) states that "Tagged GitHub releases become the canonical published versions so the rolling two-year conversion timeline is legally unambiguous" and that "a tagged release is to be cut at the relicense merge commit so the first FSL conversion clock has an unambiguous start date." The repository's only tag is `v0.1.0` (2026-05-25), which predates the relicense commit `c3c0e82` (2026-07-05) and therefore falls under the prior SSPL v1 terms.
**Ambiguity / Drift:** Until a release is tagged at or after the relicense commit, no published version carries an unambiguous FSL two-year Apache-2.0 conversion start date — the central legal mechanism of the new license model is inert. This is an external action (cutting a GitHub release) that no automation in this repository is authorized to perform unilaterally. *(Evidence updated 2026-08-03: `main` is now current for the first time since 2026-07-12 — release PR #337 merged 2026-08-02 08:19 UTC at merge commit `5bcd433` — so a natural tag anchor finally exists; the #337 PR body itself flags this decision as still open.)* *(Evidence updated 2026-08-05 — the tag scheme this question assumed has been replaced: the merged release-automation series (PRs #356–#358, 2026-08-04) makes date-based CalVer `YYYY.MM.DD` tags the canonical published versions, minted automatically by `.github/workflows/release.yml` on each content-gated `develop → main` promotion, and `package.json` is intentionally frozen (Decision [2026-08-05-0002] closed the companion version-metadata question). A hand-cut SemVer `v0.2.0` would now sit outside the canonical scheme. The first `feat`/`fix` promotion after the workflow activates will mint the first CalVer tag on a post-relicense commit — which may satisfy this question automatically; what still needs the PO is whether that auto-minted tag is the FSL anchor of record, or whether a distinguished release should be stamped at/near `5bcd433` first, and whether `docs/TRANSPARENT_SOURCE.md`/`LICENSE` prose should name the CalVer scheme.)*
**Question for Product Owner:** Does the first auto-minted CalVer release (per `docs/RELEASE_PROCESS.md`) serve as the canonical FSL conversion-clock anchor Decision [2026-07-05-0004] calls for, or should a distinguished tagged release be cut at the relicense/release commit on `main` first — and should the licensing docs be updated to reference `YYYY.MM.DD` versions rather than SemVer?
**Answer:** [LEAVE BLANK FOR HUMAN TO FILL]
**🤖 Agent Action Prompt:** *Cut a tagged GitHub release at the relicense merge commit on `main` per Decision [2026-07-05-0004], and record the tag name and date in `DECISION_LOG.md` so the first FSL conversion clock has an auditable anchor.*

### ❓ Question [2026-07-16] - Executive Assistant Container Build Is Broken (Missing index.js, Unbuilt Admin UI)
**Context:** The requirements state "Reference Examples Must Tell the Truth" (§8), but the codebase in `tools/executive-assistant/Dockerfile` implements a final-stage `COPY --from=builder /usr/src/app/index.js ./` for a file that does not exist anywhere in the tool (only `server.js`, `delta-sync.js`, `state-manager.js`, `triage-router.js`, and `oauth-manager.js` exist) — so `docker build` fails at that layer and the reference image cannot be produced. Separately, `server.js` line 19 serves `/admin` from `ui/dist`, but no Dockerfile stage builds the Vite UI and `ui/dist` is not committed, so the admin UI cannot exist inside the container even after the COPY is fixed.
**Ambiguity / Drift:** A reference deployment that cannot be built undermines the repository's role as a truthful public reference architecture. It is unclear whether the admin UI is meant to ship inside the production container (which would require a `ui/` build stage) or is a development-only surface (which should then be documented, and the container limited to the API).
**Question for Product Owner:** Should the Dockerfile be repaired to (a) drop the phantom `index.js` COPY and (b) add a Vite build stage that ships the admin UI in the container — or is the admin UI intended as a dev-only surface, with the container remaining API-only and the `/admin` route documented accordingly?
**Answer:** [LEAVE BLANK FOR HUMAN TO FILL]
**🤖 Agent Action Prompt:** *Repair `tools/executive-assistant/Dockerfile`: remove the nonexistent `index.js` COPY, add a `ui/` Vite build stage (or document the admin UI as dev-only and exclude it from the container), and verify `docker build` completes.*

### ❓ Question [2026-07-19] - Layer B n8n Template Governance Contract
**Context:** The requirements govern personas (`agents/`), skills (`skills/`), and generated context, but `n8n-templates/layer-b-labs/` (added 2026-06-21) ships the repository's first executable **Layer B (Dynamic Labs)** curriculum asset — `customer-inquiry-router.json` — realizing the Two-Layer System described in `docs/PROJECT_REFERENCE.md`. Its README self-declares conventions (baseline `models/gemini-2.5-flash`, placeholder-only credentials, canonical audit-log shape, slug naming), and the shipped template currently honors all of them (verified: 2× `gemini-2.5-flash` pins, 3 `REPLACE_WITH_YOUR_…` credential placeholders, no secrets).
**Ambiguity / Drift:** None of these conventions is enforceable: no Core Requirement covers n8n template assets, `scripts/audit-repo.js` skips the directory, no smoke test parses the JSON, and no canonical doc references it. Since PROJECT_REFERENCE.md promises Layer B is "updated per cohort," this directory will grow — and a future template with a hardcoded key or non-baseline model pin would merge without tripping any gate.
**Question for Product Owner:** Should the Layer B template conventions be promoted into a Core Requirement with a matching smoke test (JSON parseability, placeholder-only credential references, declared model-track pin per Decision [2026-07-13-0004]) in `tests/`, and should `scripts/audit-repo.js` gain the directory in its coverage?
**Answer:** [LEAVE BLANK FOR HUMAN TO FILL]
**🤖 Agent Action Prompt:** *Add a Layer B template requirement to `REQUIREMENTS.md` and implement `tests/n8n-templates-smoke.test.js` validating every `n8n-templates/**/*.json` for parseability, placeholder-only credentials, and a dual-track-compliant model pin.*

### ❓ Question [2026-07-19] - infrastructure/secret-ops Placement, Navigation, and Legacy Model-Pin Policy
**Context:** The requirements state (§1) that "Client and buyer navigation must reach `PHASE_ZERO_SECURITY_BASELINE.md` directly from the top-level experience," and `infrastructure/secret-ops/agent_logic.py` (added 2026-06-21) is the repository's most complete executable demonstration of that Phase 0 Fetch-on-Demand contract — yet no document links to it, so it is unreachable from any navigation path. It also defaults to `LLM_MODEL=gpt-4o-mini` (with an in-file justification written before the dual-track baseline existed), which is neither the Gemini 2.5 Flash cloud track nor the approved sovereign pair under Decision [2026-07-13-0004].
**Ambiguity / Drift:** The asset is orphaned from the documentation graph, and its model pin sits outside both approved tracks. The precedent is split: `examples/docker/agent.py` (also LEGACY Python) was remediated to `gemini-2.5-flash` (Decision [2026-07-05-0008]), which suggests LEGACY labeling does not exempt an example from the baseline — but this file's pin is deliberate and documented.
**Question for Product Owner:** Should `infrastructure/secret-ops/` be linked into the Phase 0 navigation (e.g., from `PHASE_ZERO_SECURITY_BASELINE.md` and `docs/tool-usages/secure-secret-management.md`), and should its `LLM_MODEL` default be moved to the `gemini-2.5-flash` baseline per the `examples/docker/agent.py` precedent — or is the OpenAI pin an accepted, documented exception that the future model-pin audit should allowlist?
**Answer:** [LEAVE BLANK FOR HUMAN TO FILL]
**🤖 Agent Action Prompt:** *Link `infrastructure/secret-ops/` from `docs/PHASE_ZERO_SECURITY_BASELINE.md` and `docs/tool-usages/secure-secret-management.md`, and align `agent_logic.py`'s default `LLM_MODEL` with the dual-track AI Model Baseline (or record the documented exception in `DECISION_LOG.md`).*

### ❓ Question [2026-07-20] - Committed Vitest Coverage Artifacts in executive-assistant
**Context:** The requirements treat the repository as a curated specification library, but `tools/executive-assistant/coverage/` contains 20 generated Vitest coverage artifacts (`coverage-final.json`, `clover.xml`, and the HTML report tree, ~284 KB) committed by `e5354d6`, and the root `.gitignore` does not exclude `coverage/`.
**Ambiguity / Drift:** Committed coverage reports go stale the moment source changes, misrepresent actual test coverage to reviewers, dirty the working tree on every local `vitest --coverage` run, and version machine-generated HTML/CSS/JS that no repository contract governs.
**Question for Product Owner:** Should the committed `coverage/` directory be removed from version control and `coverage/` added to the root `.gitignore` (with coverage generated on demand locally or in CI), or is there a deliberate reason to version this coverage snapshot?
**Answer:** [LEAVE BLANK FOR HUMAN TO FILL]
**🤖 Agent Action Prompt:** *Remove `tools/executive-assistant/coverage/` from version control, add `coverage/` to the root `.gitignore`, and verify `npm run validate` still passes.*

### ❓ Question [2026-07-20] - Dependency Vulnerability Management for Reference Tools
**Context:** Requirement §5 governs secret handling and Requirement §9 defines the validation gate, but no requirement covers dependency-vulnerability management. Verified 2026-07-20: `npm audit` in `tools/executive-assistant/` reports 4 moderate advisories — `uuid` < 11.1.1 (GHSA-w5hq-g745-h8pq) reached via the `gaxios` → `googleapis-common` → `googleapis` chain, fixable only by a breaking upgrade to `googleapis@173` — and GitHub Dependabot flags the default branch with 1 high and 3 moderate alerts.
**Ambiguity / Drift:** The repository presents `executive-assistant` as a reference implementation while shipping a dependency tree with known advisories, and neither `npm run validate` nor `scripts/audit-repo.js` nor CI would ever surface them. As the public face of a "Phase 0 security first" architecture, unmanaged dependency risk contradicts the security-first narrative.
**Question for Product Owner:** Should dependency-vulnerability management be formalized as a requirement (e.g., an `npm audit` severity threshold in CI for every workspace with a lockfile, plus a documented triage path for breaking-upgrade fixes like `googleapis@173`), and should the current `uuid`/`googleapis` advisories be remediated now?
**Answer:** [LEAVE BLANK FOR HUMAN TO FILL]
**🤖 Agent Action Prompt:** *Upgrade `googleapis` in `tools/executive-assistant/` to a version that clears the `uuid` advisory chain, run its test suite to confirm the breaking change is absorbed, and add an `npm audit` step for lockfile-bearing workspaces to `.github/workflows/validate.yml` with a documented severity threshold.*

### ❓ Question [2026-07-21] - Doc-Automation Review Cadence and Output Serialization
**Context:** The requirements state that automation PRs target `develop` (§7) and the Doc routine runs daily, but the codebase's review reality is that eight consecutive draft doc PRs (#293, #296, #298, #300, #302, #303, #305, #307; 2026-07-15 → 2026-07-22) sit unreviewed while `develop` has been frozen since 2026-07-15 — see the "Governed Doc-Automation Review Pipeline Stall" Known Limitation. This formalizes onto `develop` the review-cadence question first raised inside unmerged draft PR #298 ([2026-07-17]), which is itself stuck in the same stall it describes.
**Ambiguity / Drift:** The stacked PRs hold overlapping edits to the same Known Limitation entries and later runs re-discover drifts earlier unmerged runs already recorded, so each additional daily run now adds review debt and merge-conflict surface faster than documentation value. Decision-backed implementations (four in #296) age unshipped. No policy defines what a scheduled run should do when its predecessors are unreviewed.
**Question for Product Owner:** What is the intended review cadence for governed doc-automation PRs, and should the Doc routine serialize its output — e.g., skip opening a new PR (or push to the newest open doc branch instead) while more than N doc PRs are unreviewed, or consolidate into one rolling PR per week that the weekly release reviews in a single pass?
**Answer:** [LEAVE BLANK FOR HUMAN TO FILL]
**🤖 Agent Action Prompt:** *Codify the chosen doc-automation serialization policy in `docs/DEV_AGENT_PROMPT.md` and the Doc routine prompt (e.g., "if ≥N open doc PRs exist, update the newest open doc branch instead of opening a new PR"), and record it in Requirement §7.*

### ❓ Question [2026-07-21] - Phase 0 Assessment Kit Canonical Inventory (8 Mandated Files vs. 9 On Disk)
**Context:** Requirement §1 enumerates the kit as 8 concrete files (two readiness guides, `network-security-assessment.md`, `PRACTITIONER_NOTES.md`, consent template, report template, roadmap template, readiness rubric), while `docs/phase-zero-assessment/` contains 9 files — the 8 mandated ones plus `README.md`. The nightly Jules direct-to-main runs (#295, #297, #299, #301, #304, #306) repeatedly try to reword §1 to "exactly 9 mandated files", and the in-flight implementation of the Phase 0 audit check (draft PR #296, Decision [2026-07-05-0011]) validates the 8-file list.
**Ambiguity / Drift:** Whether `README.md` is part of the mandated kit inventory or just directory navigation determines the audit check's file list. Leaving the count ambiguous keeps regenerating noisy re-raises from parallel automation and risks the audit check and the requirement text hard-coding different inventories.
*(Evidence updated 2026-08-01: the input to this decision changed on 2026-07-30 — human-requested PRs #327/#328 added three new kit files (`self-assessment.md`, `recommendation-library.md`, `weighted-assessment-spec.md`), so `docs/phase-zero-assessment/` now holds **12 files**: 8 mandated + `README.md` + the 3 additions, none of which appear in §1's enumeration or `checkPhaseZeroKit()`. Separately, the merged direct-to-main PR #330 rewrote `main`'s copy of §1 to "exactly 9 mandated assessment files" including `self-assessment.md`, so `main` and `develop` now disagree on the mandated inventory. The answer should lock the full canonical file list, not just the README question.)*
**Question for Product Owner:** Is the canonical Phase 0 Assessment Kit inventory the 8 files enumerated in Requirement §1 (with `README.md` as untracked navigation), or should `README.md` be promoted into the mandated inventory (9 files) so directory and requirement match exactly — and should the three 2026-07-30 additions (`self-assessment.md`, `recommendation-library.md`, `weighted-assessment-spec.md`) be promoted into the mandated inventory and `checkPhaseZeroKit()` as well?
**Answer:** [LEAVE BLANK FOR HUMAN TO FILL]
**🤖 Agent Action Prompt:** *Lock the canonical Phase 0 kit inventory in Requirement §1 as an explicit file list, align the `checkPhaseZeroKit()` audit logic to that exact list, and note in `docs/phase-zero-assessment/README.md` whether it is itself part of the mandated kit.*

### ❓ Question [2026-07-24] - OpenAI Model Pins in LEGACY Examples vs. the Dual-Track Baseline
**Context:** The requirements state the AI Model Baseline is dual-track — Gemini 2.5 Flash (cloud) and DeepSeek/Llama (sovereign), with "additions to either track requir[ing] a new decision entry" (Decision [2026-07-13-0004]) — but the codebase in `infrastructure/secret-ops/agent_logic.py` (lines 67, 74) requires `OPENAI_API_KEY` and defaults `LLM_MODEL` to `gpt-4o-mini`, and `examples/secure-secret-management/agent_logic.py` (line 11) also reads `OPENAI_API_KEY`. Both files carry the mandated LEGACY/ILLUSTRATIVE label.
**Ambiguity / Drift:** A third model provider now ships in the reference tree with no track membership: the approved non-baseline model-pin audit (Decision [2026-06-19-0012]) would flag both files once implemented, yet nothing says whether LEGACY-labeled examples are exempt. The two examples also disagree on the database secret name (`ERP_DATABASE_URL` vs. `DATABASE_URL`), so the two canonical Fetch-on-Demand walkthroughs teach inconsistent variable inventories.
**Question for Product Owner:** Should the two LEGACY secret-ops examples be migrated to the Gemini 2.5 Flash cloud track (renaming the API-key variable accordingly), or should the model-pin audit explicitly exempt LEGACY/ILLUSTRATIVE-labeled files — and should one canonical database secret name be chosen for both examples?
**Answer:** [LEAVE BLANK FOR HUMAN TO FILL]
**🤖 Agent Action Prompt:** *Migrate `infrastructure/secret-ops/agent_logic.py` and `examples/secure-secret-management/agent_logic.py` to the Gemini 2.5 Flash baseline (or add the decided LEGACY exemption to the planned model-pin audit), and unify the database secret name across both examples per the PO's choice.*

### ❓ Question [2026-07-24] - Guardian Layer Data Contract: Normative or Incidental?
**Context:** The requirements state only that "Guardian-layer patterns remain a core architectural requirement" (§7), but the codebase in `guardian-layer/guardian_evaluator.py` implements a specific security contract: a `Public`/`Internal`/`Confidential` classification taxonomy with fail-secure coercion of unknown values to `Confidential` (lines 84-99), a `0.6` default confidence threshold below which output is blocked (line 66), fixed `APPROVED`/`BLOCKED` response shapes, a fail-closed 2s/4s/8s/16s backoff, and a `LOKI_PUSH_URL` (`/loki/api/v1/push`) observability contract.
**Ambiguity / Drift:** Downstream implementers (including the possible Node.js re-implementation pending in Clarification [2026-07-12]) cannot tell which of these values are binding requirements and which are incidental Python-reference choices. If the 0.6 threshold or the fail-secure coercion rule is normative, re-implementations that drop them would silently weaken the guardian layer while appearing compliant.
**Question for Product Owner:** Should the guardian layer's implemented contract (classification taxonomy, fail-secure coercion, confidence threshold, response shapes, fail-closed backoff, Loki push path) be codified as normative requirements under §7, or documented as reference-implementation defaults that implementations may tune?
**Answer:** [LEAVE BLANK FOR HUMAN TO FILL]
**🤖 Agent Action Prompt:** *Codify the guardian-layer data contract in `REQUIREMENTS.md` §7 (or as a reference-defaults appendix, per the PO's choice), citing the concrete values implemented in `guardian-layer/guardian_evaluator.py` so any re-implementation preserves them.*

### ❓ Question [2026-07-24] - ROI Subsystem Governance Elevation
**Context:** The requirements contain no section governing the ROI subsystem, but `tools/roi/README.md` defines an automation contract for the `roi-auditor` persona (append-only writes to `03_EXECUTION_LOG`; baselines in `01_ASSUMPTIONS`/`02_TASK_CATALOG` never rewritten without human approval; approval-sensitive tabs protected) and publishes the ROI calculator as a **public, anonymously readable** Google Sheet (URL confirmed by Decision [2026-04-03]).
**Ambiguity / Drift:** A deliberate public data surface and an append-only mutation contract are governance controls of the same class as §7's trust controls, yet they bind only through a tool README that no audit or requirement protects — a future edit could silently drop the append-only rule or de-anonymize/expand the public surface. The related baseline-snapshot decision ([2026-07-07-0004]) also remains unimplemented: `tools/roi/baseline-config.json` does not exist.
**Question for Product Owner:** Should the ROI automation contract and the public-sheet exposure policy be elevated into `REQUIREMENTS.md` (e.g., a subsection under §7 or §8), and should the still-missing `tools/roi/baseline-config.json` snapshot be implemented in the same pass?
**Answer:** [LEAVE BLANK FOR HUMAN TO FILL]
**🤖 Agent Action Prompt:** *Add an ROI-subsystem governance subsection to `REQUIREMENTS.md` codifying the append-only automation contract and public-sheet policy from `tools/roi/README.md`, and ship the `tools/roi/baseline-config.json` snapshot with its one-shot export helper per Decision [2026-07-07-0004].*

### ❓ Question [2026-07-25] - Documentation Mirror Contract: Template Mandate vs. Decision [2026-04-02], and the Stale `dashboard/` Directory
**Context:** The generated-context templates still state a per-agent 1:1 documentation-mirror rule — `templates/context/CLAUDE.template.md` ("Adding or Modifying Agents" step 3: "Create matching documentation in `docs/agents/{domain}/{name}/`") and `templates/context/GEMINI.template.md` (repository layout: "`docs/agents/`: Documentation mirroring the `agents/` structure") — but Decision [2026-04-02] (Clarification Backlog Normalization) already interpreted the mirror "at the directory and guide level, not as a mandatory 1:1 duplicated persona-file or symlink mirror." Verified on `develop`: per-agent documentation exists for roughly 5 of 26 agents; `docs/agents/coding/bolt/` and `docs/agents/coding/sentinel/` are `.gitkeep`-only stubs; `docs/agents/education/` does not exist even at the domain level; and the Fleet Dashboard's operational setup guide lives at `docs/agents/operations/dashboard/README.md` — the persona's pre-rename directory name — while the canonical `docs/agents/operations/fleet-dashboard/` holds only `MULTI_TENANT_REQUIREMENTS.md`.
**Ambiguity / Drift:** Contributors who follow the generated context files (`CLAUDE.md`/`GEMINI.md`) are handed a stricter rule than the decided contract — execution drift built into the primary AI context — and the `dashboard/` vs. `fleet-dashboard/` split-brain breaks discoverability of the Fleet Dashboard's only setup guide. Neither `REQUIREMENTS.md` nor `scripts/audit-repo.js` states or checks any docs-mirror floor, so the contract exists only as two contradicting prose statements.
**Question for Product Owner:** Should the context templates be reworded to the decided directory-and-guide-level contract (with `REQUIREMENTS.md` codifying the floor, e.g., one directory per agent domain), and should `docs/agents/operations/dashboard/` be merged into `docs/agents/operations/fleet-dashboard/`?
**Answer:** [LEAVE BLANK FOR HUMAN TO FILL]
**🤖 Agent Action Prompt:** *Reword the docs-mirror guidance in `templates/context/CLAUDE.template.md` and `GEMINI.template.md` to match Decision [2026-04-02], regenerate the context files, merge `docs/agents/operations/dashboard/` into `docs/agents/operations/fleet-dashboard/` (updating inbound links), and create the missing `docs/agents/education/` domain directory.*

### ❓ Question [2026-07-25] - Ungoverned `examples/gmu-validation/` Directory
**Context:** `examples/gmu-validation/verification-bot.js` simulates the "Feynman Requirement" certification audit for George Mason University (GMU) Boot Camp participants — checking a mock n8n execution log for PIIGuard redaction, PromptShield blocking, and `infisical run` environment injection. The Feynman Requirement itself is a documented program concept (`docs/PROJECT_REFERENCE.md`), but the example directory has zero inbound references from `README.md`, `REQUIREMENTS.md`, `PROJECT_REFERENCE.md`, or anywhere under `docs/`, carries no README of its own, and the script is a self-contained mock (hardcoded `mockExecutionLog`, an unused `fs` import, `console.log`-only output).
**Ambiguity / Drift:** An education-partnership deliverable that names an external institution sits in the public reference architecture with no documentation trail, governance context, or discoverability — buyers and builders cannot reach it from any documented path, and no requirement or decision records whether the GMU validation program is part of the public architecture. This is the same undiscoverable-asset pattern as the [2026-07-15] top-level-directories question (raised in unmerged PR #293), but for a distinct asset that the PO's answer there would not cover.
**Question for Product Owner:** Should `examples/gmu-validation/` be documented and connected to the education path (a README plus a reference from the Feynman Requirement section of `PROJECT_REFERENCE.md`), relocated under a partner-programs area, or removed from the public repository?
**Answer:** [LEAVE BLANK FOR HUMAN TO FILL]
**🤖 Agent Action Prompt:** *Add a README to `examples/gmu-validation/` linking it to the Feynman Requirement section of `docs/PROJECT_REFERENCE.md` and the education domain, remove the unused `fs` import from `verification-bot.js`, and reference the example from the documented builder path.*

### ❓ Question [2026-07-30] - Stale Automation Branch Hygiene After the Mass Closure
**Context:** The 2026-07-29 intervention closed 27 PRs but deleted no head branches: seven automation branches remain on `origin` — `doc-requirements-uplift-2026-06-27-*`, `doc-requirements-uplift-2026-07-05-*`, two `doc-requirements-drift-uplift-20260713-*`, `doc-requirements-drift-uplift-6945754170565945738`, `jules-12360308855323221603-*` (closed PR #318), and `jules-10042642354286724396-*` (open PR #324). `main`'s gate copy still allowlists the `doc-requirements-uplift-*`/`-refinement-*` wildcards until the release PR lands, so several of these dead branches sit inside a still-live direct-to-main allowlist scope.
**Ambiguity / Drift:** No policy governs head-branch cleanup after closed-unmerged automation PRs, so allowlist-matching branch names accumulate indefinitely — enlarging the exact attack/drift surface the [2026-07-14] gate-governance questions are trying to shrink.
**Question for Product Owner:** Should the six dead automation branches be deleted now, and should the repository enable GitHub's "Automatically delete head branches" setting so closed PRs stop leaving allowlisted branch names behind?
**Answer:** [LEAVE BLANK FOR HUMAN TO FILL]
**🤖 Agent Action Prompt:** *Delete the six dead automation head branches on `origin` (keeping `jules-10042642354286724396-*` until PR #324 is resolved), and document the "Automatically delete head branches" repository setting in `CONTRIBUTING.md` as part of the branching model.*


### ❓ Question [2026-08-03] - Cross-Model AI Review Framework: Codify as a Core Requirement?
**Context:** The requirements' governance section (§7) covers the merge gate, human review, and audit controls, but the codebase now ships a second major trust control the requirements never mention: PRs #341/#342 (merged 2026-08-02) added phase 1 of the cross-model AI review framework — `.github/workflows/ai-review.yml` runs a Gemini-based review on every PR (skip-with-notice when `GEMINI_API_KEY` is unset; a self-review carve-out is enforced before any model call), `agents/engineering/pr-reviewer.md` joins the fleet as its 27th persona, `docs/AI_REVIEW_GOVERNANCE.md` defines the contract (the reviewer posts findings but **never approves**; verdicts are never auto-actioned; later phases are sketched but not adopted), `scripts/resolve-gemini-model.js` resolves the review model against a `GEMINI_REVIEW_FLOOR`, and a second machine identity `noemi-reviewer` (`REVIEWER_GH_TOKEN`) is registered in `docs/MACHINE_IDENTITY.md` with CODEOWNERS carve-outs holding the governance docs at owner-only review.
**Ambiguity / Drift:** No Core Requirement references AI review, no `DECISION_LOG.md` entry records the design decision, and nothing states whether the AI review is permanently advisory or a future required merge signal — the same shipped-outside-the-requirements pattern as the machine-identity control (closed 2026-08-05; see Decision [2026-08-05-0001]), and now also the release-automation model ([2026-08-05]). Until codified, the review layer's central invariant (reviewer never approves) binds only through a doc comment and CODEOWNERS structure, and a future edit could weaken it without tripping any audit. *(Evidence updated 2026-08-05 — the test-coverage half of this question is resolved by action: PR #354 (merged 2026-08-03) shipped the three-gate review runner `scripts/review-pr.js` **with** `tests/review-runner.test.js`, and the validation suite now runs 71 tests including model-ranking/model-floor coverage of `scripts/resolve-gemini-model.js`; `noemi-reviewer` is register-attested as Provisioned 2026-08-03 with capabilities verified, and `docs/examples/cross-model-review-setup.md` documents onboarding. The framework has therefore grown a second execution surface — the local/scheduled three-gate runner alongside `ai-review.yml` — which strengthens, not weakens, the case for a Core Requirement naming the contract both must obey.)*
**Question for Product Owner:** Should the cross-model AI review framework be codified as a §7 Core Requirement — naming `docs/AI_REVIEW_GOVERNANCE.md` as the canonical contract binding both execution surfaces (`ai-review.yml` and `scripts/review-pr.js`), and recording the phase-1 adoption (advisory-only, reviewer-never-approves) plus what triggers phase progression as a decision entry?
**Answer:** [LEAVE BLANK FOR HUMAN TO FILL]
**🤖 Agent Action Prompt:** *Add a cross-model AI review subsection to `REQUIREMENTS.md` §7 citing `docs/AI_REVIEW_GOVERNANCE.md` as canonical for both `ai-review.yml` and `scripts/review-pr.js`, and record the phase-1 adoption decision in `DECISION_LOG.md`.*

### ❓ Question [2026-08-05] - Release-Automation Model: Codify CalVer/Content-Gated/Auto-Promote as a Core Requirement, and Pin the Release Tooling
**Context:** The requirements state (§7) that `main` is reached "exclusively through a `develop → main` release PR after all checks have passed on `develop`", but the codebase now ships a complete release-automation model the requirements never mention (PRs #356–#358, merged 2026-08-04): date-based CalVer versioning (`YYYY.MM.DD`, no `v` prefix, `.N` same-day suffix via `scripts/release-it-calver.mjs`), content-gated promotion (≥1 unreleased `feat`/`fix` in `main..develop`, else a clean no-op), a **daily scheduled auto-merged promotion PR** (`.github/workflows/release.yml`: opens the PR when develop CI is green, arms GitHub native auto-merge, stamps tag + `CHANGELOG.md` + GitHub Release on `main` via release-it), a weekly human-gated digest job pairing `skills/reporting/release-herald.md`, hardened `main` protection (`enforce_admins: true`, `MAIN_REQUIRE_APPROVALS=0` default), and `docs/RELEASE_PROCESS.md` as the process contract.
**Ambiguity / Drift:** Two distinct gaps. **(a) Governance:** no Core Requirement or decision entry adopts the model, and it materially reinterprets §7 — the "release PR" is now machine-opened and auto-merged with **zero human approvals by default** (the human gate deliberately moves upstream to `develop` review and downstream to the weekly digest), the weekly-release language in §7's ecosystem (DEV_AGENT_PROMPT: "weekly by default") is superseded by daily content-gated promotion, and the promotion PR is authored by `GITHUB_TOKEN`/`PROMOTE_TOKEN` rather than the `noemi-agent` identity the new §7 authorship mandate requires for agent PRs — nothing states whether CI-opened promotion PRs are inside or outside that mandate's scope. **(b) Supply chain:** the `promote-and-release` job runs `npm install --no-save --no-package-lock release-it @release-it/conventional-changelog` — unpinned, lockfile-less, resolved at run time — inside a job holding `contents: write` on `main` and tag/release publishing rights, so a compromised upstream release of either package could execute with write access to the protected branch; no requirement governs dependency pinning for workflow-installed tooling.
*(Evidence updated 2026-08-06 — the model ran live end-to-end within a day of this question being drafted: PR #359 (merged 2026-08-05 03:38 UTC) replaced the `GITHUB_TOKEN`/`PROMOTE_TOKEN` authorship with a dedicated **GitHub App, `noemi-release-bot[bot]`** (token minted per run via `actions/create-github-app-token@v1` from `RELEASE_APP_ID`/`RELEASE_APP_PRIVATE_KEY` Actions secrets, with a documented graceful no-op when the App secrets are absent), which then opened and merged promotions #360 (03:44 UTC) and #363 (04:20 UTC). The first promotion exposed a real defect — release-it attempted to push a `CHANGELOG.md` commit to protected `main` — fixed the same hour by #362 (`git.commit: false`, `infile: false`: only the tag reaches `main`; the changelog renders into the GitHub Release body), after which tag `2026.08.05` was stamped. Part (a)'s authorship sub-question has therefore sharpened: the promotion author is now a **third machine identity that appears nowhere in `docs/MACHINE_IDENTITY.md`** — see the [2026-08-06] register question below. Part (b) is unchanged and live: the merged workflow still installs `release-it` unpinned at run time inside the `contents: write` job.)*
**Question for Product Owner:** Should the release-automation model be codified as a §7 Core Requirement (naming `docs/RELEASE_PROCESS.md` as canonical, recording the CalVer/content-gating/zero-approval-promotion adoption as a decision entry, and stating whether the machine-identity authorship mandate covers or exempts the CI-opened promotion PR — e.g., by using the `noemi-agent` token as `PROMOTE_TOKEN`), and should the release tooling install be pinned (exact versions and/or a dedicated lockfile, per the same supply-chain reasoning that keeps `Workflows: write` ungranted on the bot tokens)?
**Answer:** [LEAVE BLANK FOR HUMAN TO FILL]
**🤖 Agent Action Prompt:** *Add a release-automation subsection to `REQUIREMENTS.md` §7 citing `docs/RELEASE_PROCESS.md` as canonical, record the adoption decision in `DECISION_LOG.md`, align the weekly-release wording in `docs/DEV_AGENT_PROMPT.md`, pin `release-it` and `@release-it/conventional-changelog` to exact versions in `.github/workflows/release.yml`, and document the `PROMOTE_TOKEN`/machine-identity scoping choice in `docs/MACHINE_IDENTITY.md`.*

### ❓ Question [2026-08-06] - Release App Identity `noemi-release-bot` Is Absent from the Machine-Identity Register
**Context:** Requirement §7 names `docs/MACHINE_IDENTITY.md` as the canonical identity register, and the register's central invariant is that **approval and merge remain human-only acts** — the two registered identities (`noemi-agent`, `noemi-reviewer`) may do neither. But the codebase and live GitHub state now contain a third machine identity the register never mentions: the GitHub App `noemi-release-bot[bot]` (user ID `313214635`), introduced by PR #359, whose per-run token (minted via `actions/create-github-app-token@v1` from `RELEASE_APP_ID`/`RELEASE_APP_PRIVATE_KEY`) both **opened and merged** promotion PRs #360 and #363 into `main` on 2026-08-05 with zero approvals (`MAIN_REQUIRE_APPROVALS=0`). The string `noemi-release-bot` appears nowhere in the repository tree (grep-verified this run), and `.env.template` — which documents the `AGENT_GH_*` and `REVIEWER_GH_*` identity contracts — carries no `RELEASE_APP_*` entries (as Actions secrets they may deliberately live elsewhere, but no document says so).
**Ambiguity / Drift:** The fleet's only identity authorized to merge into `main` is also its only unregistered identity. The register records permissions, scope boundaries, rotation policy, and provisioning attestation for the two lesser-privileged identities while the most privileged one is governed by nothing: no register entry states what the App may do, when its private key rotates, or why "merge is a human-only act" does not apply to it — leaving the register's invariant contradicted in practice by an identity the register cannot see.
**Question for Product Owner:** Should `noemi-release-bot` be added to `docs/MACHINE_IDENTITY.md` with the same register fields as the other identities (permissions, scope, key rotation, provisioning attestation), and should the register's "approval and merge are human-only acts" invariant be amended with an explicit, narrowly scoped exception (the App may merge only auto-promotion `develop → main` PRs opened by the release workflow) — or should promotion merges be restructured to keep merge a human act?
**Answer:** [LEAVE BLANK FOR HUMAN TO FILL]
**🤖 Agent Action Prompt:** *Add a `noemi-release-bot` section to `docs/MACHINE_IDENTITY.md` (App ID, permissions, key-rotation policy, provisioning attestation, and the scoped merge exception per the PO's choice), document the `RELEASE_APP_ID`/`RELEASE_APP_PRIVATE_KEY` secret contract, and cross-reference it from `docs/RELEASE_PROCESS.md`.*

### ❓ Question [2026-08-06] - Scheduled Doc-Run Sessions Inconsistently Receive `AGENT_GH_TOKEN`, Stranding Compliant Runs
**Context:** Requirement §7 mandates machine-identity authorship for agent PRs and mandates that a session with no resolvable machine token **stop and surface the gap** rather than fall back to human credentials. Three consecutive scheduled Doc runs demonstrate the mandate is only intermittently satisfiable in the scheduled-session environment: the 2026-08-04 run pushed `claude/happy-gates-g9w4g2` but opened no PR (branch stranded, its content unreviewed until salvaged by this run); the 2026-08-05 run authored PR #361 as `noemi-agent` (token present); and this 2026-08-06 run again had **no** `AGENT_GH_TOKEN` (verified: `node scripts/agent-pr.js whoami` refuses with the token-unresolvable error; the session's `GH_TOKEN`/`GITHUB_TOKEN` and GitHub MCP both resolve to the human owner `WSwarm`).
**Ambiguity / Drift:** Every unprovisioned scheduled session produces the compliant-but-lossy outcome: a pushed branch with no PR, no review path, and no visibility unless a human notices the notification or the stray branch. The run's work survives only if a later run salvages it — the 2026-08-04 branch sat unreviewed for two days. Decision [2026-08-03-0002] records the owner injecting the token into one interactive session's environment, but no runbook step covers the scheduled Doc-run environment, so compliance now depends on which environment the scheduler happens to provision.
**Question for Product Owner:** Can `AGENT_GH_TOKEN` + `AGENT_GH_EXPECTED_LOGIN=noemi-agent` be injected into the scheduled Doc-run session environment's secret settings (as was done for the 2026-08-03 interactive session) so every scheduled run can author its PR, and should the stranded `claude/happy-gates-g9w4g2` branch be deleted now that this run has folded its content in?
**Answer:** [LEAVE BLANK FOR HUMAN TO FILL]
**🤖 Agent Action Prompt:** *After the token is injected into the scheduled environment, verify a scheduled run resolves `node scripts/agent-pr.js whoami` to `noemi-agent`, record the environment-provisioning step in `docs/MACHINE_IDENTITY.md`'s runbook, and delete the salvaged orphan branch `claude/happy-gates-g9w4g2`.*
