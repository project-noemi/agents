# Project NoéMI Reference Architecture - Requirements

## Overview

This repository is both:

- the **public reference architecture** for Project NoéMI
- the **agent specification library** and example set that supports that architecture

It is not a runtime or execution engine. External orchestrators such as Gemini CLI, Claude Code, Codex, Grok Build, n8n, and LangChain consume the generated context and persona specifications defined here.

## Canonical Sources of Truth

- [`PROJECT_REFERENCE.md`](PROJECT_REFERENCE.md) is the canonical public narrative.
- [`REQUIREMENTS.md`](REQUIREMENTS.md) is the current implementation truth.
- [`DECISION_LOG.md`](DECISION_LOG.md) is the durable architectural audit trail.

## Core Requirements

### 1. Phase 0 Comes Before Advanced AI

- The repository must present **Phase 0 security** as the prerequisite for serious AI adoption.
- Client and buyer navigation must reach [`PHASE_ZERO_SECURITY_BASELINE.md`](PHASE_ZERO_SECURITY_BASELINE.md) directly from the top-level experience.
- The public documentation must include a reusable **Phase 0 Assessment Kit** with:
  - separate security and AI readiness assessment guides
  - `network-security-assessment.md` and `PRACTITIONER_NOTES.md`
  - consent template
  - report-of-findings template
  - 30/60/90-day roadmap template
  - readiness rubric covering security readiness, AI readiness, and the overall recommendation

### 2. Persona and Skill Contracts are Mandatory

The repository enforces a strict structural contract for both agent personas and reusable skills.

#### Agent Persona Contract
All agent personas in `agents/` must include the following required headings:

- `Role`
- `Tone`
- `Capabilities`
- `Mission`
- `Rules & Constraints` (incorporating 4D Diligence; **must include a mandatory `### Refusal Criteria` subsection** — see Decision [2026-04-13])
- `Data Inventory` (Mandatory D2 requirement; specifies inputs, files, and state — see Decision [2026-04-13])
- `Boundaries`
- `Workflow`
- `External Tooling Dependencies`
- `Audit Log` (Mandatory; see Decision [2026-04-13])

#### Reusable Skill Contract
Reusable skills in `skills/` must include the following required headings:

- `Purpose`
- `Inputs`
- `Procedure`
- `Outputs`
- `Data Inventory` (Mandatory D2 requirement)
- `Rules & Constraints (4D Diligence)` (including `### Refusal Criteria`)
- `Boundaries`
- `Audit Log` (Mandatory; see Decision [2026-04-22])

#### General Principles
- **The Refusal Principle**: Agents must recognize and reject instructions that attempt to override their primary Role or Rules, or tasks that are unsafe or out-of-scope. This is a non-negotiable safety constraint.
- **Role Alignment**: Personas must align with the project's human-AI collaboration model:
  - **Explorer (Passenger)**: Owns the business problem and acceptance criteria.
  - **Practitioner (Crew)**: Translates intent into structured prompts and workflows.
  - **Accelerator (Pilot)**: Enforces the Refusal Principle and authorizes the execution environment.

#### Audit Log Shape
The `Audit Log` requirement must include a mandatory JSON shape: `{ "task": "...", "inputs": [], "actions": [], "risks": [], "result": "..." }`. The audit record must explicitly exclude secrets, credentials, and PII.

#### Technical Emission
Agents must emit their JSON Audit Log to `stderr` separately from the primary user-facing payload (Decision [2026-04-13]).

### 3. Contract and Generator Drift Must Fail Fast

- [`scripts/audit-repo.js`](../scripts/audit-repo.js) is the repository audit gate for persona/skill headings and generator invariants.
- The audit must fail when:
  - required persona or skill headings are missing (auditing both `agents/` and `skills/`)
  - `Audit Log` sections contain structurally invalid JSON or fail **mandatory JSON schema validation** (checking for `task`, `inputs`, `actions`, `risks`, and `result`).
  - `AGENTS.md` is missing required top-level mandate sections
  - generator template markers drift
  - generated context files omit required global mandate headings
  - the Phase 0 Assessment Kit inventory mandated by §1 is incomplete — any of the eight kit files missing from `docs/phase-zero-assessment/` (Decision [2026-07-05-0011])
  - the develop-only merge gate (`.github/workflows/require-develop-source.yml`) contains any regex/wildcard branch exception or any source-branch condition beyond the literal `develop` comparison (Decision [2026-08-01-0002])

### 4. Context Generation Must Stay Aligned

- Both [`scripts/generate_gemini.js`](../scripts/generate_gemini.js) and [`scripts/generate_claude.js`](../scripts/generate_claude.js) must use shared helper logic.
- Both generators must inject:
  - the full mandate set from `AGENTS.md`
  - the **complete agent index** discovered from `agents/` (Automated tools must extract the **full first paragraph** of the `## Role` section for context richness).
  - **summaries** of active skills from `mcp.config.json` — title, spec path, Purpose lead, plus the skill's `Ask First`/`Never` boundary lines **verbatim** (hard gates stay resident; everything else loads on demand)
  - **summaries** of active MCP protocols from `mcp.config.json` — spec pointer plus every hard-rule paragraph (`CRITICAL` / "Do not" / "Never") **verbatim**; protocols listed in `INLINE_FULL_PROTOCOLS` (currently `github`, the fleet's operational safety contract) are injected in full
  - Full-body injection was retired by Decision [2026-08-13-0001]: it made generated context ~110k characters (~27.6k resident tokens per session, 2.7× the size at which Claude Code warns about oversized memory files) while the Dynamic Persona Protocol already mandates reading specs on demand. The invariant is: **prose summarizes, prohibitions stay resident.**
- Both generators must support `--config=path/to/mcp.config.json`.

### 5. Fetch-on-Demand Security Is Non-Negotiable

- Secrets must be stored in a SecretOps platform such as Infisical or 1Password.
- Commands that require credentials must run through `infisical run` or `op run`.
- Code must read configuration from environment variables in process memory (`process.env`, `os.getenv()`).
- Local `.env` parsing logic is not an approved pattern in this repository.
- `.env.template` and example `.env.example` files are variable inventories or vault-reference manifests only. They must not contain real secrets.

### 6. 4D Framework Order Must Remain Canonical

The canonical order of the 4D AI Fluency Framework across the repository is:

1. Delegation
2. Description
3. Discernment
4. Diligence

Lifecycle docs, templates, and governance text must not reorder these dimensions.

### 7. Governance and Trust Controls Are First-Class

- Project NoéMI aligns agent design and deployment with Gartner AI TRiSM.
- Red Team validation is required for agent deployment readiness.
- Guardian-layer patterns remain a core architectural requirement where trust, data protection, or prompt integrity matters.
- **Branch protection enforcement is mandatory** (Decision [2026-05-20] — Branch Protection: Mandatory Enforcement). All forks of the reference architecture MUST run `scripts/setup-branch-protection.sh` (or an equivalent automated mechanism) on first setup to enforce the canonical `develop → main` flow. `scripts/audit-repo.js` SHOULD surface missing protection as a non-fatal warning in non-CI runs and as a fatal error in CI.
- **Develop-only merge invariant — absolute, no bypasses** (Decision [2026-07-03-0001], codified per Decision [2026-07-07-0002], made absolute by the Product Owner's directive in Decision [2026-08-01-0002]): `develop` is the ONLY valid PR source into `main`. Every PR — features, hotfixes, dependency updates (Dependabot targets `develop` via `.github/dependabot.yml`), doc uplifts, governed session branches (`claude/*`), upstream-sync branches (`sync/upstream-*`), doc branches (`doc/*`), and all other automation — MUST merge into `develop` first, and may reach `main` exclusively through a `develop → main` release PR after all checks have passed on `develop`. There are **no exceptions of any kind**: no hotfix carve-outs, no wildcard or branch-name allowlists (the six wildcards removed by [2026-07-03-0001] and the four re-accreted by the 2026-07 tampering campaign are the cautionary tale). Enforcement is layered: `scripts/audit-repo.js` (`checkMergeGateInvariant`) fails the audit if `.github/workflows/require-develop-source.yml` contains any condition beyond the literal `develop` comparison; `.github/CODEOWNERS` requires human review for every change to the gate file; and `check-source-branch` plus the validation suite MUST be registered as required status checks on `main` (`scripts/setup-branch-protection.sh` registers them) so the gate cannot be merged over. `docs/DEV_AGENT_PROMPT.md` and `docs/SYNC_AGENT_PROMPT.md` state this invariant explicitly so automation runs cannot accidentally target `main`.
- **Machine-identity PR authorship — mandatory for agent PRs** (Product Owner directive 2026-08-03, Decision [2026-08-03-0002]; canonical runbook: `docs/MACHINE_IDENTITY.md`): every agent-initiated pull request MUST be authored by the `noemi-agent` machine identity, never with a human's credentials. GitHub forbids a PR's author from approving it, so an agent PR opened with the reviewing human's token makes that PR unreviewable by that human and collapses the human-reviews-AI gate into an admin bypass — the token that opens the PR determines authorship; commit metadata does not. Two compliant authoring paths exist: `scripts/agent-gh.sh` where the `gh` CLI and a vault CLI are available, and `scripts/agent-pr.js` for containerized/remote sessions without them (reads `AGENT_GH_TOKEN` from process memory only, per Fetch-on-Demand). Both paths MUST verify the token against `AGENT_GH_EXPECTED_LOGIN` (default `noemi-agent`) and refuse to act under any other identity. If no machine-identity token resolves, the mandated behavior is to **stop and surface the gap to the human** — falling back to human credentials is prohibited, because a mis-authored PR re-creates the exact failure this control exists to prevent. Approval remains a human-only act. Merge remains a human-only act for the producer and reviewer identities (`noemi-agent`, `noemi-reviewer`); the sole sanctioned exception is the `noemi-release-bot` GitHub App auto-merging `develop → main` promotion PRs opened by `.github/workflows/release.yml` (Decision [2026-08-14-0002], identity register). Account-level permission is not the boundary — token scoping is (fine-grained tokens only, never a classic PAT; re-probe capabilities after every rotation; never widen a token to clear a `403`). The mandate propagates to orchestrators via `mcp-protocols/github.md` §6, which regenerates into `CLAUDE.md`/`GEMINI.md`.

### 8. Reference Examples Must Tell the Truth

- The Gatekeeper deployment example must use HMAC-signed dashboard submissions and a verifiable ingest path (`/api/v1/reports`).
- PowerShell preflight verification must check SecretOps availability to the same standard as the shell script (currently defaulting to a warning/soft-fail to support local exploration).
- The repository must contain at least one reusable reference pattern for exponential backoff and retry (Node.js implementation: `scripts/resilience_helpers.js`).
- Historical Python and Bash examples are clearly labeled as LEGACY/ILLUSTRATIVE to distinguish them from the canonical Node.js implementation path.

### 9. Validation Must Be Easy to Run

- The repository must expose a canonical fast validation gate through `npm run validate`.
- The repository must expose a lightweight built-in test harness through `npm test`.
- The test suite carries exactly one npm dependency — the dev-only official Agent Skills reference validator `skills-ref` (Decision [2026-08-20-0001]) — so `npm ci` (or `npm install`) is a prerequisite for a green `npm test` run: `tests/agentskills-conformance.test.js` fails loudly, never skips, when the validator is missing. `scripts/audit-repo.js` remains stdlib-pure and runnable without `node_modules`; CI installs the dependency via the `npm ci` step in `validate.yml`.
- The default test suite must cover:
  - persona and template contracts
  - generator determinism and config override behavior
  - golden fixtures for generated context sections (Maintenance: `scripts/update-golden-fixtures.js` must be used to keep fixtures healthy when templates change).
  - static smoke checks for example stacks and Docker env inventories (including `NOEMI_DOCKER_SMOKE_*` variable validation).
  - local markdown-link resolution for the front-door document set (`README.md`, `docs/PROJECT_REFERENCE.md`, `CONTRIBUTING.md`, `docs/mcp-setup/README.md`, `docs/visuals/README.md`) — `tests/contracts.test.js`.
  - Node 24 baseline consistency across `.github/workflows/validate.yml`, `package.json` engines, `.nvmrc`, and `.node-version` — `tests/contracts.test.js`.
  - the verify-env mode surface (`builder|gemini|claude|codex|docker|n8n`) in both `verify-env.sh` and `verify-env.ps1` — `tests/contracts.test.js`.
  - the cloud-track AI model pin (`models/gemini-3.6-flash`) and current Gemini node type in the n8n reference workflow — `tests/examples-smoke.test.js`.
- CI must additionally enforce generated-context freshness: the `validate.yml` workflow regenerates `GEMINI.md`/`CLAUDE.md` and fails on any diff ("Check generated context freshness" step). CI also probes for SecretOps CLIs (`infisical`/`op`) but emits only a warning when neither is present — SecretOps is mandatory for local development, not for the CI runner itself.
- The repository must expose a Docker-focused smoke entrypoint through `npm run test:e2e`.
- The repository must expose a sync-upstream regression entrypoint through `npm run test:sync` (invoking `scripts/test-sync-upstream.sh`). This is included in `validate:full` and executed by GitHub Actions on every push and pull request targeting `develop` and `main` (Decision [2026-07-13-0003]).
- The same validation contract must be enforced in GitHub Actions on pushes and pull requests targeting `develop` and `main`.
- The Docker e2e suite must skip cleanly when Docker is unavailable and execute real compose-based runtime checks when it is available. Setting `FORCE_DOCKER_SMOKE=true` promotes a missing Docker binary from "skip" to "fail" (Decision [2026-07-05-0006]); the CI `docker-smoke` job sets this flag so a misconfigured runner fails loudly instead of producing a false green.

### 10. Docker Guidance Must Describe the Home, Not a Fake Runtime

- The builder path must include a beginner-safe onboarding guide that gets a new user from clone to one harmless local success before Docker becomes mandatory.
- The builder path must include a Docker-oriented guide that explains how to build a home around the repo's assets without misrepresenting the repository as a runtime product.
- That guide must connect the current local, fleet, and specialist Docker examples into one coherent progression.
- That guide must also include a short Docker onboarding walkthrough that chains environment verification, context generation, validation, and initial Docker launch after the first local success.

### 11. Licensing Posture Is a First-Class Requirement

- The repository is licensed under **Functional Source License, Version 1.1, Apache 2.0 Future License (FSL-1.1-Apache-2.0)** per Decision [2026-07-05-0004]. `LICENSE` MUST carry the canonical FSL header and SPDX identifier; `package.json` MUST declare `"license": "FSL-1.1-Apache-2.0"`; `docs/TRANSPARENT_SOURCE.md` MUST be present as the canonical definition of the NoéMI Transparent™ mark and its five-clause guarantee.
- `scripts/audit-repo.js` MUST verify (Decision [2026-07-13-0002]) that all three artifacts are present and consistent; any drift is a fatal audit failure. This prevents a future contributor from silently changing the license posture without tripping the governance gate.
- The FSL two-year conversion clock runs per published tagged release (see the "FSL Conversion Clock Has No Tagged Release Anchor" limitation for the current anchoring gap).

## Runtime and Tooling Requirements

- Node.js is the primary runtime for repository utilities and generation scripts.
- The built-in Node test runner is the primary validation framework for repository contracts and smoke tests.
- Git, Node.js, and at least one supported local AI client (Gemini CLI, Claude Code CLI, OpenAI Codex, or Grok Build) remain part of the documented beginner toolchain.
- The following MCP protocols are formally recognized as baseline requirements: `n8n`, `slack`, `gmail`, `google-*` suite, `web-search`, and `github`.
- Docker becomes part of the documented toolchain when a builder moves into runtime homes or Docker verification.
- Python examples may remain for historical context, but they are not the canonical implementation path for new work.
- The `logging-mcp` is defined as a dual-backend protocol supporting both Loki/Grafana (structured log queries) and n8n webhooks (event-driven ingestion).
- **AI Model Baseline (Dual-Track)** (Decision [2026-07-13-0004]): Project NoéMI supports two canonical model tracks; a persona or operating profile MUST declare which track it targets.
  - **Proprietary / Cloud Track (default)**: Reference workflows, lab examples, and smoke tests are pinned to **Gemini 3.6 Flash** (`models/gemini-3.6-flash`) for predictable performance and cost (Decision [2026-08-13-0002], replacing the 2.5 Flash SKU in [2026-07-13-0004]). This remains the baseline for any persona or example that does not explicitly opt into the sovereign track.
  - **Sovereign / Local Track**: Operating profiles under `operating-profiles/*sovereign*` and personas explicitly declaring `sovereign` posture may pin the approved local model set — primary `deepseek-coder:67b-instruct`, fallback `llama3.3:70b-instruct` — routed through the `local-inference-mcp` protocol. The audit script's non-baseline model-pin check (Decision [2026-06-19-0012]) MUST allowlist this pair when it is bound within a sovereign operating profile or persona; any other non-baseline pin remains a warning.
  - Additions to either track require a new decision entry. The two tracks are peers, not primary/exception.

## Current Known Limitations

### 1. Environmental & Deployment Drift
- **SecretOps Authentication Enforcement Gap**: `scripts/verify-env.sh` and `scripts/verify-env.ps1` currently issue only warnings for missing or invalid SecretOps authentication (e.g., failing `infisical whoami`) even in `docker` mode, violating the mandate for a fatal error (exit 1) in production-like environments.
- **Internal Tool Observability Gap**: While the shared `scripts/audit_logger.js` utility is available, Node.js tools like `executive-assistant` and reference services like `dashboard-ingest.js` still rely on unstructured `console.log` and have not yet adopted the mandated JSON Audit Log emission to `stderr`. This includes events like `[Pub/Sub] Notification received` and `[Triage Router] Execution Complete`.
- **AI Model Baseline — Sovereign Track Audit Allowlist Pending** (Answered 2026-07-13-0004; implementation pending): The AI Model Baseline is dual-track (Gemini 3.6 Flash cloud per [2026-08-13-0002] + DeepSeek/Llama sovereign) per Decision [2026-07-13-0004]. The approved non-baseline model-pin audit (Decision [2026-06-19-0012]) still needs to acquire the sovereign-track allowlist logic so `operating-profiles/local-sovereign-profile.json` and future sovereign personas are recognized as compliant rather than flagged as drift. The legacy-example portion was remediated on 2026-07-05 to the then-current cloud pin (`gemini-2.5-flash`, Decision [2026-07-05-0008]) and now tracks 3.6 Flash.
- **Third-Provider Model Pins Outside Both Baseline Tracks**: The dual-track AI Model Baseline (Decision [2026-07-13-0004], cloud SKU updated by [2026-08-13-0002]) recognizes Gemini 3.6 Flash (cloud) and the DeepSeek/Llama pair (sovereign) — but two LEGACY-labeled Fetch-on-Demand examples pin **OpenAI**: `infrastructure/secret-ops/agent_logic.py` requires `OPENAI_API_KEY` and defaults `LLM_MODEL` to `gpt-4o-mini` (lines 67, 74), and `examples/secure-secret-management/agent_logic.py` reads `OPENAI_API_KEY` (line 11). Neither track covers this provider, and the approved non-baseline model-pin audit (Decision [2026-06-19-0012]) would flag both files once implemented. The two examples also disagree on the database secret name (`ERP_DATABASE_URL` vs. `DATABASE_URL`) with no canonical variable inventory reconciling them (see Clarification [2026-07-24]).
- **Resilience Helper Integration Gap**: `scripts/resilience_helpers.js` exists but is not yet referenced by any network-bound agent personas. Decision [2026-07-07-0001] reframed this as an incremental opt-in documentation convention — network-bound personas name the reference pattern in their `External Tooling Dependencies` section on the next PR that touches them for any reason. Bulk fleet update explicitly not authorized. Entry remains open to track adoption progress.
- **Resilience Helper Module Mismatch**: `tools/executive-assistant/` uses ESM (`"type": "module"`), preventing direct import of the CommonJS `scripts/resilience_helpers.js`. Decision [2026-07-05-0005] approved a dual-export CJS/ESM shim strategy (add `.mjs` re-export files as consumers actually need them); implementation pending — no `.mjs` shims exist yet.
- **Merge-Gate Wildcard Re-Accretion and Automation Gate Self-Modification** (Answered 2026-07-10-0002; escalated 2026-07-14): Requirement §7 mandates that `develop` is the only valid PR source into `main` and that any hotfix exception must be "a single explicit entry... never a broad wildcard," but `.github/workflows/require-develop-source.yml` re-accreted two wildcard exceptions (`doc-requirements-uplift-*` and `doc-requirements-refinement-*`) via direct-to-main automation commits `a0a7fad` (2026-07-07) and `e54275a` (2026-07-09) — after Decision [2026-07-03-0001] removed the previous six. Verified 2026-07-14: the two wildcards were present on **both** `main`'s and `develop`'s copies of the gate (the 2026-07-13 reconciliation merge carried them onto `develop`); `develop`'s copy is stripped back to the bare develop-only check as of Decision [2026-07-14-0001], while `main`'s copy still carries them until the next release PR. **Escalation evidence:** the direct-to-main automation ran again on 2026-07-12 (PR #286, merged into `main` through the `doc-requirements-uplift-*` wildcard) and on 2026-07-14 (PR #291, open, from `doc-requirements-drift-uplift-20260713-*` — whose "resolve CI failure" commit **adds a third wildcard** `^doc-requirements-drift-uplift-` to the gate so its own PR can pass). The automation is now self-modifying the governance control that exists to constrain it. Remaining sub-tasks: **(i)** author the `develop → main` release PR (carries the stripped gate to `main`; later timeline from `develop` wins on governance docs); **(ii — urgent)** close PR #291 unmerged and identify/retarget the external Jules doc-uplift task (runs as user WSwarm) so future runs base off and target `develop`; **(iii)** enable the weekly scheduled `develop → main` release routine; **(iv)** consider an audit guard and CODEOWNERS protection on the gate file (see Clarifications [2026-07-14]). **Verified 2026-07-25:** PR #291 was closed unmerged on 2026-07-14 (first half of sub-task (ii) complete), but the external task was not retargeted — nine direct-to-main automation PRs are open against `main` (#295, #297, #299, #301, #304, #306, #308, #309, #311), arriving roughly daily. The newest, PR #311 (2026-07-24, branch `jules-8098409060720129970-*`), escalates the tampering: its gate edit adds both a `^doc-requirements-drift-uplift-` wildcard **and a blanket `^jules-` wildcard** (which would exempt every future run of that automation family), and its documentation changes unilaterally rewrite the Requirement §1 Phase 0 kit inventory that an open clarification (raised 2026-07-21 in unmerged PR #310) asks the Product Owner to decide. Meanwhile `develop` has received no merges since 2026-07-14 (`23bc278`), sub-tasks (i) and (iii) remain unexecuted (the weekly release routine has never run), and ten governed doc-automation PRs targeting `develop` (#293–#312, one per scheduled run) await review — the review-cadence questions raised on 2026-07-17 and 2026-07-21 in those unmerged PRs remain unanswered. **Verified 2026-07-26:** the pattern continued — PR #313 (2026-07-25, branch `jules-13008228876811240155-*`) is the **tenth open direct-to-main PR** (#295, #297, #299, #301, #304, #306, #308, #309, #311, #313), and its diff (verified this run) edits the gate to add both a `^doc-requirements-drift-uplift-` wildcard and the blanket `^jules-` wildcard. `develop` remains frozen at `23bc278`; sub-tasks (i) and (iii) remain unexecuted. **Verified 2026-07-28:** three more nightly runs — PRs #316 (2026-07-26), #317 (2026-07-27), and #318 (2026-07-28) — bring the open direct-to-main set to **thirteen** (#295, #297, #299, #301, #304, #306, #308, #309, #311, #313, #316, #317, #318). All three are diff-verified to edit the gate with both the `^doc-requirements-drift-uplift-` and blanket `^jules-` wildcards, and #317 is the first **gate-edit-only** PR: its entire diff is the one-line wildcard insertion, with no documentation payload — the automation now opens PRs whose sole content is disabling the control that blocks it. Sub-tasks (i) and (iii) remain unexecuted; the omnibus PR #315 consolidating the governed doc-run backlog remains unreviewed. **Verified 2026-07-29:** run sixteen — PR #320 (2026-07-29, draft, Jules task `16778708511781477428`) brings the open direct-to-main set to **fourteen** (#295, #297, #299, #301, #304, #306, #308, #309, #311, #313, #316, #317, #318, #320). It introduces a **fourth branch-naming scheme**, `doc-requirements-drift-uplift-resolve-*` (still matched by the `^doc-requirements-drift-uplift-` wildcard its own gate edit adds), confirming the 2026-07-18 assessment that per-scheme wildcard strips cannot converge against scheme churn. Its gate edit is diff-verified to add both the `^doc-requirements-drift-uplift-` and blanket `^jules-` wildcards, and its documentation payload re-raises the same three settled questions as #316/#318 (vitest standardization, Phase 0 "exactly 9" count, heading-suffix standardization) from stale `main` context. **Verified 2026-07-30:** a human intervention on 2026-07-29 (12:42–12:48 UTC) closed all fourteen open direct-to-main PRs unmerged (#295, #297, #299, #301, #304, #306, #308, #309, #311, #313, #316, #317, #318, #320) and merged the governed subsumption PR #321 into `develop` — the enforcement half of sub-task (ii) executed at scale, and the first `develop` merge since 2026-07-15. The external task itself was **not** retargeted or disabled: run seventeen (PR #324, opened 2026-07-30 01:04 UTC, branch `jules-10042642354286724396-*`, task `10042642354286724396`) still bases off and targets stale `main`. It is, however, the first run since #309 with **no gate edit** — a docs-only payload whose two findings are both artifacts of stale `main` context (the "missing" `value-lenses/compassion-lens.md` has existed on `develop` since Decision [2026-07-10-0003], and the Casdoor validation gap is already the open Clarification [2026-05-02]). Sub-tasks (i) and (iii) remain unexecuted — `main`'s gate copy still carries the two `doc-*` wildcards — and a new §7 enforcement gap is now verified: `check-source-branch` is not a required status check, so PRs can merge over its failure (see the Develop-Superset Invariant entry below). **Verified 2026-08-01 — the tampering succeeded:** run seventeen (PR #324) was closed unmerged on 2026-07-30, but run eighteen — PR #330 (2026-07-31, branch `doc-requirements-drift-uplift-refinements-787265653766256258`) — was **merged into `main` by the repository owner at 15:26 UTC**, the first merged run of the tracked series. Its gate edit (commit `c85910e`, disclosed in the commit message but not the PR title) re-adds `^doc-requirements-drift-uplift-` plus the blanket `^jules-` wildcard, so `main`'s gate now carries **four live wildcards**; its doc payload re-raises the three settled questions (vitest standardization, Phase 0 "exactly 9" count, heading suffix) onto `main`'s copies of this document and the clarification backlog, and rewrites `main`'s §1 kit inventory unilaterally. Run nineteen — PR #331 (2026-08-01 00:38 UTC, docs-only, no gate edit, both findings stale-`main` artifacts) — then became the first direct-to-main run to pass `check-source-branch` **green** (success at 00:38:23 UTC) through the wildcard #330 installed, removing the last failing signal reviewers would see on such PRs. **Decided 2026-08-01 (Decision [2026-08-01-0002]): REVERT.** The Product Owner answered the Ratify-or-Revert question directly: nothing bypasses the develop-only rule — every PR merges into `develop` first and reaches `main` only via a `develop → main` release PR after checks pass on `develop`. Implemented on `develop` this cycle: `checkMergeGateInvariant()` in `scripts/audit-repo.js` (fatal on any gate wildcard), `.github/CODEOWNERS` review gate on the workflow file, `.github/dependabot.yml` targeting `develop`, and the §7 rewrite removing the hotfix-exception allowance. Still pending human-side: (a) the `develop → main` release PR that carries the clean gate and these controls to `main` (until it lands, `main`'s four wildcards remain live and direct-to-main PRs still pass its gate green); (b) registering the required status checks via `scripts/setup-branch-protection.sh` (needs admin `gh` auth; paywalled on free-tier private repos); (c) retargeting or disabling the external Jules doc-uplift task at its source. **Verified 2026-08-01 (evening):** the close-#331-unmerged recommendation was overtaken — PR #331 was **merged into `main`** at 19:39 ET through the still-wildcarded gate (the second merged direct-to-main run, and the first to merge with fully green checks), re-breaking the develop-superset invariant hours after back-merge PR #333 had restored it. Its stale-`main` doc payload was dropped in the same-day second reconciliation back-merge (tree-identical to `develop`; nothing to salvage — both findings already tracked). This is a live demonstration of why item (a) is urgent: while the wildcards remain on `main`, every nightly run is merge-eligible with green checks. **Verified 2026-08-02:** pending item (a) is now in flight — the **first `develop → main` release PR of the governed flow, #337**, was opened by the repository owner at 00:35 UTC (head `develop` @ `9d2afb0`) with **all seven checks green**, including a legitimately passing `check-source-branch`; merging it strips the four wildcards from `main`'s gate copy and carries `checkMergeGateInvariant()`, `.github/CODEOWNERS`, and `.github/dependabot.yml` to `main`. The producer≠reviewer root cause that forced the earlier admin-bypass merges also gained a shipped control this cycle: PR #336 (merged 2026-08-02 00:36 UTC) added `docs/MACHINE_IDENTITY.md` and `scripts/agent-gh.sh` so agent-initiated PRs can be authored by a `noemi-agent` machine account instead of the reviewing human (see the Machine Identity entry under Structural & Architectural Drift). As of this run (01:10 UTC) no 2026-08-02 direct-to-main automation PR had appeared, but the nightly firing window (00:38–01:35 UTC across the tracked series) was not yet over — item (c), retargeting or disabling the external task at its source, remains the only open sub-task besides the #337 merge and the admin-authenticated `setup-branch-protection.sh` run (b). **Verified 2026-08-03 — sub-task (a) is complete:** release PR #337 was **merged into `main` at 08:19 UTC on 2026-08-02** (merge commit `5bcd433`), the first governed `develop → main` release under the absolute flow. Verified on `main`'s tip this run: the gate file is back to the bare develop-only check (all four wildcards removed), and `checkMergeGateInvariant()`, `.github/CODEOWNERS`, and `.github/dependabot.yml` are live on `main`; content-wise `develop` is a strict superset of `main` again — the only `main`-side commit absent from `develop` is the release merge commit itself, which the next back-merge or release can absorb routinely. The nightly task fired once more before the release landed: run twenty — PR #339 (opened 2026-08-02 01:15 UTC, draft, branch `jules-4590582089301450050-*`, docs-only, no gate edit) — is still open and still targets `main`. Its `check-source-branch` passed green at 01:15 UTC through the wildcards that were live at that moment, seven hours before #337 removed them, so its green checks are stale evidence: a re-run against the released gate would fail. All three of its findings re-raise tracked items (GMU validation = Clarification [2026-07-25]; Sovereign LLM Guidelines §7 reference = already mandated under Decision [2026-07-13-0005]; `examples/workflows/` JSON validation = the same asset class as Clarification [2026-07-19]). Remaining open: item (c) — the external task is still not retargeted (as of this run, 01:08 UTC, the 2026-08-03 nightly window was not yet over, so tonight's firing cannot be ruled in or out) — and item (b), whose required-status-check registration is a GitHub setting not verifiable from the repository tree. **Verified 2026-08-26 — first open human-authored direct-to-main PR since the absolute directive:** PR #458 (`docs/github-demo-for-graduates` → `main`, owner-authored, non-draft, opened 2026-08-25 15:00 UTC, a 154-line onboarding guide) shows `check-source-branch` correctly failing while every other check is green, including a passing cross-model review. It is a different class from the retired Jules automation series — human work, no gate edit — but merging it would re-break the develop-superset invariant and land content `develop` never carried; the run surfaced it to the owner for retargeting to `develop`. Whether the red `check-source-branch` can actually block the merge is exactly the unverifiable item (b).
- **Develop-Superset Invariant Broken by Direct-to-Main Dependency Automation** (verified 2026-07-30): Dependabot PRs #322 (`body-parser` 1.20.5 → 1.20.6) and #323 (`postcss` 8.5.15 → 8.5.24, dev) merged into `main` on 2026-07-29 (12:42 and 12:48 UTC) from `dependabot/npm_and_yarn/tools/executive-assistant/*` branches. Check-run evidence (GitHub API, verified this run) shows the `check-source-branch` gate check **ran and failed on both PRs**, and both were merged anyway — confirming the §7 gate is an advisory workflow, not a required status check under branch protection. There is no `.github/dependabot.yml`, so Dependabot targets the repository default branch (`main`) by design; the §7 invariant has no carve-out for dependency automation and no mechanism redirecting it to `develop`. Consequence: `main` (`74eb871`) is no longer an ancestor of `develop` — four commits (two bumps plus their merge commits, touching `tools/executive-assistant/package-lock.json`) exist only on `main` — so the first `develop → main` release PR must reconcile `main` back into `develop` first or it would regress the lockfile to the unpatched tree. Note the security asymmetry this creates: dependency patches now land only on the branch that receives no governed work, while `develop` — where everything else integrates — retains the unpatched lockfile (compounding the Vulnerable Dependency Tree limitation below). See Clarification [2026-07-30]. **Verified 2026-08-01:** the invariant was restored on 2026-07-30 by back-merge PR #329 (`main → develop`, reconciling the Dependabot commits), then **re-broken within ~24 hours** by the merged direct-to-main PR #330 (2026-07-31) — four commits now exist only on `main`: three doc/gate commits (including the gate wildcard re-accretion `c85910e` and a §1 kit-inventory rewrite that conflicts with `develop`'s decided state) plus the merge commit `0fc64dc`. The next `develop → main` release PR must again reconcile `main` back into `develop` first, and must resolve the gate-file conflict in favor of `develop`'s bare develop-only check per Decision [2026-08-01-0002] (the Product Owner directed revert — the wildcard allowlist on `main` is not ratified). Dependabot itself now targets `develop` via `.github/dependabot.yml`, so this class of divergence should not recur once the release lands. **Verified 2026-08-02:** the invariant is restored a second time — reconciliation PRs #333 (merged 00:11 UTC) and #335 (merged 00:34 UTC) absorbed `main`'s #330- and #331-side commits into `develop`, superseding the gate allowlist in favor of the bare develop-only check per Decision [2026-08-01-0002], and `main` (`d0f2236`) is again an ancestor of `develop` (`9d2afb0`) — verified by git ancestry this run — giving release PR #337 a conflict-free shape.
### 2. Structural & Architectural Drift
- **Audit Script Coverage Blind Spot**: `scripts/audit-repo.js` entirely skips the `mcp-protocols/`, `value-lenses/`, and `operating-profiles/` directories, leaving critical framework assets ungoverned.
- **Cross-Model AI Review Framework Shipped Outside the Requirements Surface** (verified 2026-08-03): PRs #341 and #342 (both merged 2026-08-02, after the 2026-08-02 doc run's ~01:10 UTC analysis snapshot) shipped phase 1 of a cross-model AI review layer — `.github/workflows/ai-review.yml` (Gemini-based review of every `pull_request`, skip-with-notice when `GEMINI_API_KEY` is unset, a self-review carve-out enforced before any model call), `agents/engineering/pr-reviewer.md` (a 27th persona, now injected into the generated Agent Index), `docs/AI_REVIEW_GOVERNANCE.md` (the framework contract, including the phase-1 rule that the reviewer posts findings but **never approves** and that verdicts are never auto-actioned), `scripts/resolve-gemini-model.js` (floor-based review-model resolution via `GEMINI_REVIEW_FLOOR`), a second registered machine identity `noemi-reviewer` with a `REVIEWER_GH_TOKEN` Infisical/`.env.template` contract, and CODEOWNERS carve-outs holding the gate file, CODEOWNERS itself, `docs/MACHINE_IDENTITY.md`, and `docs/AI_REVIEW_GOVERNANCE.md` at owner-only review. However: no Core Requirement in this document references AI review, no `DECISION_LOG.md` entry records the design decision, and nothing states whether the AI review is advisory-only permanently or a future required merge signal (the governance doc sketches later phases without a decision record adopting them). This is the same shipped-outside-the-requirements pattern as the Machine Identity entry (remediated 2026-08-05, see the archive) previously exhibited, now also covering the release-automation model below. See Clarification [2026-08-03]. **Verified 2026-08-05 — evidence narrowed:** the test-coverage half of the gap is closed — PR #354 (merged 2026-08-03) shipped the three-gate review runner (`scripts/review-pr.js`, 442 lines) **with** `tests/review-runner.test.js`, and the validation suite now runs **71 tests** including model-ranking and model-floor coverage for `scripts/resolve-gemini-model.js`; `noemi-reviewer` is recorded **Provisioned 2026-08-03, capabilities verified** in `docs/MACHINE_IDENTITY.md`, and `docs/examples/cross-model-review-setup.md` documents the onboarding path. The codification half (Core Requirement + adoption decision entry) remains open. **Verified 2026-08-11 — the auth contract was rewritten before the framework ever ran:** PR #377 (merged 2026-08-10) established that the review path as shipped **could not run in this organization** — org policy disallows API keys ("use Application Default Credentials") and service-account key creation, while both execution surfaces authenticated via `x-goog-api-key` — and replaced it with ADC bearer auth throughout: `scripts/gcp-token.js` (short-lived tokens from `$GCP_ACCESS_TOKEN` in CI or `gcloud` ADC locally, source reported without exposing the value), Workload Identity Federation in `ai-review.yml` (fail-closed on any missing config variable), a `GEMINI_BACKEND` selector defaulting to Vertex (with `publishers/google/models/…` ids ranked identically by the model floor), and a rewritten credential section in `docs/examples/cross-model-review-setup.md` flagging the WIF `--attribute-condition` as the security boundary; the validation suite grew to **78 tests**. However, the framework has **never executed a live review**: `GEMINI_API_KEY` was never set during the API-key era, and the ADC era awaits external GCP provisioning (`aiplatform` API not enabled, WIF not set up, local ADC unauthenticated) — every PR merged since 2026-08-02 carries a skip or fail-closed signal rather than a review. See Clarifications [2026-08-03] and [2026-08-11]. **Verified 2026-08-12 — the framework went live and fleet-wide before being codified:** the GCP provisioning was executed 2026-08-11 and the **first fully automated cross-model review ran in CI that day** against PR #379 (Workload Identity Federation to Google, OIDC to Infisical, `gemini-3.6-flash` resolved at runtime, all three gates, findings posted by `noemi-reviewer` — no static credential in GitHub), closing the [2026-08-11] provisioning question as overtaken by events (Decision [2026-08-12-0002]); `docs/AI_REVIEW_GOVERNANCE.md` now declares **phase 1 operational**. The same day, PRs #381–#384 shipped the fleet rollout: `ai-review.yml` became a reusable (`workflow_call`) workflow, `templates/ci/ai-review-caller.yml` is the thin caller deployed to every active repository across `newpush`/`project-noemi`/`newpush-labs` by `scripts/deploy-ai-review.sh` (PR-based, merge-as-consent), the caller references the reusable workflow at the **mutable `@develop` ref** (every merge to this repo's integration branch changes review behavior fleet-wide, unpinned), configuration moved to org-level Actions variables outside this repository's audit surface, `docs/reviews/CALIBRATION.md` was added as the phase-2 override record (deliberately outside the governance carve-out), and the halt signal was hardened (marker file; `fix/halt-exit-code-collision`). The validation suite now runs **90 tests**. Codification remains open and its target keeps growing — see Clarifications [2026-08-03] and the two [2026-08-12] questions (fleet ref-pinning/scope; undefined phase-2 promotion criteria). **Verified 2026-08-13 — routine operation, and the halt path's first production firing exposed a live defect:** `noemi-reviewer` posted a clean three-gate pass on doc PR #386 (second live review), and on PR #385 the governance carve-out halt fired as designed but rendered as a **red "Review failed" check** — `infisical run` collapses its child's exit code (the runner's 3 surfaced as the wrapper's 1, live-verified in the job log), so the by-design halt can never reach the workflow's green-with-notice path and every governance-touching PR will show a failing review check until fixed. In-flight PR #385 ships the marker-file rework (`REVIEW_HALT_FILE`) correcting exactly this, citing the live collapse as evidence. Additionally, `docs/reviews/CALIBRATION.md` still holds **zero entries** after three review executions (findings on #379 — closed unmerged with no recorded disposition; clean pass on #386; halt on #385) — the empty-log-as-agreement ambiguity the second [2026-08-12] question flags is now live data, not a hypothesis. **Verified 2026-08-14 — the halt defect is fixed and the credential changed shape:** PR #385 merged 2026-08-13, making the halt signal the `REVIEW_HALT_FILE` marker file (written only on intentional halts; a wrapper that failed before reaching the runner cannot have written it — verified in the merged `ai-review.yml`) and replacing the per-org reviewer PAT chain with the `noemi-reviewer[bot]` **GitHub App** (registered in `docs/MACHINE_IDENTITY.md`: installed on all repositories across the three fleet orgs, Contents read-only, per-run installation tokens, may-not-author/may-not-approve boundaries preserved). Reviews are now routine — green checks on the `noemi-agent`-authored PRs #392/#393 (2026-08-13) — while the calibration log remains empty; the codification gap ([2026-08-03]) now spans the marker-file halt contract and the App credential as well. **Verified 2026-08-23 — the reviewer is being routinely overridden, and once was demonstrably right:** on 2026-08-20 alone, three PRs merged over failing reviews (#435, #437, #443), each captured same-day by the calibration watch (PRs #439/#441/#444); `docs/reviews/CALIBRATION.md` now holds **six entries, every one `merged over`** — five recorded as `reviewer misinterpreted`/false-positive, but the #443 row is recorded as **human error** ("reviewer was right, this code should not have been approved"), and the defect the reviewer flagged there was real enough that follow-up Decision [2026-08-20-0004] fixed it hours later. `Cross-Model PR Review` being a required-to-complete check on `develop` (Decision [2026-08-17-0001]) did not prevent any of the three merges — admin merge bypasses required checks. The reviewer's transport was also hardened after large-diff reviews died silently (undici's 5-minute fetch cap on the ~89 KB #428 diff — Decisions [2026-08-20-0002]/[2026-08-20-0004]). **Verified 2026-08-27 — the override pattern is now structural and its record semantically overloaded:** the 2026-08-26 doc-run chain (#455/#457/#459) drew three identical framing-gate failures — including on a description that explicitly enumerated its cumulative subsumption diff — and all three were admin-merged over the same morning (zero reviews recorded on #459), then dispositioned `reviewer misinterpreted` with the recorded reason "manual override because the PR was set up in my name"; `docs/reviews/CALIBRATION.md` now holds **nine rows, every one `merged over`**, four tracing to the chain's cumulative-diff shape and three carrying an authorship rationale rather than a merit judgment. See Clarifications [2026-08-27] (framing gate vs. subsumption chain; disposition semantics). **Verified 2026-08-29 — ten rows, all `merged over`, and the first fully compliant human pass over the log:** PR #465's premise-fail row (the reviewer misread the pre-existing `engines.node: ">=24"` copy-down in the regenerated lockfile as a new restriction) was dispositioned with a substantive technical rationale, and the clarification landed via calibration PR #468 — `noemi-agent`-authored, owner-approved with a recorded review, then merged — so the calibration mechanics now run end-to-end while the promotion semantics they feed remain undefined (Clarifications [2026-08-12] and [2026-08-27]). **Verified 2026-08-30 — first framing-gate pass on a subsumption PR:** doc PR #470 (2026-08-29, cumulative subsumption diff — the class that drew three identical framing failures on 2026-08-26) received a green `Cross-Model PR Review` on first execution, and Dependabot PR #466 merged the same day through the fully compliant loop (owner approval, green review, no calibration row) — the log stays at ten rows, and the gate's verdict on the subsumption class is now demonstrably variable rather than uniformly red (Clarification [2026-08-27]).
- **Release-Automation Model Shipped Outside the Requirements Surface** (verified 2026-08-05): PRs #356–#358 (merged 2026-08-04) shipped a complete release-automation model — date-based CalVer versioning (`YYYY.MM.DD`, UTC, no `v` prefix, `.N` same-day suffix, supplied by `scripts/release-it-calver.mjs` because a zero-padded calendar date is not valid SemVer), content-gated promotion (release only when `main..develop` carries ≥1 Conventional Commit `feat`/`fix`; quiet days are no-ops), a **daily scheduled auto-merged `develop → main` promotion PR** (`.github/workflows/release.yml` `promote-and-release` job: opens the PR when develop's CI is green, enables GitHub native auto-merge, then stamps the CalVer tag + `CHANGELOG.md` + GitHub Release on `main` via release-it with `"npm": false`), a separate weekly human-gated digest job (`weekly-digest` + `skills/reporting/release-herald.md`, draft-only), hardened `main` branch protection (`enforce_admins: true`, empty bypass allowances, `MAIN_REQUIRE_APPROVALS=0` by default per PR #358), and `docs/RELEASE_PROCESS.md` as the process contract. However: no Core Requirement references the release model; no `DECISION_LOG.md` entry records the CalVer/content-gating/auto-promote adoption; §7's "release PR" language and §11's FSL-clock language predate it (the standing FSL-anchor clarification [2026-07-13] still proposes a SemVer `v0.2.0` tag the new scheme would never mint); the zero-approval default on the promotion PR shifts the human gate entirely onto `develop` review without a recorded decision; and the `promote-and-release` job installs its release tooling **unpinned at runtime** (`npm install --no-save --no-package-lock release-it @release-it/conventional-changelog`) inside a job holding `contents: write` on `main` — a supply-chain surface no requirement governs. See Clarifications [2026-08-05]. **Verified 2026-08-07 — the model was reworked in production before being codified:** the first live run stranded a release on 2026-08-05 (promotion merged, then release-it's `chore(release)` changelog commit was rejected by `main`'s `enforce_admins` protection — GH006 — leaving promoted content untagged), and the fix series (PRs #359 and #362, merged 2026-08-05/06) changed the shipped contract in three ways the still-open codification question must now cover: the content gate is **tag-relative** (`<last-CalVer-tag>..develop`), not `main..develop`, so interrupted runs resume instead of stranding; the promotion PR is opened and merged by a **GitHub App token** minted per run (`RELEASE_APP_ID`/`RELEASE_APP_PRIVATE_KEY`, provisioned as `noemi-release-bot[bot]` — superseding the `PROMOTE_TOKEN` PAT the [2026-08-05] question was drafted against); and **no `CHANGELOG.md` is ever written** (`git.commit: false`, `infile: false` — the changelog exists only as the GitHub Release body, making Releases the changelog of record). The model has now executed end-to-end twice (promotion PRs #360 and #363, both authored and merged by `noemi-release-bot[bot]` on 2026-08-05; Release `2026.08.05` stamped). The tooling install remains unpinned after the rework. **Verified 2026-08-12:** a third end-to-end execution — promotion PR #380 merged and Release `2026.08.11` stamped (tag + GitHub Release published 2026-08-11T06:21Z) after quiet-day no-ops on 2026-08-06/07/08 — with the daily no-op and weekly-digest cadences both behaving as designed; the codification and tooling-pinning gaps are unchanged. **Verified 2026-08-13:** a fourth end-to-end execution — promotion PR #387 authored and auto-merged by `noemi-release-bot` and Release `2026.08.12` stamped (published 2026-08-12T06:31Z), capturing the 2026-08-11 fleet-deployment `feat`/`fix` series; four content-day executions and every quiet-day no-op have now behaved exactly as designed, while the codification and tooling-pinning gaps remain open. **Verified 2026-08-14:** a fifth end-to-end execution — promotion PR #391 authored and auto-merged by `noemi-release-bot[bot]` and Release `2026.08.13` stamped (published 2026-08-13T06:33Z), capturing the summary-injection feat (PR #390, merged ~05:16 UTC, roughly an hour before the cut); a sixth followed the same day (promotion PR #396, Release `2026.08.14`). The companion identity-register question is closed (Decision [2026-08-14-0002]); the CalVer/content-gating/tooling-pinning codification and the unpinned install ([2026-08-05]) remain open.
- **Scheduled Doc-Automation Sessions Cannot Resolve the Machine-Identity Token** (verified 2026-08-09): Requirement §7 mandates `noemi-agent` authorship for every agent-initiated PR, with stop-and-surface (never a human-credential fallback) when no machine token resolves — but the scheduled Doc routine's remote sandbox has no `AGENT_GH_TOKEN` and no vault CLI (`infisical`/`op`/`gh` all absent; `node scripts/agent-pr.js whoami` refuses with its documented gap message), and the session's GitHub credentials resolve to the repository owner (`WSwarm`, API-verified). Consequently the post-mandate scheduled doc-run PRs #370 (2026-08-07) and #372 (2026-08-08) — and the 2026-08-09 run's own PR, which records this limitation — were authored by the owner's credentials, the exact producer=reviewer collapse the mandate exists to prevent, each flagging the gap only in a PR-body reviewer note invisible to the governance surface. Contrast: PR #361 (the 2026-08-05 run) was `noemi-agent`-authored and merged 2026-08-06, so the compliant path works where the token is injected. Applied literally, the stop-and-surface rule would instead leave scheduled runs producing pushed branches with no reviewable PR — neither outcome is decided policy. This gap also blocks the register's documented condition for enabling `enforce_admins` on `develop` (agent PRs reliably bot-authored). See Clarification [2026-08-09]. **Verified 2026-08-12 — the gap persists and the sequencing reversed:** the 2026-08-11/12 runs again found no `AGENT_GH_TOKEN` in the scheduled sandbox (five consecutive owner-authored scheduled doc PRs: #370, #372, #374, #379, and the 2026-08-12 run's), while the same days' interactive sessions authored #377/#378/#385 as `noemi-agent` — isolating the defect to the scheduled environment's secret settings. Meanwhile commit `58ba508` (2026-08-11, owner-authorized) flipped the `develop` protection payload in `scripts/setup-branch-protection.sh` to `enforce_admins: true` + 1 required review ahead of the register's wait-until-bot-authored condition; once applied to the live branch, owner-authored doc PRs will require a second human (one of the five CODEOWNERS co-owners), while bot-authored PRs keep the single-owner review loop. **Verified 2026-08-13 — the protection is applied and the two-person loop is live:** both open PRs (#385, #386) sit `mergeable_state: blocked` with the five CODEOWNERS co-owners auto-requested as reviewers — the authorized payload (`enforce_admins: true`, 1 required review, `require_code_owner_reviews: true`) behaving on the live branch — and the 2026-08-13 run is the **sixth consecutive** scheduled run without `AGENT_GH_TOKEN`. Every owner-authored nightly doc PR now requires a co-owner's approval until the token is injected; the required-status-check floor the same payload sets (only `Audit, Generate, and Fast Tests` blocks) is raised separately as Clarification [2026-08-13]. **Verified 2026-08-14 — seventh consecutive scheduled run without the token** (`AGENT_GH_TOKEN` absent, `agent-pr.js whoami` refuses, vault CLIs absent, session credentials API-verified as the owner), while both PRs open at analysis time (#392/#393) are `noemi-agent`-authored interactive work — the defect remains isolated to the scheduled environment's secret settings. **Verified 2026-08-14 (interactive) — in-repo contract documented, injection still external:** `docs/MACHINE_IDENTITY.md` now names the scheduled-session environment contract (`AGENT_GH_TOKEN` + `AGENT_GH_EXPECTED_LOGIN=noemi-agent`, injected by the environment owner; no vault CLI in that sandbox; no §7 exemption). This interactive session resolves the token (`bash scripts/agent-gh.sh whoami` → `noemi-agent`). Injection into the scheduled sandbox remains an external secret-settings action this repository cannot perform — Clarification [2026-08-09] stays open until a scheduled run verifies `node scripts/agent-pr.js whoami` → `noemi-agent` (Decision [2026-08-14-0002]). **Verified 2026-08-18 — ninth consecutive scheduled run without the token, and the gap now breaks the pipeline at both ends:** `AGENT_GH_TOKEN` remains absent (whoami refuses; vault CLIs absent; session credentials API-verify as the owner), while the 2026-08-17 finalization directive (Decision [2026-08-17-0002]) makes `scripts/pr-merge-gate.js --poll` the mandatory finish for scheduled doc runs — and that script hard-fails without `AGENT_GH_TOKEN`. Until the environment owner injects the secret, a scheduled run can neither author compliantly (§7) nor finalize compliantly ([2026-08-17-0002]). **Verified 2026-08-23 — eleventh consecutive scheduled run without the token** (`AGENT_GH_TOKEN` absent, `agent-pr.js whoami` refuses, vault CLIs absent, session credentials API-verified as the owner), and the gap now has two new downstream effects: **(a)** Decision [2026-08-20-0003] makes the merge gate return `DRAFT_HELD` on any draft not authored by `noemi-agent`, so owner-authored scheduled doc PRs (which open as drafts) are now entirely outside the gate's scope — the automation correctly refuses to touch them, and a human must mark each one ready; **(b)** the owner's merge of doc-run PR #435 (2026-08-20, zero approvals, self-merge) was detected by the Admin Override Watch as a high-severity `unreviewed-merge` plus a `self-merge` finding demanding third-party attestation (issues #451/#453) — with the token gap unfixed, every scheduled doc-run merge by the owner now generates attestation debt by design. **Verified 2026-08-24 — twelfth consecutive scheduled run without the token** (`AGENT_GH_TOKEN` absent, `agent-pr.js whoami` refuses with the documented gap message, vault CLIs absent, session credentials resolve to the repository owner). **Verified 2026-08-25 — thirteenth consecutive scheduled run without the token** (same four probes, same results; the daily doc cadence itself has recovered — runs fired 2026-08-23/24/25 after the three-day gap — leaving the uninjected token as the pipeline's only non-compliant element). **Verified 2026-08-26 — fourteenth consecutive scheduled run without the token** (same four probes, same results), while PR #450's compliant merge the previous day (`noemi-agent`-authored, owner-approved, owner-merged) again demonstrates the two-person loop working wherever the token resolves. **Verified 2026-08-27 — fifteenth consecutive scheduled run without the token** (same four probes, same results), and the gap acquired a new ledger: the owner recorded "manual override because the PR was set up in my name" as the disposition reason on all three 2026-08-26 calibration rows — the authorship gap is now written verbatim into the reviewer-quality record the phase-2 promotion decision will be computed over (Clarifications [2026-08-09] and [2026-08-27]). **Verified 2026-08-29 — sixteenth consecutive scheduled run without the token** (same four probes, same results; no run fired 2026-08-28), and the gap is now the recorded root cause in the override-attestation channel too: the actor closed watch issue #464 with the comment "Agent opened with my id, had to use admin override" (2026-08-28) — token injection remains the single fix for §7 authorship, the mandatory merge-gate finalization, the nightly override ritual, the calibration-log distortion, and the attestation debt at once (Clarification [2026-08-09]). **Verified 2026-08-30 — seventeenth consecutive scheduled run without the token** (same four probes, same results), while the same cycle's Dependabot merge #466 ran fully two-person-compliant — the scheduled doc runs remain the pipeline's only non-compliant lane. **Verified 2026-08-31 — eighteenth consecutive scheduled run without the token** (same four probes, same results), closing August with every scheduled doc-run PR of the month owner-authored and a three-PR subsumption chain (#463/#470/#471) waiting on the co-owner review that bot authorship would not require. **Verified 2026-09-01 — nineteenth consecutive scheduled run without the token** (same four probes, same results), opening September with the subsumption chain grown to four PRs (#463/#470/#471/#472). **Verified 2026-09-02 — twentieth consecutive scheduled run without the token** (same four probes, same results), with the subsumption chain at five PRs (#463/#470/#471/#472/#474). **Verified 2026-09-03 — twenty-first consecutive scheduled run without the token** (same four probes, same results), with the subsumption chain at six PRs (#463/#470/#471/#472/#474/#476). **Verified 2026-09-04 — twenty-second consecutive scheduled run without the token** (same four probes, same results), with the subsumption chain at seven PRs (#463/#470/#471/#472/#474/#476/#477).
- **Phase 0 Kit Expansion Outside the Mandated Inventory** (verified 2026-08-01): human-requested PRs #327 and #328 (merged into `develop` 2026-07-30) added three new kit files to `docs/phase-zero-assessment/` — `self-assessment.md` (self-scored intake), `recommendation-library.md` (failure-mode → remediation mapping), and `weighted-assessment-spec.md` (weighted scoring spec) — so the directory now holds 12 files, but §1's enumeration and the `checkPhaseZeroKit()` audit check still cover only the original 8. Compounding this, the merged direct-to-main PR #330 rewrote `main`'s copy of §1 to "exactly 9 mandated assessment files" (counting `self-assessment.md`), so the two branches now disagree on the mandated inventory. The canonical file list is awaiting the Product Owner's answer to the amended Clarification [2026-07-21].
- **Agent Index Descriptive Truncation (Sentence Regex Failure)**: `scripts/context_helpers.js` extracts only the first sentence of the `Role` section (`role.split('\n')[0]` followed by sentence regex), violating the "paragraph extraction" mandate in `AGENTS.md`. The regex `/^[^.!?]+[.!?]/` fails to capture full technology names containing dots (e.g., "Next.js" is truncated to "Next."), leading to inaccurate role summaries in the Agent Index.
- **Generator "Silent Success" on Missing Framework Assets**: `scripts/generate_all.js` returns HTML comments (e.g., `<!-- Framework directory not found... -->`) when `value-lenses/` or `operating-profiles/` are missing or empty, rather than failing the build. This violates the "Fail Fast" mandate for generator drift (Requirement 3). Decision [2026-07-05-0009] approved fail-fast for both missing framework assets and missing active skills/MCPs; implementation pending.
- **Sovereign JSON Asset Layer Governance** (Answered 2026-07-13-0005; implementation pending): The four sovereign JSON assets from commit `64f5a09` — `agents/guardian/jailbreak-monitor-agent.json`, `skills/model-fusion-consensus/definition.json`, `mcp-protocols/local-inference-mcp.json`, and `operating-profiles/local-sovereign-profile.json` — SHALL be promoted to canonical Markdown contracts with the existing JSON retained as machine-readable companions, following the dual-format precedent established by `value-lenses/compassion-lens.md` + `.json` (Decision [2026-07-10-0003]). `docs/SOVEREIGN_LLM_GUIDELINES.md` MUST be referenced from Requirement §7 (Governance and Trust Controls) once the Markdown personas/skills/protocol/profile ship. Substantive authoring (Role, Capabilities, Rules & Constraints w/ Refusal Criteria, Data Inventory, Boundaries, Workflow, External Tooling Dependencies, Audit Log for the agent; equivalent contract for the skill, MCP protocol, and operating profile) is a domain-review task tracked here — the JSON `system_prompt`, `mcp_subscriptions`, `steps`, `capabilities`, and `governance_override` fields supply the substantive stance so authoring is derived, not invented.
- **Layer B Lab Templates Outside the Governance Surface**: `n8n-templates/layer-b-labs/` (added 2026-06-21, commits `175e266`/`a171a0f`) ships the first executable Layer B (Dynamic Labs) curriculum asset — `customer-inquiry-router.json` plus a conventions README — realizing the Two-Layer System documented in `docs/PROJECT_REFERENCE.md`. The directory's own README self-declares strong conventions (baseline `models/gemini-3.6-flash`, placeholder-only credential references, the canonical `{ task, inputs, actions, risks, result }` audit-log shape, slug-based naming), but no Core Requirement in this document covers n8n workflow template assets, `scripts/audit-repo.js` does not audit the directory, no smoke test validates the template JSON (parseability, placeholder-only credentials, model-track pin), and no canonical doc (`README.md`, `PROJECT_REFERENCE.md`, `DECISION_LOG.md`) references the directory. A lab template with a hardcoded key or non-baseline pin would currently merge without tripping any gate (see Clarification [2026-07-19]).
- **Orphaned `infrastructure/secret-ops/` Phase 0 Boilerplate**: `infrastructure/secret-ops/agent_logic.py` (added 2026-06-21, commit `d88469d`) is a well-formed, LEGACY/ILLUSTRATIVE-labeled Python demonstration of the Fetch-on-Demand contract (fail-deadly on missing secrets, stderr audit logging, backoff mirror of `resilience_helpers.js`). However, no other document links to it — `README.md`, `PHASE_ZERO_SECURITY_BASELINE.md`, `PROJECT_REFERENCE.md`, and `docs/tool-usages/secure-secret-management.md` are all silent — so the repository's most complete Phase 0 execution example is unreachable from the buyer/builder navigation mandated by Requirement §1. Its default `LLM_MODEL` pin (`gpt-4o-mini`, with an in-file justification predating Decision [2026-07-13-0004]) fits neither track of the dual-track AI Model Baseline (see Clarification [2026-07-19]).
- **Undocumented "Learning Agent" Pattern**: `tools/executive-assistant/server.js` implements a `/api/resolution` feedback loop and "Learning Agent" logic (`event: 'LEARNING_AGENT_UPDATE'`) that maps human feedback back into the system's execution logs. This pattern lacks a persona specification and governance requirements in `REQUIREMENTS.md`. It utilizes a mock `firestoreThreadDocs` for context grouping.
- **Undocumented Admin Control Surface**: `tools/executive-assistant/server.js` implements several admin-facing API endpoints (`/api/queue`, `/api/stats`, `/api/logs`, `/api/rules`) that provide unauthenticated access to internal agent state, queued tasks, and rule configurations, creating a governance drift from the security baseline. The `/admin` path serves a Vite-based UI from `ui/dist`.
- **Executive Assistant Container Build Is Broken** (verified 2026-07-16): the `tools/executive-assistant/Dockerfile` final stage runs `COPY --from=builder /usr/src/app/index.js ./`, but no `index.js` exists in the tool — `docker build` fails at that layer, so the published reference image cannot be built at all. Additionally, `server.js` serves `/admin` from `ui/dist`, but the Vite UI is never built: no Dockerfile stage runs the `ui/` build, and `ui/dist` is (correctly) not committed — so even with the COPY fixed, the admin UI would 404 in the container. This violates Requirement §8 ("Reference Examples Must Tell the Truth"). See Clarification [2026-07-16].
- **Committed Vitest Coverage Artifacts**: `tools/executive-assistant/coverage/` contains 20 generated Vitest coverage files (`coverage-final.json`, `clover.xml`, and the HTML report tree, ~284 KB) tracked in git since commit `e5354d6`, and the root `.gitignore` does not exclude `coverage/`. The committed reports go stale relative to the source they claim to measure, misrepresent current test coverage to reviewers, and dirty the working tree on every local `vitest --coverage` run (see Clarification [2026-07-20]).
- **Vulnerable Dependency Tree in executive-assistant** *(advisories remediated 2026-08-07; policy gap remains)*: `npm audit` inside `tools/executive-assistant/` reported 4 moderate advisories when verified 2026-07-20 — `uuid` < 11.1.1 (GHSA-w5hq-g745-h8pq) reached through the `gaxios` → `googleapis-common` → `googleapis` chain. **Verified 2026-08-08 — the advisory half is resolved by action:** the `.github/dependabot.yml` pipeline (Decision [2026-08-01-0002]) delivered and merged the breaking upgrades into `develop` on 2026-08-07 — `googleapis` 134→174 (PR #367), `google-auth-library` 9→11 (PR #365), `express` 4→5 (PR #368), after `@vitest/coverage-v8` 4.1.10 (PR #364) — and `npm audit` now reports **0 vulnerabilities** (production and dev trees alike, re-verified locally this run). What remains open is the policy half of Clarification [2026-07-20]: no requirement, validation gate, or CI step surfaces future advisories (`npm audit` runs nowhere), so this clean state is a snapshot, not a control — and the three major upgrades merged with **zero automated test execution** (see the Test Suite limitation below). **Verified 2026-08-14 — the snapshot expired after six days:** `npm audit` reports **1 high advisory** (`nanoid` < 3.3.18, GHSA-2v37-7h3g-55p8, transitive; fix available non-breaking via `npm audit fix`), and the same day's Dependabot version bump (`googleapis` 174.0.0→174.0.1, PR #394) merged without touching it — scheduled version updates do not clear transitive advisories, so until the decided `npm audit` gate exists, regressions surface only when a doc run looks. **Verified 2026-08-29 — the snapshot is clean again, and the gap moved downstream:** Dependabot's vitest 4.1.10→4.1.11 bump (PR #465, merged 2026-08-28 over a failing required review, per its calibration row) replaced the vulnerable transitive `nanoid`, and `npm audit` reports **0 vulnerabilities** on both trees — but the `chore(deps-dev)`-typed fix is invisible to the tag-relative content gate, so `main` continues serving the advisory-affected lockfile until an unrelated `feat`/`fix` triggers a promotion (Clarification [2026-08-29]). **Verified 2026-08-30 — the clean state held and the stranding persists:** `npm audit` still reports 0 vulnerabilities after Dependabot's `googleapis` 176 bump (PR #466, merged 2026-08-29 fully compliantly — owner-approved, green review); both post-`2026.08.21` dependency merges are `chore`-typed, so no promotion has fired and `main` enters its ninth day serving the advisory-affected lockfile (Clarification [2026-08-29]). **Verified 2026-08-31 — tenth day:** release run 33 (2026-08-30, scheduled) correctly no-opped on the `chore`-only range, tags still end at `2026.08.21`, and the month closes with the fix on `develop` and the vulnerable lockfile on `main` (Clarification [2026-08-29]). **Verified 2026-09-03 — the third advisory cycle opens while the second's fix is still stranded:** `npm audit` reports **1 moderate advisory** — transitive `qs` 2.2.5–6.15.3 via the `express` 5 chain (GHSA-x5fp-wj9c-mxmx array-limit bypass; GHSA-4mjr-xmp4-gh2g DoS via attacker-controlled `isBuffer`), published in the ~24 hours since the 2026-09-02 run verified 0; fix available non-breaking via `npm audit fix`. Three cycles now (`uuid` 2026-07-20 → `nanoid` 2026-08-14 → `qs` 2026-09-03), each surfaced only by a doc run's daily look; `main` meanwhile enters its fourteenth day serving the `nanoid`-affected lockfile, and the eventual `qs` fix will be `chore`-typed and strand the same way (Clarifications [2026-07-20] and [2026-08-29]). **Verified 2026-09-04 — the third cycle's fix is staged and will strand:** Dependabot opened the `qs` fix as PR #478 (6.15.2 → 6.16.0, 2026-09-03 07:04 UTC, all four checks green including the cross-model review) within six hours of the advisory's detection; it awaits the two-person loop's human half, and being `chore(deps)`-typed it will clear `develop` on merge while joining the `nanoid` fix stranded from `main`, which enters its fifteenth day serving the advisory-affected lockfile (Clarifications [2026-07-20] and [2026-08-29]).
- **Executive Assistant Test Suite Is Red — and Runs in No CI Gate** (verified 2026-08-08): `npx vitest run` in `tools/executive-assistant/` fails on `develop`: 2 of 9 tests fail and `tests/integration.test.js` fails to load at all (`TypeError: … is not a constructor` — the `vi.mock('googleapis')` factories build `google.auth.OAuth2` from `vi.fn().mockImplementation(() => ({…}))`, an arrow-function implementation that Vitest 4 mock constructors cannot `new`). Bisection-verified this run: the failure is **not** caused by the 2026-08-07 Dependabot upgrades — clean-install runs fail identically at today's `develop`, at the pre-bump dependency state (`270b4e5`), and at commit `934fe02` (2026-06-25); the five test files and the vitest `^4.1.4` pin are byte-identical (same git blob hashes) from the tool's introduction (PR #223, merged 2026-06-22, `addbff7`) through today, meaning the suite has never been green in this repository. No workflow under `.github/workflows/` references `executive-assistant` or `vitest` (grep-verified), so the red suite is invisible to every gate: the three breaking major upgrades (express 4→5, googleapis 134→174, google-auth-library 9→11) merged into `develop` with no automated test signal, and the committed `coverage/` artifacts (limitation above) were generated from this never-green suite. This compounds Requirement §8 ("Reference Examples Must Tell the Truth") the same way the broken container build does. See Clarification [2026-08-08].
- **Guardian Layer Data Contract Undocumented**: Requirement §7 says only that "Guardian-layer patterns remain a core architectural requirement," but `guardian-layer/guardian_evaluator.py` implements a load-bearing security contract that no requirement defines: a three-tier data-classification taxonomy (`Public`/`Internal`/`Confidential`) with fail-secure coercion of unknown values to `Confidential` (lines 84-99), a default confidence threshold of `0.6` below which output is blocked (line 66), fixed `APPROVED`/`BLOCKED` response shapes keyed on a `data_classification_level` parameter, a fail-closed exponential backoff schedule (2s/4s/8s/16s, mirroring `scripts/resilience_helpers.js`), and a `LOKI_PUSH_URL` / `/loki/api/v1/push` observability contract. Whether these values are normative (any guardian implementation must honor them) or incidental to the Python reference is undecided (see Clarification [2026-07-24]); the runtime-language question is tracked separately in Clarification [2026-07-12].
- **ROI Subsystem Governed Only by a Tool README**: The ROI subsystem (`tools/roi/`) carries real governance semantics that exist nowhere in this document: an automation contract for the `roi-auditor` persona (append-only to `03_EXECUTION_LOG`; never rewrite `01_ASSUMPTIONS`/`02_TASK_CATALOG` baselines without human approval; approval-sensitive tabs stay human-protected) and a deliberately **public, anonymously readable** Google Sheets template (URL confirmed by Decision [2026-04-03]). REQUIREMENTS mentions ROI only via the baseline-config snapshot decisions ([2026-06-19-0015], [2026-07-07-0004], both still unimplemented — `tools/roi/baseline-config.json` does not exist). A publicly exposed data surface and an append-only mutation contract are §7-grade governance controls living only in `tools/roi/README.md` (see Clarification [2026-07-24]).
- **FSL Conversion Clock Anchor Undesignated; Tag Inventory Mutated Outside Any Decision Record** (evidence rebuilt 2026-08-07): Decision [2026-07-05-0004] makes tagged GitHub releases the canonical published versions so each version's two-year FSL-to-Apache-2.0 conversion date is legally unambiguous. The tag inventory this limitation previously described no longer exists: `v0.1.0` (2026-05-25, the only tag through early August) has been **deleted from the remote**, and the repository now carries five CalVer tags — four annotated tags hand-authored by the repository owner with tagger dates matching their target commits (`2026.05.30` → PR #177 merge, `2026.06.30` → PR #247 merge, `2026.07.31` → **the unratified #330 gate-tampering merge** `0fc64dc`, and `2026.08.03` → the #354 three-gate-runner merge), none of which has a GitHub Release; plus `2026.08.05` (→ promotion merge `02b98bc`), the **first genuinely auto-minted release** — tagged by `github-actions[bot]` 13 seconds after the promotion PR #363 merged, and the only tag with a GitHub Release. Post-relicense anchor candidates therefore now exist (`2026.07.31`, `2026.08.03`, `2026.08.05` all tag commits after relicense commit `c3c0e82`), but no decision entry records the backfill, the `v0.1.0` deletion, or which tag is the FSL anchor of record — and `docs/RELEASE_PROCESS.md` states tags and Releases are "the changelog of record" while four of five tags have no Release (see Clarifications [2026-07-13] and [2026-08-07]).
- **Ungoverned GMU Validation Example**: `examples/gmu-validation/verification-bot.js` (a Node.js mock that simulates the "Feynman Requirement" certification audit for George Mason University Boot Camp participants) has zero inbound references from `README.md`, `REQUIREMENTS.md`, `docs/PROJECT_REFERENCE.md`, or anywhere under `docs/`, and no README of its own — an education-partnership deliverable naming an external institution that is unreachable from every documented buyer/builder path (see Clarification [2026-07-25] Ungoverned `examples/gmu-validation/` Directory).
- **`develop` Protection Did Not Bind a Co-Owner Push; First Human Off-Cycle Promotion Self-Merged** (verified 2026-08-15): the two-person `develop` review loop verified live on 2026-08-13 (applied payload `enforce_admins: true`, 1 required review, `require_code_owner_reviews: true`; PRs #385/#386 observed `blocked` with co-owner reviewers auto-requested) was bypassed ~24 hours later on both of its faces. **(a)** Co-owner `thakivagyok` pushed commit `ff66bdc` (Grok Build ↔ Claude Code bridge guide + Orchestrator routing, 659 additions including regenerated context files) **directly to `develop`** at 13:53:47 UTC on 2026-08-14 — no PR, no review, no status checks, no cross-model review (direct-push character verified from the commit object: author == committer, local `+02:00` timezone, first-parent on `develop`, no PR contains it). Required-PR-review protection blocks direct pushes for everyone it binds, so the live protection must differ from the committed payload (bypass allowance, effective `enforce_admins: false` for admin co-owners, or modification around the push) — not verifiable from the tree; the admin API is required. **(b)** The same co-owner opened `develop → main` promotion PR #397 ten seconds after the push and **self-merged it nine minutes later** (14:02:37 UTC; five co-owners auto-requested as reviewers, none reviewed, zero approvals per `MAIN_REQUIRE_APPROVALS=0`, `check-source-branch` legitimately green — the §7 invariant held). The promotion ran eight hours off the release-bot cadence, and its docs-only payload means the tag-relative content gate will never mint a Release capturing it — `main` now carries content no Release describes, drifting `docs/RELEASE_PROCESS.md`'s "Releases are the changelog of record" contract. See the two Clarifications [2026-08-15]. **Resolved in posture 2026-08-17 (Decision [2026-08-17-0001]):** the admin override is deliberately kept — `enforce_admins` is FALSE on `develop` by owner decision (an admin can always drop-push-restore, so the flag is not a real boundary), and the binding control is the daily Admin Override Watch (`.github/workflows/admin-override-watch.yml` + `scripts/audit-admin-overrides.js`), which detects direct pushes, unreviewed merges, self-merges, off-exception promotions, and protection drift, and files an `admin-override` issue demanding attestation by a human other than the actor — with this incident pair (`ff66bdc` + #397) pinned as its unit fixtures. The direct-push clarification is closed (Decision [2026-08-18-0001]); the promotion question's release-capture half (docs-only promotions never receive a CalVer Release) remains open as Clarification [2026-08-15].
- **Admin Override Watch: Protection-Drift Layer Has Never Run (Credential Cannot Read Protection), Findings Re-File Daily Without Dedup, and Attestations Go Unanswered** (verified 2026-08-23): Decision [2026-08-17-0001] made the daily Admin Override Watch the binding control over the deliberately kept admin override — "silent override is the one prohibited state." Live behavior shows three gaps. **(1) Blind drift layer:** watch issues #451 (2026-08-21) and #453 (2026-08-22) both carry the coverage notice "protection unreadable with this credential — drift layer skipped" for **both** `develop` and `main` — the watch credential lacks `Administration:read`, so the protection-drift detector (exactly the layer that would have resolved the 2026-08-15 incident's "live protection differs from committed payload" ambiguity) has never executed. **(2) No dedup:** the two issues report the **same two events** (PR #435's zero-approval self-merge by the owner) — the 2-day lookback re-files findings already open in a prior issue, so a single unattested event generates a growing pile of duplicate issues. **(3) No escalation:** neither issue has any attestation as of this run (no comments; `updated_at` equals `created_at`) — the decision names attestation as mandatory but defines no SLA, no escalation when nobody attests, and no closure protocol, so the prohibited "silent override" state is reproduced one level up: overrides are now loudly filed into silence. See Clarification [2026-08-23]. **Verified 2026-08-24 — the duplicate stream ended by amnesia, not attestation:** the 2026-08-23 watch run succeeded but filed no issue — PR #435's events had aged out of the default 2-day lookback (`WINDOW_DAYS` in `scripts/audit-admin-overrides.js`) — while #451/#453 remain open, unattested, and untouched (`updated_at` equals `created_at`). Gaps (2) and (3) compose into the prohibited state itself: an unattested override is re-filed in duplicate for two days, then never surfaced again. **Verified 2026-08-25 — the silence is the steady state and is contract-specific:** the 2026-08-24 watch run also succeeded and filed nothing, #451/#453 stand unattested into their fourth and third days — while the same repository's weekly-digest approval loop demonstrably works (W33/W34 digest drafts, issues #376/#412, human-processed and closed 2026-08-18; W35 draft #456 filed 2026-08-24 on cadence) — so the unanswered attestations trace to the override-watch contract's missing routing/SLA/escalation, not to a generally unattended issue tracker. **Verified 2026-08-26 — the first human engagement is a self-attestation by the actor:** issue #451 was closed 2026-08-25 17:40 UTC by the repository owner — the same account whose zero-approval self-merge the issue reports — with the single comment "acknowledged", while duplicate #453 remains open with zero comments. Decision [2026-08-17-0001] requires attestation by a human **other than** the actor, but nothing in the workflow or script verifies the attester's identity, so the closure was accepted silently — live evidence for the question's "who may close an attested issue" clause. **Verified 2026-08-31 — the month closes with zero third-party attestations:** the second cluster repeated the full cycle (detected as issue #464, self-attested by the actor citing the token gap, re-filed identically as #469 the next morning, then aged out of the 2-day lookback), the 2026-08-29/30 watch runs filed nothing, and #453/#469 stand open unattested into their tenth and third days — the watch's entire operating history is now two self-attestations and silence (Clarification [2026-08-23]).

- **Issue-Coding Loop Shipped Outside the Requirements Surface; Runner Code Contradicts the “Not a Runtime” Claim** (verified 2026-08-18): Decision [2026-08-16-0004] (PR #409, merged 2026-08-17) added the customer-facing issue-coding loop’s full spec surface — persona `agents/engineering/issue-conductor.md`, skills `classification/issue-intake` and `orchestration/issue-plan`, `docs/architecture/issue-coding-loop.md`, `docs/model-routing.json`, `docs/entitlements.schema.json`, and `tenants/internal.json` (a new top-level directory class holding tenant-entitlement config) — and merged PR #423 (2026-08-18, Decision [2026-08-17-0004] after the collision renumber in this PR) places the Stage A **runner code** in this repository (“do not add a second repo”). No Core Requirement covers any of it, so the audit gate protects none of the loop’s contracts (stage model families, `noemi-conductor`/`noemi-agent`/`noemi-reviewer-bot` identity separation, skip/budget stops, `planRedTeam.maxCycles`), the Overview’s “not a runtime or execution engine” claim is now contradicted by deliberately housed runner code, and no policy states whether `tenants/` may ever hold real customer configs in this public FSL-licensed repo. See Clarification [2026-08-18]. **Verified 2026-08-23 — the loop is now live-wired and released, still with zero requirements coverage:** the 2026-08-20 merge series (#428, #448, #449; Decisions [2026-08-18-0004]–[2026-08-18-0010] and [2026-08-20-0005]–[2026-08-20-0007]) shipped the full Stage A→D pipeline — fail-closed Stage A sufficiency, Stage B plan drafts, **live Gemini** Stage B′ (`coding-loop/critic.js`, ADC auth), **live Grok** Stage C PR-opening (`coding-loop/writer.js`, fail-closed on `XAI_API_KEY`, xAI catalogue-resolved model), a reusable pickup workflow (`.github/workflows/coding-loop.yml`) with a fleet caller template (`templates/ci/coding-loop-caller.yml`), and a `--profile spec` mode that reuses the loop to author personas and skills — and Release `2026.08.21` promoted all of it to `main`. Decision [2026-08-20-0006] resolved the runtime tension by doctrine ("product loop vs orchestration host": the loop code lives here; hosts only trigger/persist/resume), but the Overview's "not a runtime" sentence and the missing Core Requirement are unchanged, so the audit gate still protects none of the loop's contracts even as they execute live model calls and open PRs. In-flight PR #450 (Decision [2026-08-20-0008] minted there) adds Claude Code as an interactive host and a `bulk` model family. **Verified 2026-08-26 — #450 merged 2026-08-25 (compliant two-person loop), but its docs-typed payload strands the decided config:** the `bulk` stage now exists in `develop`'s `docs/model-routing.json` while the tag-relative content gate sees nothing to promote, and the fleet coding-loop caller resolves `@main` — so fleet behavior diverges from Decision [2026-08-20-0008] until unrelated releasable work arrives (see the [2026-08-19] docs-only-stranding Clarification, second instance).

- **Skills Distribution Surface (`skills-dist/`) Shipped Outside the Requirements Surface; Publication Is Stranded on `develop` by the Content Gate** (verified 2026-08-19): PRs #430/#431 (merged 2026-08-18, `noemi-agent`-authored) shipped Phase 1 of an external skills-distribution pipeline — `skills-dist/<slug>/SKILL.md` artifacts targeting **the skills.sh ecosystem**, byte-deterministically built from `skills/` by `scripts/generate_all.js`, with a fatal drift/extra/missing audit check (`checkSkillsDistDeterminism`), a build-verified FSL license notice, a withhold-placeholder-skills gate, and `main`-pinned source links. Enforcement is strong, but no Core Requirement or `DECISION_LOG.md` entry names the surface: nothing governs the registry contract (which ref skills.sh consumes), the phase plan ("Phase 1" is named only in a commit message), or the truth conditions of the embedded governance badge ("passed cross-model review before publication" — stamped unconditionally by the builder even though merges over failing reviews are a live, calibration-logged pattern). Two concrete truth-drifts are live: **(a)** `main`'s `skills-dist/` holds only 4 of 11 artifacts (the promotion that became Release `2026.08.18` captured #430 but not #431), while `develop`'s 11 artifacts cite `blob/main/skills/...` source links whose `main` copies still carry the `TBD` placeholders the pipeline itself refuses to publish; **(b)** the stranding is structural, not transient — #431's payload is `docs`+`chore` only, which the tag-relative content gate (`.github/workflows/release.yml`: "a hundred chore/docs/ci" commits never release) will never promote on its own, so a docs-only skill correction can leave the public registry serving stale or placeholder content indefinitely. See the two Clarifications [2026-08-19]. **Verified 2026-08-23 — the stranding instance healed by rider, exactly as predicted; the structural gap is unchanged:** the 2026-08-20 `feat`/`fix` series (#428/#434/#438/#440 and then #449) triggered promotions that became Releases `2026.08.20` and `2026.08.21`, incidentally carrying #431's docs-only skill content — `main` now serves **13 of 13** artifacts (the catalogue grew by `governed-loop` and `spec-author`) with substantive source files behind the `blob/main/` links. Conformance also strengthened: Decision [2026-08-20-0001] (PR #438) aligned the artifacts with the open Agent Skills spec and put the official `skills-ref` validator into CI. But nothing changed structurally — no requirement names the surface, no alert fires when `main`'s artifacts diverge from `develop`'s, and the healing depended on unrelated releasable work happening to arrive within two days.

### 3. Substantive & Safety Drift
- **Substantive Content Baseline (The "TBD" Problem)**: most agent personas still use boilerplate for `Data Inventory` and `Refusal Criteria`, bypassing the D2 (Description) and Refusal Principle mandates. **Skills half remediated by action 2026-08-18:** PR #431 (`noemi-agent`-authored, human-merged) completed substantive, role-specific `Data Inventory`, `Refusal Criteria`, and audit-log sections for the seven placeholder skills, so all 11 reusable skills are now substantively compliant — verified 2026-08-19: the only remaining `TBD` under `skills/` is the template itself (`skills/SKILL_TEMPLATE.md`), and the skills-dist builder's withhold-placeholders gate publishes all 11. The personas half of the fleet-wide remediation authorization (Clarification [2026-04-26], deferred by Decision [2026-06-19-0010]) remains open.
- **Audit Script Substantive Blindness**: `scripts/audit-repo.js` verifies the presence of headings but fails to detect "TBD" placeholders or validate the content of `Refusal Criteria` subsections. *(Remediated 2026-08-20: PR #434, `noemi-agent`-authored, merged the fatal placeholder gate — `findPlaceholderViolations()` wired into `auditFile`, Decision [2026-08-18-0002] — so placeholder content in mandatory persona/skill sections now fails the audit.)*

### 4. Referential & Link Integrity Drift
- **Internal Referential Integrity Gap (Partial)**: Automated markdown-link verification exists only for the five front-door documents (`tests/contracts.test.js`, "all local markdown links in front-door docs resolve to existing files"). Links inside `agents/`, `skills/`, `docs/agents/`, and `**Skill:**` path references within agent workflows still have no automated verification — the audit gate (`scripts/audit-repo.js`) performs no link checks at all. The 2026-07-14 reality check verified all `**Skill:**` references manually, but nothing prevents regression.
- **Documentation Mirror Contract Drift**: `templates/context/CLAUDE.template.md` ("Create matching documentation in `docs/agents/{domain}/{name}/`") and `templates/context/GEMINI.template.md` ("Documentation mirroring the `agents/` structure") still state the per-agent 1:1 mirror rule that Decision [2026-04-02] softened to "the directory and guide level, not... a mandatory 1:1 duplicated persona-file or symlink mirror" — so the generated context files hand contributors a stricter rule than the decided contract. In practice the mirror is also internally inconsistent: per-agent documentation exists for roughly 5 of 26 agents, `docs/agents/coding/bolt/` and `docs/agents/coding/sentinel/` are `.gitkeep`-only stubs, `docs/agents/education/` is missing at the domain level, and the Fleet Dashboard's setup guide lives under the stale pre-rename directory `docs/agents/operations/dashboard/` while the canonical `docs/agents/operations/fleet-dashboard/` holds only `MULTI_TENANT_REQUIREMENTS.md` (see Clarification [2026-07-25] Documentation Mirror Contract).
- **Audit Script File Verification Gap**: `scripts/audit-repo.js` does not verify that skills or MCPs referenced in `mcp.config.json` exist as physical files.
- **Decision-ID Uniqueness Unenforced** (verified 2026-08-14): `DECISION_LOG.md` entry IDs (`[YYYY-MM-DD-NNNN]`) are the citation keys the whole documentation set resolves against, but no audit check or documented convention enforces their uniqueness or governs allocation across parallel sessions. First live collision: the 2026-08-13 interactive session (PR #390) and the 2026-08-13 scheduled doc run (PR #386) each minted `[2026-08-13-0001]`, and both merged the same day, leaving Requirement §4's citation ambiguous until the 2026-08-14 run renumbered the doc-run entry to `[2026-08-13-0003]` (skipping `-0002`, claimed by in-flight PR #393 — a second collision avoided only by manually checking open PRs). With daily scheduled runs, interactive sessions, and bot-authored PRs all appending decision entries, same-day collisions are structural; a duplicate currently merges silently (see Clarification [2026-08-14]). **Verified 2026-08-15 — the second collision arrived one day later, created by the fix for the first:** the 2026-08-14 renumbering's replacement ID `[2026-08-13-0003]` was itself already claimed — by in-flight PR #392's Grok fourth-stack decision, which the renumbering run missed while checking PR #393's claim on `-0002` — and both PRs merged 2026-08-14/15, putting two `[2026-08-13-0003]` headings live on `develop` until the 2026-08-15 run re-renumbered the doc-run record to `[2026-08-13-0004]` (Decision [2026-08-15-0001]). Even a session that actively sweeps in-flight claims can miss one; the proposed fatal audit check is the demonstrated minimum. **Verified 2026-08-18 — third collision: flagged pre-merge, merged anyway:** PR #423 minted `[2026-08-17-0003]` (“Coding Loop Lives in This Repository”) while `develop` already carried `[2026-08-17-0003]` (issue-loop 429/5xx retry, merged 2026-08-17). The doc run's sweep of in-flight branch diffs caught it and flagged it on the PR at 01:15 UTC — the first pre-merge catch — but the advisory flag did not bind: #423 merged as-is 98 minutes later (02:53 UTC, merge `2800ecd`), putting two `-0003` headings live on `develop`. The corrective renumber to `[2026-08-17-0004]` was applied in the conflict-resolving merge into the doc run's PR branch and lands with that PR. Net lesson: manual sweeps can detect but not prevent; only the proposed fatal audit check blocks a collision at merge time.

### 5. Remediated Limitations (Archive)
- **Release Promotion Bot (`noemi-release-bot`) Absent from the Machine-Identity Register** (Remediated: 2026-08-14, Decision [2026-08-14-0002]): `docs/MACHINE_IDENTITY.md` now registers `noemi-release-bot` as an **enterprise-level** GitHub App (comments as `noemi-release-bot[bot]`; named owner `@WSwarm`; Actions secrets `RELEASE_APP_ID` / `RELEASE_APP_PRIVATE_KEY`; installed on the fleet orgs `newpush` / `project-noemi` / `newpush-labs`, not `agents` only; Contents + Pull requests write). The register's merge-is-human-only rule is amended with a scoped, sanctioned exception: this App may auto-merge only `develop → main` promotion PRs opened by the release workflow in an installed fleet repo. `docs/RELEASE_PROCESS.md` names the provisioned App, creates it at the enterprise, and cross-references the register. Requirement §7 records the same exception. The broader release-automation codification ([2026-08-05]) is a separate open question.
- **Machine Identity Control Shipped Outside the Requirements Surface** (Remediated: 2026-08-05, Decisions [2026-08-03-0002] and [2026-08-05-0001]): the control shipped by PR #336 (2026-08-02) — `docs/MACHINE_IDENTITY.md` and `scripts/agent-gh.sh` — initially had no requirement, no decision entry, and no mandate level. All three gaps closed: the Product Owner's 2026-08-03 directive made `noemi-agent` authorship **mandatory** for agent PRs (recorded as Decision [2026-08-03-0002], codified where orchestrators read it as `mcp-protocols/github.md` §6 with `scripts/agent-pr.js` as the no-`gh` remote path, self-applied by the re-authoring of PRs #343→#347 and #345→#348), and the 2026-08-05 doc run codified it as the Requirement §7 machine-identity bullet plus `docs/DEV_AGENT_PROMPT.md`/`docs/SYNC_AGENT_PROMPT.md` authorship sections. Provisioning is register-attested for both identities (`noemi-agent` Provisioned 2026-08-02, user ID `311935378`; `noemi-reviewer` Provisioned 2026-08-03, capabilities verified); the effective-permission caveat was decided by the repository owner on 2026-08-03 (bots remain in the `developers`/`coders` teams; **token scoping is the boundary** — fine-grained tokens only, re-probe after rotation, never widen to clear a `403`); and the `enforce_admins` posture is decided and documented in the register (`true` with empty bypass allowances on `main` via `scripts/setup-branch-protection.sh`; deliberately `false` on `develop` until agent PRs are reliably bot-authored, so admins keep an escape hatch).
- **Governed Doc-Automation Review Pipeline Stall** (Remediated: 2026-07-29): the fifteen-day review stall ended with a human intervention at 12:42–12:48 UTC on 2026-07-29 — the final subsumption PR #321 (which carried the omnibus #315, the 2026-07-28 record #319, and the eleven earlier drafts #293–#314 with no information loss) was reviewed and **merged into `develop`**, and the thirteen superseded governed drafts plus the fourteen direct-to-main automation PRs were closed unmerged. `develop` moved from `23bc278` (frozen since 2026-07-15) to `bc5a5eb`, carrying every stalled decision-backed implementation (the `FORCE_DOCKER_SMOKE` mode, `checkPhaseZeroKit()`, five-key Audit Log schema validation, `tools/roi/baseline-config.json`, `compassion-lens.md`) into the integration branch — verified this run: `npm run validate` passes (audit gate + 44/44 tests) and regeneration is deterministic on the merged tree. The consolidate-then-subsume strategy (Decisions [2026-07-26-0001], [2026-07-28-0001], [2026-07-29-0001]) was validated by the outcome: reviewers merged one PR to absorb the entire stalled series. The weekly `develop → main` release (sub-tasks (i)/(iii) of Decision [2026-07-10-0002]) is now the sole remaining blocker for `main`'s currency, and the serialization-policy question (Clarification [2026-07-21]) remains open for future stalls.
- **CI/CD "False Green" Risk in E2E** (Remediated: 2026-07-16, Decision [2026-07-05-0006]): `tests/e2e/docker-smoke.test.js` now supports the opt-in `FORCE_DOCKER_SMOKE=true` flag that promotes a missing Docker binary from "skip" to "fail" while preserving the clean-skip default for local development, and the CI `docker-smoke` job in `.github/workflows/validate.yml` sets the flag so a runner misconfigured to lack Docker fails loudly. Verified: forced mode fails on a Docker-less host; default mode still skips 3/3 cleanly.
- **Phase 0 Audit Gap** (Remediated: 2026-07-16, Decision [2026-07-05-0011]): `scripts/audit-repo.js` now runs `checkPhaseZeroKit()`, a fatal file-presence check for the eight kit files mandated by Requirement §1 in `docs/phase-zero-assessment/` (`security-assessment.md`, `ai-readiness-assessment.md`, `network-security-assessment.md`, `PRACTITIONER_NOTES.md`, `consent-template.md`, `report-template.md`, `roadmap-template.md`, `readiness-rubric.md`). Verified: removing a kit file fails the audit with exit 1.
- **Audit Log Schema Validation Gap** (Remediated: 2026-07-16, per Requirement §3 / Decision [2026-05-20]): `scripts/audit-repo.js` now validates that every persona and skill Audit Log JSON carries the five mandatory keys (`task`, `inputs`, `actions`, `risks`, `result`) in addition to the existing valid-JSON check. All 26 personas and 8 skills pass; a missing key fails the audit with exit 1.
- **ROI Auditor Baseline Snapshot Missing** (Remediated: 2026-07-16, Decision [2026-07-07-0004] / [2026-06-19-0015]): `tools/roi/baseline-config.json` now exists as the committed machine-readable snapshot of the baseline-time and labor-rate dictionaries, derived from the illustrative data in `tools/roi/generate_roi_template.py` (the public Sheets template remains the human-editable source). The `ROI Auditor` persona's Workflow, Capabilities, and External Tooling Dependencies now reference the local snapshot for baseline reads, keeping Google Sheets access append-only.
- **Stale Repository Layout in Generated Context** (Remediated: 2026-07-19): the `Repository Layout` section of `templates/context/CLAUDE.template.md` listed only six top-level directories, omitting nine that now exist (`value-lenses/`, `operating-profiles/`, `tools/`, `templates/`, `tests/`, `n8n-templates/`, `guardian-layer/`, `infrastructure/`, `clients/`). The template and the generated `CLAUDE.md` now describe the full tree, so downstream models no longer receive an incomplete map of the repository (Decision [2026-07-19-0001]).
- **Licensing Posture Not Codified** (Remediated: 2026-07-13, Decision [2026-07-13-0002]): Requirement §11 now codifies the FSL-1.1-Apache-2.0 posture and the Transparent Source Guarantee, and `scripts/audit-repo.js` verifies that `LICENSE` carries the canonical FSL header/SPDX identifier, that `package.json` declares `"license": "FSL-1.1-Apache-2.0"`, and that `docs/TRANSPARENT_SOURCE.md` is present. Any drift is now a fatal audit failure. A future contributor cannot silently change the license posture without tripping the governance gate.
- **Sync-Upstream Regression Harness Outside the Validation Gate** (Remediated: 2026-07-13, Decision [2026-07-13-0003]): `package.json` now exposes `npm run test:sync` (invoking `scripts/test-sync-upstream.sh`), included in `validate:full`; `.github/workflows/validate.yml` runs it as the `sync-upstream-regression` job on every push and PR targeting `develop` and `main`. Requirement §9 lists sync-script regression coverage as part of the canonical validation contract.
- **Red Team Vector JSON Companion Divergence** (Remediated: 2026-07-13, Decision [2026-07-13-0006]): `examples/red-team-gauntlet/test-vectors.json` regenerated as a lockstep companion of the canonical `test-vectors.yaml` (Decision [2026-07-07-0003]) — same vector IDs (`pi-*`, `pii-*`), same field shape, same content. `examples/red-team-gauntlet/test-vectors.schema.json` shipped so the JSON's `$schema` reference resolves. The JSON now self-identifies as the companion (`"canonical": "yaml"`, `"source": "examples/red-team-gauntlet/test-vectors.yaml"`) so downstream tooling cannot mistake it for the source of truth.
- **Ungoverned Value-Lens JSON (compassion-lens)** (Remediated: 2026-07-10): `value-lenses/compassion-lens.md` now exists as the canonical Markdown lens, with `compassion-lens.json` retained as its machine-readable companion — the same dual-format pattern established by `american-dream`. The Project NoéMI Anti-Replacement rule is defined in `value-lenses/README.md` with five operational clauses (Human-Review Gate on Terminal Actions, Workforce Uplift Audit, Contextual Forgiveness, Precarity Consciousness, Anti-Dehumanizing Language) each traced to the persona clauses that already materialize it (`roi-auditor.md` Workforce Uplift Constraint, `student-success-coach.md` Precarity Consciousness, `qa-risk-manager.md` Contextual Forgiveness Rule). `GEMINI.md`, `CLAUDE.md`, and the value-lenses golden fixture were regenerated; full test suite passes 44/44 (Decision [2026-07-10-0003]). An optional follow-up remains open under the "Audit Script Coverage Blind Spot" limitation: extend `scripts/audit-repo.js` to enforce a small JSON schema on lens companion files so no future JSON-only lens can re-introduce this drift.
- **Infrastructure Asset Implementation Gap** (Remediated: 2026-07-05, verified 2026-07-10): `templates/tiers/` now ships substantive `basic.md`, `standard.md`, and `premium.md` tier templates, and `operating-profiles/standard-operating-profile.md` provides the first Markdown profile, which the generator injects into `GEMINI.md`/`CLAUDE.md` (the operating-profiles golden fixture was regenerated accordingly). The assets were salvaged from stale PR #254 in commit `9c19548`. This satisfies the bounded implementation approved by Decision [2026-06-19-0011] and overtakes the tier-template PO-deferral in Decision [2026-06-19-0009]; Clarifications [2026-06-10] and [2026-06-15] were closed as overtaken by events (Decision [2026-07-10-0001]).
- **Automation Branch Flow Documentation Gap** (Remediated: 2026-07-07): Requirement §7 now codifies the develop-only merge invariant established by Decision [2026-07-03-0001] and the automation-branch documentation follow-through from Decision [2026-07-07-0002]. `docs/DEV_AGENT_PROMPT.md` and `docs/SYNC_AGENT_PROMPT.md` state the invariant explicitly. All governed automation (`claude/*`, `sync/upstream-*`, `doc/*`) targets `develop`; `main` is reached only via a periodic release PR.
- **Red Team Vector Format Ambiguity** (Resolved: 2026-07-07): the shipped `examples/red-team-gauntlet/test-vectors.yaml` is canonical per Decision [2026-07-07-0003]; Decision [2026-06-19-0008] (which prescribed a JSON alternative) is annotated as superseded. No file rewrite is required.
- **ROI Auditor Baseline Access Ambiguity** (Resolved: 2026-07-07; implemented 2026-07-16): the committed `tools/roi/baseline-config.json` snapshot path from Decision [2026-06-19-0015] is canonical per Decision [2026-07-07-0004]; the live-Sheets alternative in Decision [2026-06-12-0001] is annotated as superseded. The snapshot file now exists (see the ROI Auditor Baseline Snapshot Missing entry above).
- **SecretOps Provider Bias** (Remediated: 2026-07-05): `tests/examples-smoke.test.js` now accepts either `op://` or `infisical://` vault-reference patterns and either `op run` or `infisical run` Compose wrappers, satisfying the multi-provider mandate in Requirement §5 (Decision [2026-07-05-0007]).
- **Heading Inconsistency** (Resolved by design: 2026-07-05): the dual naming — `Rules & Constraints` for personas, `Rules & Constraints (4D Diligence)` for skills — is retained; `scripts/audit-repo.js` performs case-insensitive comparison and tolerates the suffix per Decision [2026-05-28-0004], so no rename is needed (Decision [2026-07-05-0010]).
- **Sync Script Identity Drift** (Remediated: 2026-06-19, verified 2026-07-05): `scripts/sync-upstream.sh` now reads `NOEMI_UPSTREAM_REMOTE`, `NOEMI_UPSTREAM_URL`, and related values from the environment with upstream-preserving defaults; the hardcoded `MY_ORGANIZATION` placeholder is gone (Decision [2026-06-19-0002]).
- **Red Team Gauntlet Serialization Gap** (Remediated, verified 2026-07-05): machine-readable starter vectors now ship as `examples/red-team-gauntlet/test-vectors.yaml` (five vectors: three prompt-injection, two PII), consumable by the `Client Onboarding` validation workflow (Decision [2026-06-19-0008]; shipped as YAML rather than the JSON named in the decision). A JSON companion `test-vectors.json` was additionally salvaged from PR #254 in commit `9c19548` (2026-07-05); YAML remains the canonical format per Decision [2026-07-07-0003], and the companion has since been found divergent from it (see the Red Team Vector JSON Companion Divergence entry under Referential & Link Integrity Drift).
- **Smoke Test Variable Validation Gap** (Remediated: 2026-06-19, verified 2026-07-05): `tests/examples-smoke.test.js` now bidirectionally validates every `NOEMI_DOCKER_SMOKE_*` variable referenced by the e2e suite against `.env.template` (Decision [2026-06-19-0003]).
- **Missing shared `audit_logger.js`** (Remediated: 2026-06-19): The shared utility for structured JSON Audit Log emission is now available in the `scripts/` directory.
- **Missing Onboarding and Configuration Directories** (Remediated: 2026-05-28): `clients/`, `.gatekeeper/`, and `templates/tiers/` directories now exist with `.gitignore` placeholders.
- **Framework Injection Gap** (Remediated: 2026-05-28): `Value Lenses` and `Operating Profiles` are now injected by `scripts/generate_all.js`.
- **Artifact Naming Convention Alignment** (Remediated: 2026-05-10): `docs/n8n workflows/` renamed to `docs/n8n-workflows/`.
- **Node.js 24 Baseline Alignment** (Remediated: 2026-05-10): `executive-assistant` and `gatekeeper-deployment` updated to Node 24 images.
