# Docker Sandbox — Required Environment Variables
# Inject at runtime: op run --env-file=.env -- docker compose up -d
#              or:   infisical run --env=dev -- docker compose up -d
# Do NOT commit real values. This file shows variable names only.

POSTGRES_PASSWORD=changeme
