# Fleet Deployment — Required Environment Variables
# Inject at runtime: op run --env-file=.env.example -- docker compose up -d
#              or:   infisical run --env=dev -- docker compose up -d
# Do NOT commit real values. Use vault references or placeholders only.

GF_ADMIN_PASSWORD=op://noemi/grafana/admin-password
CASDOOR_DB_PASSWORD=op://noemi/casdoor-db/password
COHORT01_DB_PASSWORD=op://noemi/cohort01-db/password
COHORT02_DB_PASSWORD=op://noemi/cohort02-db/password
